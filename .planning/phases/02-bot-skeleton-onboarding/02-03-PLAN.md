---
phase: 02-bot-skeleton-onboarding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/bot-sessions.ts
  - src/db/schema/index.ts
  - scripts/verify-schema.ts
  - drizzle/0003_bot_sessions.sql
  - drizzle/0004_bot_sessions_rls.sql
  - drizzle/meta/_journal.json
autonomous: false
requirements: [ONBOARD-01, ONBOARD-05]

must_haves:
  truths:
    - "A bot_sessions table exists in the live Supabase database, proven by querying the database, not by the build passing"
    - "bot_sessions has Row Level Security enabled with zero policies, matching how users/diary/fdc_foods were protected"
    - "The migration SQL was read by the owner before it touched the real database"
    - "npm run verify-schema reports bot_sessions alongside the three Phase 1 tables"
  artifacts:
    - path: "src/db/schema/bot-sessions.ts"
      provides: "Drizzle definition of the key/value store backing conversation state"
      exports: ["botSessions", "BotSessionRow", "NewBotSessionRow"]
      contains: "pgTable('bot_sessions'"
    - path: "drizzle/0003_bot_sessions.sql"
      provides: "Generated CREATE TABLE for bot_sessions"
      contains: "bot_sessions"
    - path: "drizzle/0004_bot_sessions_rls.sql"
      provides: "RLS lockdown for bot_sessions, mirroring 0002_enable_rls.sql"
      contains: "ENABLE ROW LEVEL SECURITY"
  key_links:
    - from: "src/db/schema/index.ts"
      to: "src/db/schema/bot-sessions.ts"
      via: "barrel re-export consumed by drizzle.config.ts"
      pattern: "export \\* from './bot-sessions'"
    - from: "scripts/verify-schema.ts"
      to: "bot_sessions table in the live database"
      via: "REQUIRED_TABLES existence + RLS checks"
      pattern: "bot_sessions"
---

<objective>
Create the `bot_sessions` table that grammY's session/conversation storage needs,
through the repository's mandated migration workflow: write the Drizzle schema,
generate reviewable SQL, have the owner read it, then apply it and prove against
the live database that the table is really there.

Purpose: `.planning/research/ARCHITECTURE.md` Anti-Pattern 3 forbids keeping
conversation state in process memory — a half-finished onboarding must survive
`npm run bot` being restarted. That durability requires a real table, and this
repository's `drizzle.config.ts` bans `drizzle-kit push`, so the table only
exists once a reviewed SQL file has been applied.

Output: `src/db/schema/bot-sessions.ts`, two committed migration files, an
extended `npm run verify-schema`, and a table that exists in Supabase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-bot-skeleton-onboarding/02-CONTEXT.md
@.planning/phases/02-bot-skeleton-onboarding/02-RESEARCH.md
@.planning/phases/02-bot-skeleton-onboarding/02-PATTERNS.md
@.planning/phases/01-foundation-data-domain-math/01-CONTEXT.md
</context>

<interfaces>
<!-- Contract Plan 04's storage adapter implements against. Do not redefine. -->

src/db/schema/bot-sessions.ts exports:
  export const botSessions = pgTable('bot_sessions', {
    key: text('key').primaryKey(),                 // grammY session key
    value: jsonb('value').notNull(),               // plugin-owned serialized state
    updatedAt: timestamp('updated_at', { withTimezone: true }).defaultNow().notNull(),
  });
  export type BotSessionRow = typeof botSessions.$inferSelect;
  export type NewBotSessionRow = typeof botSessions.$inferInsert;
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: bot_sessions Drizzle schema, barrel export, and verify-schema coverage</name>
  <files>src/db/schema/bot-sessions.ts, src/db/schema/index.ts, scripts/verify-schema.ts</files>
  <read_first>
    - src/db/schema/diary.ts (the closest existing simple table: the "why this table, why this shape, what is deferred" doc-comment header, the `pgTable` call style, the `$inferSelect`/`$inferInsert` type exports at the bottom)
    - src/db/schema/index.ts (three re-export lines, ordered by creation, not alphabetically — append, do not sort)
    - drizzle/0002_enable_rls.sql (the exact rationale and SQL shape for the RLS lockdown this table also needs)
    - scripts/verify-schema.ts (`REQUIRED_TABLES` at line 62 and every place it is consumed: the existence check around lines 62-80 including the hardcoded human-readable detail string, and the RLS check around lines 259-270)
    - .planning/phases/02-bot-skeleton-onboarding/02-PATTERNS.md — the `src/db/schema/bot-sessions.ts` and `drizzle/0003_bot_sessions.sql` sections
    - .planning/research/ARCHITECTURE.md — Anti-Pattern 3 (conversation state must not live in process memory)
  </read_first>
  <action>
    Create `src/db/schema/bot-sessions.ts` exactly per <interfaces>, with a
    doc-comment header in the style of `diary.ts` that states: this table backs
    grammY's session and `@grammyjs/conversations` persistence; the `key` is the
    grammY session key and the `value` is the plugin's own serialized blob, so
    this project never interprets its contents; `jsonb` is used rather than
    `text` so Postgres validates the JSON; and the table gets
    `ENABLE ROW LEVEL SECURITY` with zero policies for the same Supabase
    PostgREST-exposure reason documented in `drizzle/0002_enable_rls.sql`.

    Append `export * from './bot-sessions';` as the fourth line of
    `src/db/schema/index.ts` — this is what makes `drizzle-kit generate` (which
    reads `./src/db/schema/index.ts` per `drizzle.config.ts`) see the new table.
    Without this line the next task generates an empty migration.

    Extend `scripts/verify-schema.ts`:
    - Add `'bot_sessions'` to `REQUIRED_TABLES`. This automatically covers both
      the table-existence check and the RLS-enabled check, since both iterate
      that constant.
    - Update the hardcoded Russian success string that currently reads
      "users, diary, fdc_foods — все три таблицы найдены" so it is generated
      from `REQUIRED_TABLES` instead of naming three tables — otherwise the
      message lies the moment a fourth table is added. Do the same for the RLS
      check's success detail string.
    - Do not add a `bot_sessions`-specific column check; the table is a plain
      key/value store with no constraints worth asserting.

    Do NOT run `db:generate` or `db:migrate` in this task — that is Task 2 and
    Task 3, deliberately separated so the generated SQL is reviewed before it is
    applied.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('src/db/schema/bot-sessions.ts','utf8');for(const t of [\"pgTable('bot_sessions'\",'jsonb','primaryKey','BotSessionRow','NewBotSessionRow']){if(!s.includes(t))throw new Error('bot-sessions.ts missing '+t)}const idx=fs.readFileSync('src/db/schema/index.ts','utf8');if(!idx.includes(\"./bot-sessions\"))throw new Error('barrel missing bot-sessions export');const v=fs.readFileSync('scripts/verify-schema.ts','utf8');if(!/REQUIRED_TABLES[^;]*bot_sessions/s.test(v))throw new Error('verify-schema REQUIRED_TABLES missing bot_sessions');if(/все три таблицы/.test(v))throw new Error('verify-schema still claims three tables');console.log('schema OK')" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema/bot-sessions.ts` defines `pgTable('bot_sessions', ...)` with a `text` primary key `key`, a `notNull` `jsonb` `value`, and a `defaultNow().notNull()` `updated_at` timestamptz
    - `src/db/schema/index.ts` has exactly four `export *` lines, with `./bot-sessions` last
    - `scripts/verify-schema.ts` `REQUIRED_TABLES` contains `bot_sessions`
    - No string in `scripts/verify-schema.ts` hardcodes "все три таблицы" or an explicit three-table list
    - `npx tsc --noEmit` exits 0 and `npm test` exits 0
    - No migration was generated or applied in this task
  </acceptance_criteria>
  <done>The table is defined in TypeScript and visible to drizzle-kit, and the existing schema-verification script will report on it once it exists for real.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the migration SQL and hand-write the RLS lockdown — generate only, do not apply</name>
  <files>drizzle/0003_bot_sessions.sql, drizzle/0004_bot_sessions_rls.sql, drizzle/meta/_journal.json</files>
  <read_first>
    - drizzle.config.ts (the header comment banning `drizzle-kit push` and the `schema: './src/db/schema/index.ts'` / `out: './drizzle'` settings)
    - drizzle/0001_init_schema.sql (what generated `CREATE TABLE` output looks like in this repo, so a deviation is recognisable)
    - drizzle/0002_enable_rls.sql (the complete file — the comment explaining WHY, the `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` lines, and the guarded `DO $$` block that revokes grants from `anon`/`authenticated` only when those roles exist)
    - drizzle/meta/_journal.json (how applied/pending migrations are tracked — this file must be committed alongside the SQL)
    - src/db/schema/bot-sessions.ts (Task 1)
  </read_first>
  <action>
    Run `npm run db:generate`. drizzle-kit reads the schema barrel, diffs against
    its stored snapshot, and writes the next numbered SQL file under `drizzle/`
    plus an updated snapshot and `_journal.json`. Expect `0003_<slug>.sql`; if
    drizzle-kit picks a random slug, that is fine — record the real filename in
    the SUMMARY and use it everywhere below instead of the placeholder name.

    Immediately read the generated file and confirm it contains exactly one
    `CREATE TABLE "bot_sessions"` with the three expected columns and no
    `DROP` statement of any kind. If it contains a `DROP TABLE`, a `DROP COLUMN`,
    or touches `users`, `diary` or `fdc_foods` at all, STOP and report — that
    means the stored snapshot is out of sync with the live database and applying
    it could destroy Phase 1 data. Do not "fix" it by editing the file.

    Then hand-write `drizzle/0004_bot_sessions_rls.sql`, modelled line-for-line
    on `drizzle/0002_enable_rls.sql`:
    - A leading comment in Russian explaining, for someone who has not seen RLS
      before, why this exists: Supabase publishes every `public` table through
      its PostgREST API to anyone holding the public anon key, so a table with
      RLS off is world-readable. With RLS on and no policies, that API returns
      nothing, while the bot's own `postgres` role (BYPASSRLS) is unaffected.
      Add the table-specific note: `bot_sessions` holds partially-completed
      onboarding answers (sex, age, weight), so it is exactly as sensitive as
      `users`.
    - `ALTER TABLE "bot_sessions" ENABLE ROW LEVEL SECURITY;`
    - The same existence-guarded `DO $$ ... END $$;` block revoking all grants on
      `bot_sessions` from `anon, authenticated`, so the migration is still a
      no-op on a non-Supabase Postgres.

    Register `0004_bot_sessions_rls.sql` in `drizzle/meta/_journal.json` the same
    way `0002_enable_rls.sql` is registered, since it is hand-written rather than
    generated. Follow whatever shape the existing entry for `0002` has exactly.

    Do NOT run `npm run db:migrate` here. Do NOT run `drizzle-kit push` — it is
    banned by `drizzle.config.ts` and can silently drop columns. Print both SQL
    files in full to the terminal at the end of this task so the next task's
    reviewer already has them on screen.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=fs.readdirSync('drizzle').filter(f=>f.endsWith('.sql')).sort();const created=files.filter(f=>/bot_session/i.test(f)||/^0003_/.test(f));if(created.length<1)throw new Error('no new migration generated');const gen=fs.readFileSync('drizzle/'+created[0],'utf8');if(!/CREATE TABLE[^;]*bot_sessions/i.test(gen))throw new Error('generated migration does not create bot_sessions');if(/DROP\s+(TABLE|COLUMN)/i.test(gen))throw new Error('generated migration contains a DROP — STOP and investigate snapshot drift');const rls=files.find(f=>/rls/i.test(f)&&/bot_session/i.test(f));if(!rls)throw new Error('bot_sessions RLS migration missing');const r=fs.readFileSync('drizzle/'+rls,'utf8');if(!/ENABLE ROW LEVEL SECURITY/i.test(r))throw new Error('RLS migration does not enable RLS');if(!/pg_roles/.test(r))throw new Error('RLS migration missing the guarded anon/authenticated revoke');const j=fs.readFileSync('drizzle/meta/_journal.json','utf8');if(!j.includes(rls.replace('.sql','')))throw new Error('RLS migration not registered in _journal.json');console.log('migrations generated (not applied): '+created[0]+', '+rls)"</automated>
  </verify>
  <acceptance_criteria>
    - A generated migration exists containing `CREATE TABLE "bot_sessions"` with columns `key`, `value`, `updated_at`
    - The generated migration contains no `DROP TABLE` or `DROP COLUMN`
    - `drizzle/0004_bot_sessions_rls.sql` exists, enables RLS on `bot_sessions`, and contains the `pg_roles`-guarded revoke block
    - Both migration filenames appear in `drizzle/meta/_journal.json`
    - `drizzle/meta/*_snapshot.json` is updated and staged
    - `npm run db:migrate` was NOT run in this task
    - Both SQL files were printed in full for the owner to read
  </acceptance_criteria>
  <done>Two reviewable SQL files exist on disk, the generated one verified to create only `bot_sessions` and drop nothing, and nothing has touched the real database yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Owner reviews the migration SQL, then it is applied and proven against the live database</name>
  <files>drizzle/0003_bot_sessions.sql, drizzle/0004_bot_sessions_rls.sql</files>
  <read_first>
    - drizzle/0003_bot_sessions.sql and drizzle/0004_bot_sessions_rls.sql (Task 2 — the exact SQL about to run)
    - drizzle.config.ts (the review-before-apply rule this checkpoint enforces)
    - scripts/verify-schema.ts (Task 1 — the command that proves the table really exists)
  </read_first>
  <what-built>
    Task 1 defined the table in TypeScript. Task 2 generated the `CREATE TABLE`
    SQL and hand-wrote the RLS lockdown, and verified the generated file drops
    nothing. Nothing has been applied to the real Supabase database yet — this
    project's rule is that a human reads every SQL file before it touches the
    real database, because a migration is the one operation that can destroy
    data that already exists.
  </what-built>
  <action>
    Print `drizzle/0003_bot_sessions.sql` and `drizzle/0004_bot_sessions_rls.sql`
    in full, present the review framing below to the owner, and pause. After the
    owner approves, run `npm run db:migrate` and then `npm run verify-schema`,
    showing the output of each. On any failure, stop and report the error
    verbatim — never retry with `drizzle-kit push`.
  </action>
  <how-to-verify>
    Present both SQL files to the owner in full, with this framing in Russian:

    "«Миграция» — это файл с SQL-командами, который меняет структуру базы
    данных. Мы применяем миграции только из проверенных файлов, потому что
    неправильная миграция может удалить существующие данные. Прочитай оба
    файла ниже. Первый создаёт новую таблицу `bot_sessions` — в ней бот хранит,
    на каком шаге анкеты остановился пользователь, чтобы разговор не терялся
    при перезапуске бота. Второй закрывает эту таблицу от публичного API
    Supabase — так же, как мы уже сделали для `users`, `diary` и `fdc_foods` в
    Фазе 1.

    На что смотреть:
    1. В первом файле должно быть только `CREATE TABLE "bot_sessions"` с тремя
       колонками. Слова `DROP` быть не должно нигде.
    2. Ни в одном из файлов не должно упоминаться `users`, `diary` или
       `fdc_foods` — эти таблицы мы не трогаем.
    3. Во втором файле должна быть строка
       `ALTER TABLE "bot_sessions" ENABLE ROW LEVEL SECURITY;`"

    After the owner types "применяй", run in this order and show the output of
    each:
    1. `npm run db:migrate`
    2. `npm run verify-schema`

    If `db:migrate` fails, do not retry with different flags and never fall back
    to `drizzle-kit push`. Report the error verbatim and stop.
  </how-to-verify>
  <resume-signal>Type "применяй" to apply the migration, or describe what looks wrong in the SQL.</resume-signal>
  <verify>
    <automated>npm run verify-schema && node -e "const{execSync}=require('child_process');const out=execSync('npm run -s verify-schema -- --json',{encoding:'utf8'});if(!out.includes('bot_sessions'))throw new Error('verify-schema output does not mention bot_sessions');if(/\[FAIL\]/.test(out))throw new Error('verify-schema reported a failure');console.log('bot_sessions verified against the live database')"</automated>
  </verify>
  <acceptance_criteria>
    - The owner explicitly approved the SQL before `db:migrate` ran
    - `npm run db:migrate` exits 0
    - `npm run verify-schema` exits 0, lists `bot_sessions` among the found tables, and reports RLS enabled on it with no `[FAIL]` line
    - `drizzle-kit push` was never invoked
    - Phase 1's `users`, `diary` and `fdc_foods` tables are still reported as present and RLS-protected by the same run
  </acceptance_criteria>
  <done>`bot_sessions` exists in the real Supabase database with RLS on, proven by querying Postgres system catalogs — not inferred from a green build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL -> live Supabase database | The one operation in this phase that can destroy Phase 1's indexed data |
| `public` schema table -> Supabase PostgREST API | Every `public` table is auto-published to anyone holding the project's public anon key |
| Conversation state -> `bot_sessions.value` | Partially completed onboarding answers (sex, age, weight) are health-adjacent personal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | `bot_sessions` exposed via Supabase PostgREST | mitigate | `drizzle/0004_bot_sessions_rls.sql` enables RLS with zero policies and revokes grants from `anon`/`authenticated`, mirroring `0002_enable_rls.sql`; `npm run verify-schema` asserts RLS is on for `bot_sessions` on every run |
| T-02-09 | Destruction of data | `drizzle-kit push` or an unreviewed generated migration | mitigate | `push` is banned in `drizzle.config.ts`; Task 2's automated check fails the task if the generated SQL contains any `DROP`; Task 3 is a blocking human review gate before `db:migrate` runs |
| T-02-10 | Tampering (false confidence) | Assuming the table exists because the build is green | mitigate | The table's existence is asserted only by `npm run verify-schema`, which queries `information_schema`/`pg_class` in the live database; it is the acceptance criterion for Task 3, not `tsc` |
</threat_model>

<verification>
1. `npm run db:migrate` — exits 0 after explicit owner approval.
2. `npm run verify-schema` — exits 0, `bot_sessions` present, RLS enabled, no `[FAIL]`.
3. `npx tsc --noEmit` and `npm test` — green.
4. `git status --short` shows both SQL files, the updated snapshot and `_journal.json` staged.
</verification>

<success_criteria>
- `bot_sessions` exists in Supabase with RLS enabled and zero policies.
- The SQL that created it was read by the owner first, and `drizzle-kit push` was never used.
- `npm run verify-schema` now covers four tables and would fail loudly if `bot_sessions` were dropped.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bot-skeleton-onboarding/02-03-SUMMARY.md`.
Record: the exact generated migration filename, the full `npm run verify-schema`
output, and confirmation that `drizzle-kit push` was not used.
</output>
