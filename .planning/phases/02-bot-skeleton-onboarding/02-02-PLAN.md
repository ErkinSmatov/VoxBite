---
phase: 02-bot-skeleton-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bot/onboarding/parse-fields.ts
  - src/bot/onboarding/parse-fields.test.ts
  - src/bot/onboarding/options.ts
  - src/bot/onboarding/options.test.ts
  - src/bot/onboarding/rate-presets.ts
  - src/bot/onboarding/rate-presets.test.ts
  - src/bot/onboarding/assemble-profile.ts
  - src/bot/onboarding/assemble-profile.test.ts
  - src/bot/formatting/onboarding-copy.ts
  - src/bot/formatting/onboarding-copy.test.ts
autonomous: true
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-06]

must_haves:
  truths:
    - "Typed age/height/weight input is validated against plausibility bounds and rejected with a Russian re-prompt containing a concrete example"
    - "The only selectable weight-change rates are 0.25, 0.5, 0.75 and 1 kg/month — a value above 1 is unrepresentable"
    - "The rate cap constant is asserted equal to the domain's MAX_RATE_KG_PER_MONTH so UI and domain cannot drift apart"
    - "Every button choice arriving as callback_data is decoded through an allowlist of known values, never trusted as-is"
    - "A complete answer set assembles into a NutritionProfile that calculateNutritionTargets accepts"
    - "The confirmation message contains the calculated targets and the non-medical-device disclaimer"
    - "Every module in this plan imports zero grammY symbols"
  artifacts:
    - path: "src/bot/onboarding/parse-fields.ts"
      provides: "Pure parse/validate for the three typed numeric fields"
      exports: ["ParseResult", "parseAge", "parseHeight", "parseWeight"]
    - path: "src/bot/onboarding/options.ts"
      provides: "Button option lists and safe callback_data encode/decode for sex, activity, goal, timezone"
      exports: ["SEX_OPTIONS", "ACTIVITY_OPTIONS", "GOAL_OPTIONS", "TIMEZONE_OPTIONS", "DEFAULT_TIMEZONE", "decodeOption"]
    - path: "src/bot/onboarding/rate-presets.ts"
      provides: "The four allowed rate values and their safe decode (ONBOARD-02 structural cap)"
      exports: ["RATE_PRESETS_KG_PER_MONTH", "decodeRate"]
    - path: "src/bot/onboarding/assemble-profile.ts"
      provides: "OnboardingAnswers -> NutritionProfile handoff into the Phase 1 domain"
      exports: ["OnboardingAnswers", "assembleProfile"]
    - path: "src/bot/formatting/onboarding-copy.ts"
      provides: "All Russian onboarding message templates including the ONBOARD-06 disclaimer"
      exports: ["DISCLAIMER_TEXT", "targetsWithDisclaimerMessage", "questionCopy"]
  key_links:
    - from: "src/bot/onboarding/assemble-profile.ts"
      to: "src/domain/nutrition/index.ts"
      via: "type-only import of NutritionProfile from the barrel"
      pattern: "domain/nutrition/index.js"
    - from: "src/bot/onboarding/rate-presets.test.ts"
      to: "src/domain/nutrition/constants.ts"
      via: "assertion that the highest preset equals MAX_RATE_KG_PER_MONTH"
      pattern: "MAX_RATE_KG_PER_MONTH"
---

<objective>
Build every piece of onboarding logic that does not need Telegram: numeric field
parsing with Russian re-prompts, the button option lists with safe
`callback_data` decoding, the rate presets that make >1 kg/month structurally
unreachable, the `NutritionProfile` assembly, and all Russian copy including the
non-medical-device disclaimer.

Purpose: D-08 requires the onboarding logic to be unit-tested with zero grammY
imports, and D-09 rules out an end-to-end emulation harness — so this module set
is where essentially all of the phase's automated confidence lives. Everything
here is a pure function with a colocated Vitest file, mirroring how
`src/domain/nutrition/*` was built in Phase 1.

Output: `src/bot/onboarding/*` and `src/bot/formatting/*`, fully tested, ready
for Plan 05's conversation to orchestrate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-bot-skeleton-onboarding/02-CONTEXT.md
@.planning/phases/02-bot-skeleton-onboarding/02-RESEARCH.md
@.planning/phases/02-bot-skeleton-onboarding/02-PATTERNS.md
@.planning/phases/02-bot-skeleton-onboarding/02-VALIDATION.md
@TECH_SPEC.md
</context>

<interfaces>
<!-- Contracts Plan 05's conversation consumes. Do not redefine there. -->

src/bot/onboarding/parse-fields.ts:
  export type ParseResult<T> = { ok: true; value: T } | { ok: false; error: string };
  export function parseAge(text: string): ParseResult<number>;      // integer years
  export function parseHeight(text: string): ParseResult<number>;   // integer cm
  export function parseWeight(text: string): ParseResult<number>;   // kg, one decimal allowed

src/bot/onboarding/options.ts:
  export interface Option<T extends string> { value: T; label: string; callbackData: string }
  export const SEX_OPTIONS: readonly Option<Sex>[];
  export const ACTIVITY_OPTIONS: readonly Option<ActivityLevel>[];
  export const GOAL_OPTIONS: readonly Option<Goal>[];
  export const TIMEZONE_OPTIONS: readonly Option<string>[];         // IANA zone strings
  export const DEFAULT_TIMEZONE = 'Asia/Almaty';
  export function decodeOption<T extends string>(
    options: readonly Option<T>[], callbackData: string | undefined,
  ): T | undefined;                                                  // undefined = unknown/forged

src/bot/onboarding/rate-presets.ts:
  export const RATE_PRESETS_KG_PER_MONTH: readonly [0.25, 0.5, 0.75, 1];
  export function decodeRate(callbackData: string | undefined): number | undefined;

src/bot/onboarding/assemble-profile.ts:
  export interface OnboardingAnswers {
    sex: Sex; ageYears: number; heightCm: number; weightKg: number;
    activityLevel: ActivityLevel; goal: Goal;
    desiredRateKgPerMonth?: number; timezone: string;
  }
  export function assembleProfile(answers: OnboardingAnswers): NutritionProfile;

src/bot/formatting/onboarding-copy.ts:
  export const DISCLAIMER_TEXT: string;                              // ONBOARD-06
  export function targetsWithDisclaimerMessage(t: NutritionTargets): string;
  export const questionCopy: { sex: string; age: string; height: string; weight: string;
    activity: string; goal: string; rate: string; timezone: string;
    greeting: string; saved: string; restarting: string };

Consumed from Phase 1 (import from the barrel only, never the individual files):
  src/domain/nutrition/index.js -> NutritionProfile, NutritionTargets, Sex, Goal, ActivityLevel
  src/domain/nutrition/constants.js -> MAX_RATE_KG_PER_MONTH (test-only import)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure parsing for the three typed numeric fields</name>
  <files>src/bot/onboarding/parse-fields.ts, src/bot/onboarding/parse-fields.test.ts</files>
  <read_first>
    - src/domain/nutrition/target-calories.ts (the repo's existing "validate a number, check plausibility bounds, fail with a descriptive message" shape — note it *throws*; parse-fields must *return* a result instead, because invalid user text is expected input, not a programming error)
    - src/domain/nutrition/bmr-tdee.test.ts (the `it.each` table-driven invalid-input test style to copy)
    - src/config/env.ts lines 17-28 (`buildMissingKeysMessage` — the Russian "state the fact, then state what to do" voice every user-facing string in this repo uses)
    - src/db/schema/users.ts (the column types the parsed values land in: `age_years` integer, `height_cm` integer, `weight_kg` real)
    - .planning/phases/02-bot-skeleton-onboarding/02-CONTEXT.md — "Numeric field entry" under Claude's Discretion
    - .planning/phases/02-bot-skeleton-onboarding/02-PATTERNS.md — the `src/bot/onboarding/parse-fields.ts` section
  </read_first>
  <behavior>
    - `parseAge('29')` → `{ ok: true, value: 29 }`.
    - `parseAge` accepts surrounding whitespace (`'  29 '`) and rejects a decimal (`'29.5'`), non-numeric text (`'двадцать девять'`), empty string, `'NaN'`, `'Infinity'`, and anything outside 10..100 inclusive.
    - `parseHeight` accepts integers 100..250 cm; rejects `'1.78'` (metres), `'0'`, `'300'`, and non-numeric text.
    - `parseWeight` accepts 30..300 kg, allows one decimal place with either `.` or `,` as the separator (`'72,5'` → `72.5`), rejects two decimals (`'72.55'`), rejects out-of-range and non-numeric values.
    - Every `{ ok: false }` result's `error` string is Russian, states the accepted range, and contains a concrete example number (e.g. `178`) — asserted by regex, not by exact string equality, so copy edits do not break the suite.
    - No function ever throws for any string input, including empty string and very long strings.
  </behavior>
  <action>
    Write `src/bot/onboarding/parse-fields.test.ts` first, as an `it.each` table
    per field covering every case in <behavior> plus the boundary values on both
    sides of each range (e.g. 9/10/100/101 for age).

    Then `src/bot/onboarding/parse-fields.ts`:
    - Module doc comment naming ONBOARD-01 and the CONTEXT.md discretion note it
      implements, following the repo convention that every module header says
      which decision it came from.
    - `ParseResult<T>` as the discriminated union in <interfaces>.
    - One small internal helper that trims, replaces `,` with `.`, and rejects
      strings that are not a plain decimal number — do not use bare `Number()`
      alone, because it accepts `''`, `'0x10'`, `'1e3'` and `'Infinity'`.
    - `parseAge` / `parseHeight` / `parseWeight` per <behavior>, each with its
      bounds expressed as named module-level constants (`AGE_MIN`, `AGE_MAX`,
      `HEIGHT_MIN_CM`, `HEIGHT_MAX_CM`, `WEIGHT_MIN_KG`, `WEIGHT_MAX_KG`) so the
      numbers are changeable in one place and quotable in the error text.
    - Weight rounds to one decimal before returning.
    - Zero imports from `grammy` or `@grammyjs/*`. This is a hard rule for
      everything under `src/bot/onboarding/` (D-08).
  </action>
  <verify>
    <automated>npx vitest run src/bot/onboarding/parse-fields.test.ts && npx tsc --noEmit && node -e "const fs=require('fs');const b=fs.readFileSync('src/bot/onboarding/parse-fields.ts','utf8');if(/from '(grammy|@grammyjs)/.test(b))throw new Error('parse-fields.ts must not import grammY');const c=b.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')&&!l.trim().startsWith('/*')).join('\n');for(const t of ['parseAge','parseHeight','parseWeight','AGE_MIN','HEIGHT_MIN_CM','WEIGHT_MIN_KG']){if(!c.includes(t))throw new Error('missing '+t)}console.log('parse-fields OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/bot/onboarding/parse-fields.test.ts` exits 0 with at least 20 cases across the three fields
    - Tests assert both boundary values on each side of every range
    - A test asserts `parseWeight('72,5')` returns `72.5`
    - A test asserts `parseHeight('1.78')` is rejected
    - A test asserts every rejection message matches `/[0-9]/` (contains a concrete number) and is non-empty
    - `parse-fields.ts` contains no import from `grammy` or `@grammyjs/*`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Typed numeric input is validated against plausibility bounds, never crashes, and always re-prompts in Russian with a concrete example — verified by a table-driven suite that needs no Telegram.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Button options and rate presets with allowlisted callback_data decoding</name>
  <files>src/bot/onboarding/options.ts, src/bot/onboarding/options.test.ts, src/bot/onboarding/rate-presets.ts, src/bot/onboarding/rate-presets.test.ts</files>
  <read_first>
    - src/domain/nutrition/types.ts (the exact `Sex`, `Goal`, `ActivityLevel` string unions the option lists must produce)
    - src/domain/nutrition/constants.ts (`ACTIVITY_MULTIPLIERS` keys — the five activity levels and their meaning; `MAX_RATE_KG_PER_MONTH`)
    - src/db/schema/users.ts (the `check` constraints that will reject any value these decoders let through wrongly; `timezone` defaults to `Asia/Almaty`)
    - TECH_SPEC.md §3.2 and §6.2 (the required field list and the five activity gradations, in the owner's own wording)
    - .planning/phases/02-bot-skeleton-onboarding/02-CONTEXT.md — "Rate input (ONBOARD-02)" and "Timezone" under Claude's Discretion
    - .planning/phases/02-bot-skeleton-onboarding/02-PATTERNS.md — the `rate-presets.ts` section, including the note that the top preset must be asserted equal to `MAX_RATE_KG_PER_MONTH`
  </read_first>
  <behavior>
    - `SEX_OPTIONS` has exactly 2 entries with values `male`, `female` and Russian labels.
    - `ACTIVITY_OPTIONS` has exactly 5 entries whose `value`s are exactly the keys of `ACTIVITY_MULTIPLIERS` (`minimal`,`low`,`medium`,`high`,`very_high`), each with a Russian label that describes the activity level in everyday terms, not just a word.
    - `GOAL_OPTIONS` has exactly 3 entries with values `gain`, `loss`, `maintain`.
    - `TIMEZONE_OPTIONS` values are all valid IANA zone strings (each accepted by `Intl.DateTimeFormat` with that `timeZone`), include `Asia/Almaty`, and `DEFAULT_TIMEZONE` is `Asia/Almaty` and is one of the options.
    - Every option's `callbackData` is unique within its list, is at most 64 bytes when UTF-8 encoded (Telegram's hard limit), and is prefixed with its field name (`sex:`, `activity:`, `goal:`, `tz:`).
    - `decodeOption(SEX_OPTIONS, 'sex:male')` → `'male'`; `decodeOption(SEX_OPTIONS, 'sex:other')`, `decodeOption(SEX_OPTIONS, 'goal:loss')`, `decodeOption(SEX_OPTIONS, undefined)` and `decodeOption(SEX_OPTIONS, '')` all → `undefined`.
    - `RATE_PRESETS_KG_PER_MONTH` is exactly `[0.25, 0.5, 0.75, 1]`, and its maximum equals `MAX_RATE_KG_PER_MONTH` imported from the domain constants.
    - `decodeRate('rate:0.5')` → `0.5`; `decodeRate('rate:2')`, `decodeRate('rate:1.0001')`, `decodeRate('2')`, `decodeRate(undefined)` all → `undefined`.
  </behavior>
  <action>
    Write both test files first.

    `src/bot/onboarding/options.ts`:
    - Module doc comment naming ONBOARD-01 and stating that the `value` fields
      must stay identical to the Postgres `check` constraint literals in
      `src/db/schema/users.ts`, since these strings are written straight into
      that table.
    - The four option lists per <behavior>, typed against the domain's `Sex`,
      `Goal`, `ActivityLevel` (type-only imports from
      `../../domain/nutrition/index.js`, the barrel).
    - Activity labels must be usable by someone who has never seen a TDEE table:
      pair the level name with a concrete description, e.g. minimal = сидячая
      работа, почти нет движения; very_high = тяжёлый физический труд или
      2 тренировки в день. Source the gradation meanings from TECH_SPEC §6.2.
    - Timezone list: `Asia/Almaty`, `Asia/Aqtobe`, `Asia/Tashkent`,
      `Europe/Moscow`, `Asia/Dubai`, `Europe/Berlin` — with Russian city labels
      and the UTC offset in the label. Keep the list short and realistic for the
      closed beta; a full picker is out of scope.
    - `decodeOption(options, callbackData)` — the security-relevant function:
      `callback_data` arrives from the client and can be forged, so decoding is a
      lookup against the supplied option list and returns `undefined` for
      anything not found. Never `split(':')[1]` and cast.

    `src/bot/onboarding/rate-presets.ts`:
    - Module doc comment naming ONBOARD-02 and stating the three-layer defence
      explicitly: this constant is the UI layer (a value above 1 is not
      representable), `calculateTargetCalories` clamps in the domain layer, and
      the `users_desired_rate_check` constraint is the database layer.
    - `RATE_PRESETS_KG_PER_MONTH` as a `readonly` tuple of the four values.
    - `decodeRate(callbackData)` — same allowlist discipline as `decodeOption`:
      strip the `rate:` prefix, then return the matching member of
      `RATE_PRESETS_KG_PER_MONTH` or `undefined`. Do not parse an arbitrary
      float.

    In `rate-presets.test.ts`, import `MAX_RATE_KG_PER_MONTH` from
    `../../domain/nutrition/constants.js` and assert
    `Math.max(...RATE_PRESETS_KG_PER_MONTH) === MAX_RATE_KG_PER_MONTH` — this is
    the regression guard that stops the UI cap and the domain cap drifting apart.
    That test-only import of a non-barrel file is deliberate and allowed; the
    production modules still import from the barrel.

    Zero grammY imports in both production files — `InlineKeyboard` construction
    happens in Plan 05's `src/bot/keyboards/`, which reads these lists.
  </action>
  <verify>
    <automated>npx vitest run src/bot/onboarding/options.test.ts src/bot/onboarding/rate-presets.test.ts && npx tsc --noEmit && node -e "const fs=require('fs');for(const f of ['src/bot/onboarding/options.ts','src/bot/onboarding/rate-presets.ts']){const b=fs.readFileSync(f,'utf8');if(/from '(grammy|@grammyjs)/.test(b))throw new Error(f+' must not import grammY')}const r=fs.readFileSync('src/bot/onboarding/rate-presets.ts','utf8');if(!/0\.25[\s,]+0\.5[\s,]+0\.75[\s,]+1/.test(r))throw new Error('RATE_PRESETS_KG_PER_MONTH must be exactly 0.25, 0.5, 0.75, 1');const t=fs.readFileSync('src/bot/onboarding/rate-presets.test.ts','utf8');if(!t.includes('MAX_RATE_KG_PER_MONTH'))throw new Error('rate-presets.test.ts must assert against MAX_RATE_KG_PER_MONTH');console.log('options+presets OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/bot/onboarding/options.test.ts src/bot/onboarding/rate-presets.test.ts` exits 0
    - A test asserts `ACTIVITY_OPTIONS.map(o => o.value)` equals `Object.keys(ACTIVITY_MULTIPLIERS)`
    - A test asserts every `TIMEZONE_OPTIONS` value is accepted by `new Intl.DateTimeFormat('ru', { timeZone: value })` without throwing
    - A test asserts every `callbackData` is unique per list and `Buffer.byteLength(cb, 'utf8') <= 64`
    - A test asserts `decodeOption` returns `undefined` for a forged value, for a value belonging to a different list, and for `undefined`
    - A test asserts `RATE_PRESETS_KG_PER_MONTH` deep-equals `[0.25, 0.5, 0.75, 1]`
    - A test asserts `Math.max(...RATE_PRESETS_KG_PER_MONTH) === MAX_RATE_KG_PER_MONTH`
    - A test asserts `decodeRate('rate:2')` and `decodeRate('rate:1.0001')` both return `undefined`
    - Neither production file imports `grammy` or `@grammyjs/*`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Every button choice the bot will ever offer is a typed, tested constant, and every value coming back from a button is decoded through an allowlist — so a forged `callback_data` cannot inject a value the database or the domain would reject, and >1 kg/month is not offerable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Profile assembly and all Russian onboarding copy incl. the ONBOARD-06 disclaimer</name>
  <files>src/bot/onboarding/assemble-profile.ts, src/bot/onboarding/assemble-profile.test.ts, src/bot/formatting/onboarding-copy.ts, src/bot/formatting/onboarding-copy.test.ts</files>
  <read_first>
    - src/domain/nutrition/index.ts (the barrel — the only permitted import path for domain symbols) and src/domain/nutrition/types.ts (`NutritionProfile`, `NutritionTargets` field-by-field)
    - src/domain/nutrition/calculate-targets.ts (what `calculateNutritionTargets` does with the profile, and the `floorApplied` / `rateKgPerMonth` fields the confirmation screen should reflect)
    - src/config/env.ts lines 17-28 (the Russian "fact, then what to do" copy voice)
    - scripts/check-setup.ts (the same voice at length — match its register: plain, direct, no marketing tone)
    - TECH_SPEC.md §6.3 and §6.5 (the safety floor and the explicit "не медицинское изделие" framing the disclaimer must carry)
    - .planning/phases/02-bot-skeleton-onboarding/02-CONTEXT.md — "Disclaimer (ONBOARD-06)" under Claude's Discretion, including the note that the final wording needs the owner's explicit sign-off
    - .planning/phases/02-bot-skeleton-onboarding/02-PATTERNS.md — the `onboarding-copy.ts` and `assemble-profile.ts` sections
  </read_first>
  <behavior>
    - `assembleProfile` returns an object with exactly the `NutritionProfile` fields and no extras — `timezone` is carried in `OnboardingAnswers` (the bot persists it) but must NOT appear on the returned profile, because the domain type has no such field.
    - `assembleProfile` omits `desiredRateKgPerMonth` entirely when `goal` is `maintain`, and includes it when the goal is `gain` or `loss`.
    - The output of `assembleProfile` is accepted by `calculateNutritionTargets` and produces a result with a positive `targetKcal` — asserted by calling the real domain function, not a mock.
    - `DISCLAIMER_TEXT` is a non-empty Russian string containing the substrings `не медицинск` and `врач`.
    - `targetsWithDisclaimerMessage(targets)` contains the target calories, all three macro gram values, and `DISCLAIMER_TEXT`, with the disclaimer positioned after the numbers and before the end of the message (ONBOARD-06: seen before confirming).
    - `targetsWithDisclaimerMessage` adds an extra explanatory line when `targets.floorApplied` is true, telling the user in Russian that the safe calorie floor overrode the requested deficit.
    - `targetsWithDisclaimerMessage` states the rate actually used (`targets.rateKgPerMonth`) when it is greater than 0.
    - Every string in `questionCopy` is non-empty Russian; the age/height/weight questions each contain a concrete example number.
  </behavior>
  <action>
    Write both test files first.

    `src/bot/onboarding/assemble-profile.ts` — a composition-only function per
    <interfaces>. Type-only import of `NutritionProfile` from
    `../../domain/nutrition/index.js`. Destructure explicitly rather than
    spreading the whole `OnboardingAnswers`, so `timezone` cannot leak into the
    domain object; add a comment saying exactly that. Drop
    `desiredRateKgPerMonth` for `maintain`.

    `src/bot/formatting/onboarding-copy.ts` — every Russian string the bot sends,
    in one module so the owner can review copy in one place.
    - `DISCLAIMER_TEXT`: use this draft verbatim as the starting point (Plan 07
      is where the owner signs off on or edits the final wording; mark it with a
      comment saying `ONBOARD-06 — черновик, требует утверждения владельцем,
      см. план 02-07`):

      "⚠️ Важно: VoxBite — не медицинское изделие. Расчёт целевых калорий и БЖУ
      сделан по общей формуле (Миффлина-Сан Жеора) и носит справочный характер.
      Он не учитывает заболевания, приём лекарств, беременность и другие
      индивидуальные особенности. Бот не ставит диагнозов и не назначает
      лечение. Перед тем как менять питание, посоветуйся с врачом или
      дипломированным диетологом."

    - `targetsWithDisclaimerMessage(targets)`: heading, then calories, then
      protein/fat/carbs in grams, then the rate line when applicable, then the
      floor-applied line when applicable, then a blank line, then
      `DISCLAIMER_TEXT`. Do not add a "нажми кнопку" instruction inside this
      function — the buttons carry their own labels.
    - `questionCopy`: one string per onboarding step plus `greeting`, `saved`
      and `restarting`. Age/height/weight questions each name the unit and give
      an example ("Сколько тебе лет? Напиши число, например 29").
    - No emoji beyond the single ⚠️ in the disclaimer; the rest stays plain,
      matching `check-setup.ts`'s register.
    - Zero grammY imports — this module returns strings, it never touches `ctx`.

    In `onboarding-copy.test.ts`, assert on substrings and regexes, never on
    whole-message equality, so Plan 07's wording edit does not break the suite.
  </action>
  <verify>
    <automated>npx vitest run src/bot/onboarding/assemble-profile.test.ts src/bot/formatting/onboarding-copy.test.ts && npx tsc --noEmit && node -e "const fs=require('fs');for(const f of ['src/bot/onboarding/assemble-profile.ts','src/bot/formatting/onboarding-copy.ts']){const b=fs.readFileSync(f,'utf8');if(/from '(grammy|@grammyjs)/.test(b))throw new Error(f+' must not import grammY')}const c=fs.readFileSync('src/bot/formatting/onboarding-copy.ts','utf8');for(const t of ['DISCLAIMER_TEXT','targetsWithDisclaimerMessage','questionCopy']){if(!c.includes(t))throw new Error('onboarding-copy.ts missing '+t)}if(!/не медицинск/.test(c))throw new Error('disclaimer text missing the non-medical-device wording');const a=fs.readFileSync('src/bot/onboarding/assemble-profile.ts','utf8');if(/\.\.\.answers/.test(a))throw new Error('assembleProfile must destructure explicitly, not spread (timezone would leak into NutritionProfile)');console.log('copy+assemble OK')" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/bot/onboarding/assemble-profile.test.ts src/bot/formatting/onboarding-copy.test.ts` exits 0
    - A test calls the real `calculateNutritionTargets` on `assembleProfile`'s output and asserts `targetKcal > 0`
    - A test asserts the assembled profile has no `timezone` property (`Object.keys` comparison)
    - A test asserts `desiredRateKgPerMonth` is absent for `goal: 'maintain'` and present for `goal: 'loss'`
    - A test asserts `DISCLAIMER_TEXT` matches `/не медицинск/` and `/врач/`
    - A test asserts `targetsWithDisclaimerMessage(t)` contains `DISCLAIMER_TEXT` and all four numbers from `t`
    - A test asserts the extra floor-applied line appears only when `floorApplied` is true
    - Neither production file imports `grammy` or `@grammyjs/*`
    - `assemble-profile.ts` contains no `...answers` spread
    - `npm test` exits 0 and `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Answers turn into a valid `NutritionProfile` that the real Phase 1 math accepts, and every Russian string the bot will send — including the disclaimer that ONBOARD-06 requires — lives in one reviewable, tested module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram text message -> parse-fields | Free-text from a user, arbitrary length and content |
| Telegram `callback_query.data` -> options/rate decoders | Client-supplied string; a modified client can send any value, including one that was never rendered as a button |
| Parsed values -> `users` table and domain math | A value that slips through lands in a checked Postgres column and in a calorie formula |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering (ASVS V5) | `decodeOption`, `decodeRate` | mitigate | Both decode by lookup against the closed option list and return `undefined` for anything else; no `split(':')[1]` + cast anywhere. Unit tests assert forged values (`sex:other`, `rate:2`, cross-list values) return `undefined` |
| T-02-05 | Tampering (product-safety constraint ONBOARD-02) | Rate selection | mitigate | Three independent layers: `RATE_PRESETS_KG_PER_MONTH` makes >1 unrenderable, `decodeRate` refuses any non-preset value, `calculateTargetCalories` clamps, and `users_desired_rate_check` rejects at the database. A test binds the top preset to `MAX_RATE_KG_PER_MONTH` so the layers cannot drift |
| T-02-06 | Denial of Service | `parseAge`/`parseHeight`/`parseWeight` on hostile input | mitigate | Pure functions with no regex backtracking risk on unbounded input, no exceptions thrown for any string, and range checks before any use; tests include empty and non-numeric input |
| T-02-07 | Repudiation / safety | Disclaimer omitted from the confirmation screen | mitigate | `targetsWithDisclaimerMessage` composes the disclaimer into the same string as the numbers, and a unit test asserts its presence — the disclaimer cannot be dropped by editing the conversation flow alone (ONBOARD-06) |
</threat_model>

<verification>
1. `npx vitest run src/bot/onboarding src/bot/formatting` — all suites green.
2. `npm test` — whole suite green.
3. `npx tsc --noEmit` — zero errors.
4. `grep -rn "from 'grammy" src/bot/onboarding src/bot/formatting` — no matches (D-08's zero-grammY rule).
</verification>

<success_criteria>
- All onboarding logic that does not need Telegram exists as pure, tested modules under `src/bot/onboarding/` and `src/bot/formatting/`.
- A rate above 1 kg/month is neither renderable nor decodable.
- Forged `callback_data` decodes to `undefined` rather than to a value.
- The non-medical-device disclaimer is part of the confirmation message by construction.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bot-skeleton-onboarding/02-02-SUMMARY.md`.
Record: the final `DISCLAIMER_TEXT` draft verbatim (Plan 07 needs it for the
owner sign-off), the chosen plausibility bounds for age/height/weight, and the
timezone list.
</output>
