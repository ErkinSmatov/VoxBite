---
phase: 02-bot-skeleton-onboarding
plan: 07
type: execute
wave: 5
depends_on: [06]
files_modified:
  - src/bot/formatting/onboarding-copy.ts
  - src/bot/formatting/onboarding-copy.test.ts
  - .planning/phases/02-bot-skeleton-onboarding/02-MANUAL-CHECKLIST.md
autonomous: false
requirements: [ONBOARD-02, ONBOARD-05, ONBOARD-06]

must_haves:
  truths:
    - "The disclaimer wording shipped to users is the wording the owner explicitly approved"
    - "All four ROADMAP success criteria were verified by the owner in a real Telegram client"
    - "The two-terminal 409 case and the mid-conversation restart case were exercised for real, not assumed"
  artifacts:
    - path: ".planning/phases/02-bot-skeleton-onboarding/02-MANUAL-CHECKLIST.md"
      provides: "The completed, signed-off manual verification record"
      contains: "- [x]"
  key_links:
    - from: "src/bot/formatting/onboarding-copy.ts"
      to: "the owner's approval"
      via: "DISCLAIMER_TEXT set to the approved wording"
      pattern: "DISCLAIMER_TEXT"
---

<objective>
Close the two things this phase cannot finish autonomously: get the owner's
explicit approval on the non-medical-device disclaimer wording (an open item
carried in STATE.md since Phase 1), and walk the manual checklist end to end in
a real Telegram client.

Purpose: ONBOARD-06 is a legal/safety statement, not a feature — CONTEXT.md is
explicit that Claude must not silently invent the copy and call the phase done.
And with no emulation harness (D-09), the checklist walkthrough is the only
evidence the four success criteria actually hold.

Output: an approved disclaimer in the code, and a completed checklist document
with every box ticked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-bot-skeleton-onboarding/02-CONTEXT.md
@.planning/phases/02-bot-skeleton-onboarding/02-MANUAL-CHECKLIST.md
@.planning/phases/02-bot-skeleton-onboarding/02-02-SUMMARY.md
@.planning/phases/02-bot-skeleton-onboarding/02-06-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Owner approves the non-medical-device disclaimer wording (ONBOARD-06)</name>
  <files>src/bot/formatting/onboarding-copy.ts</files>
  <read_first>
    - src/bot/formatting/onboarding-copy.ts (Plan 02 — the current `DISCLAIMER_TEXT` draft and where it is composed into the targets message)
    - TECH_SPEC.md §6.3 and §6.5 (the safety floor and the project's own "не медицинское изделие" framing)
    - .planning/phases/02-bot-skeleton-onboarding/02-CONTEXT.md — "Disclaimer (ONBOARD-06)", including the instruction not to invent legal copy silently
    - .planning/STATE.md — the "Blockers/Concerns" entry recording this as an open item since Phase 1
  </read_first>
  <decision>What exact Russian wording does the bot show as its non-medical-device disclaimer, and where does it appear?</decision>
  <context>
    ONBOARD-06 requires the user to see, during onboarding and before confirming
    their targets, that the bot is not a medical device and does not replace a
    doctor or dietitian. The placement is already decided by CONTEXT.md's default
    and implemented: the disclaimer is part of the same message as the calculated
    numbers, immediately above the "Всё верно" / "Изменить" buttons, so it cannot
    be skipped but also does not block the first `/start`.

    The wording is the owner's call. This is a statement about the product's
    liability posture, and Claude has no standing to finalise it. Present the
    current draft verbatim and ask for approval or an edit.

    Current draft (from Plan 02):

    "⚠️ Важно: VoxBite — не медицинское изделие. Расчёт целевых калорий и БЖУ
    сделан по общей формуле (Миффлина-Сан Жеора) и носит справочный характер.
    Он не учитывает заболевания, приём лекарств, беременность и другие
    индивидуальные особенности. Бот не ставит диагнозов и не назначает лечение.
    Перед тем как менять питание, посоветуйся с врачом или дипломированным
    диетологом."

    Point out honestly: this is a plain-language draft written by Claude, not
    reviewed by a lawyer. If the owner intends to charge money for this bot
    later (the payment milestone is on the roadmap), a real legal review of this
    string before launch is worth the small cost — but that is a v2 concern, not
    a blocker for a closed beta with friends.
  </context>
  <options>
    <option id="approve-as-is">
      <name>Approve the draft unchanged</name>
      <pros>Nothing to do; the wording already names the three things that matter (not a medical device, not a diagnosis, see a doctor)</pros>
      <cons>Written by Claude, not by a lawyer</cons>
    </option>
    <option id="edit">
      <name>Owner supplies edited wording</name>
      <pros>The owner owns the words that carry their liability posture</pros>
      <cons>Requires the owner to write the text; the string must still name the non-medical-device fact so the existing unit test keeps passing</cons>
    </option>
    <option id="shorten">
      <name>Shorten to one or two sentences</name>
      <pros>Less wall of text on the confirmation screen, which is already dense with numbers</pros>
      <cons>Drops the specific caveats about illness, medication and pregnancy</cons>
    </option>
  </options>
  <action>
    Present the draft and the three options. Do not proceed to Task 2 until the
    owner has answered. If the owner supplies new wording, capture it verbatim,
    including punctuation and any emoji, and confirm it back before editing any
    file.
  </action>
  <verify>
    <automated>echo "Decision recorded in the SUMMARY; no code change in this task"</automated>
  </verify>
  <acceptance_criteria>
    - The owner's answer is one of: approve-as-is, edited wording (captured verbatim), or shorten (with the resulting text captured verbatim)
    - The chosen wording is recorded in the plan SUMMARY exactly as it will appear in the code
    - No file was edited in this task
  </acceptance_criteria>
  <resume-signal>Reply "как есть", or paste the wording you want instead.</resume-signal>
  <done>The disclaimer wording is the owner's decision on the record, not Claude's default.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the approved wording and re-verify</name>
  <files>src/bot/formatting/onboarding-copy.ts, src/bot/formatting/onboarding-copy.test.ts</files>
  <read_first>
    - src/bot/formatting/onboarding-copy.ts (Plan 02 — `DISCLAIMER_TEXT` and `targetsWithDisclaimerMessage`)
    - src/bot/formatting/onboarding-copy.test.ts (Plan 02 — the substring/regex assertions, deliberately written so a copy edit does not break them)
    - The Task 1 decision (the approved wording, verbatim)
  </read_first>
  <action>
    If the owner approved the draft unchanged, edit nothing and only remove the
    `черновик, требует утверждения владельцем` comment above `DISCLAIMER_TEXT`,
    replacing it with a line recording that the wording was approved by the owner
    and the date.

    If the owner supplied different wording, replace `DISCLAIMER_TEXT` with it
    exactly as given — do not "improve" the owner's phrasing, do not adjust
    punctuation, do not re-add caveats they removed. Then check the existing
    assertions in `onboarding-copy.test.ts`: they assert the text matches
    `/не медицинск/` and `/врач/`. If the approved wording no longer contains one
    of those, update that assertion to a substring the new wording does contain
    that still proves the non-medical-device meaning is present — and state in
    the SUMMARY which assertion changed and why. Never delete the assertion
    outright; ONBOARD-06 needs a test that fails if the disclaimer disappears.

    Do not change `targetsWithDisclaimerMessage`'s composition — the disclaimer
    stays inside the same message as the numbers.
  </action>
  <verify>
    <automated>npx vitest run src/bot/formatting/onboarding-copy.test.ts && npm test && npx tsc --noEmit && node -e "const fs=require('fs');const c=fs.readFileSync('src/bot/formatting/onboarding-copy.ts','utf8');if(/черновик/.test(c))throw new Error('DISCLAIMER_TEXT is still marked as an unapproved draft');if(!/утвержд/i.test(c))throw new Error('missing the comment recording owner approval');const t=fs.readFileSync('src/bot/formatting/onboarding-copy.test.ts','utf8');if(!/DISCLAIMER_TEXT/.test(t))throw new Error('the disclaimer assertion was removed');console.log('disclaimer approved and asserted')"</automated>
  </verify>
  <acceptance_criteria>
    - `DISCLAIMER_TEXT` equals the wording the owner approved, character for character
    - The `черновик, требует утверждения` comment is gone and replaced with a dated approval note
    - `onboarding-copy.test.ts` still contains an assertion that fails if the disclaimer is removed from the confirmation message
    - `npm test` and `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>The string users read is the string the owner signed off on, and a test still guards its presence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner walks the manual checklist in a real Telegram client</name>
  <files>.planning/phases/02-bot-skeleton-onboarding/02-MANUAL-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/02-bot-skeleton-onboarding/02-MANUAL-CHECKLIST.md (Plan 06 — the document being walked)
    - .planning/ROADMAP.md — Phase 2's four success criteria, which sections 1-4 of the checklist correspond to
    - .planning/phases/02-bot-skeleton-onboarding/02-CONTEXT.md — D-08 (this walkthrough is half of the phase's verification strategy) and D-09 (there is no automated substitute)
  </read_first>
  <what-built>
    Everything: the bot process with its fail-closed allowlist and 409 handling
    (Plan 04), the seven-step onboarding conversation with the rate cap and the
    disclaimer (Plans 02 and 05), the `/start` entry point (Plan 06), and the
    checklist document itself. The unit suite covers all the pure logic. What no
    test can cover is whether the buttons actually appear, whether the messages
    read sensibly in Russian, and whether the two process-level failure modes
    behave — those are exactly what this walkthrough is for.
  </what-built>
  <action>
    Hand the checklist to the owner, walk it with them section by section, record
    their answer for each section, and pause until every item is resolved. Do not
    attempt any fix inside this checkpoint — capture failures verbatim and stop.
    When every box is ticked, fill in the sign-off date and commit the document.
  </action>
  <how-to-verify>
    Ask the owner to open `.planning/phases/02-bot-skeleton-onboarding/02-MANUAL-CHECKLIST.md`
    and work through it top to bottom, ticking each box as they go. Sections 0
    through 6 are self-contained; the whole thing is roughly 15-20 minutes.

    Before they start, state the two things that matter most:
    - Section 5's mid-conversation restart (start onboarding, answer three
      questions, Ctrl+C, restart, keep answering) is the one that proves
      conversation state really lives in Postgres and not in memory. If the bot
      restarts the questionnaire instead of continuing, that is a real defect,
      not a quirk.
    - Section 5's two-terminal test must produce the Russian "уже запущен"
      message. If it prints a stack trace, or anything containing a long string
      with a colon in it that could be the token, stop and report it.

    Ask the owner to report, for each section, "ок" or what actually happened.
    For any failure, capture the exact terminal output and the exact Telegram
    message, then stop — do not attempt fixes inside this checkpoint. Failures
    become gap-closure work for `/gsd-plan-phase --gaps`.

    When every box is ticked, commit the checklist with the boxes checked and
    the sign-off date filled in.
  </how-to-verify>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/02-bot-skeleton-onboarding/02-MANUAL-CHECKLIST.md';const t=fs.readFileSync(p,'utf8');const open=(t.match(/^\s*-\s*\[ \]/gm)||[]).length;const done=(t.match(/^\s*-\s*\[x\]/gmi)||[]).length;if(open>0)throw new Error(open+' checklist items are still unchecked');if(done<12)throw new Error('only '+done+' items ticked, expected at least 12');if(!/20[0-9]{2}-[0-9]{2}-[0-9]{2}/.test(t))throw new Error('sign-off date missing');console.log('checklist signed off: '+done+' items')"</automated>
  </verify>
  <acceptance_criteria>
    - Every checkbox in `02-MANUAL-CHECKLIST.md` is ticked and a sign-off date is present
    - Success criterion 1 confirmed: `/start` walks all seven fields, invalid numeric input re-prompts in Russian, targets appear at the end
    - Success criterion 2 confirmed: the rate question offers exactly four buttons, maximum 1 кг/мес, with no free-text entry
    - Success criterion 3 confirmed: "Изменить" restarts without saving, "Всё верно" saves, and a second `/start` shows the saved targets instead of restarting
    - Success criterion 4 confirmed: the disclaimer is visible in the same message as the targets, before anything is confirmed
    - The two-terminal run produced the Russian "уже запущен" message with no stack trace and nothing resembling the token
    - The mid-conversation restart continued the conversation and produced exactly one `users` row for the tester's `telegram_id`
    - `/whoami` returned the caller's own numeric ID
  </acceptance_criteria>
  <resume-signal>Reply "все пункты ок", or list which checklist items failed and paste what you saw.</resume-signal>
  <done>All four success criteria and the three resilience scenarios were observed working in a real Telegram client by the owner, and the evidence is a committed, ticked checklist rather than an assumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Owner-supplied disclaimer text -> user-facing message | Free text substituted into a message the product's safety posture depends on |
| Manual walkthrough output -> SUMMARY and git | Terminal output pasted during verification can contain the bot token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | Repudiation / safety (ONBOARD-06) | `DISCLAIMER_TEXT` | mitigate | The wording is set only after an explicit owner decision recorded in the SUMMARY, and a unit test that fails if the disclaimer disappears from the confirmation message is preserved through the edit |
| T-02-26 | Information Disclosure | Terminal output captured during the walkthrough | mitigate | The checkpoint instructs the owner to stop and report if anything token-shaped appears, and the SUMMARY must not include raw terminal dumps from the 409 test beyond the fixed Russian string |
| T-02-27 | Tampering (false sign-off) | Checklist completion | mitigate | The automated gate fails while any box remains unchecked or the sign-off date is missing, so "verified" cannot be asserted without the document reflecting it |
</threat_model>

<verification>
1. `npm test` and `npx tsc --noEmit` — green after the wording edit.
2. `02-MANUAL-CHECKLIST.md` has zero unchecked boxes and a sign-off date.
3. The owner's answers for each of the four success criteria are recorded in the SUMMARY.
</verification>

<success_criteria>
- The disclaimer shipping to users is the owner's approved wording, still covered by a test.
- All four ROADMAP success criteria are confirmed working in a real Telegram client.
- The 409, mid-conversation-restart and no-internet scenarios were exercised for real.
- STATE.md's long-standing "disclaimer copy needs owner wording" concern is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bot-skeleton-onboarding/02-07-SUMMARY.md`.
Record: the approved disclaimer verbatim, the owner's per-section walkthrough
results, and any defect found (which becomes gap-closure input). Note explicitly
that the STATE.md disclaimer-wording blocker is now resolved. Do not paste raw
terminal output that could contain the token.
</output>
