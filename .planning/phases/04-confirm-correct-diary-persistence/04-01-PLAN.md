---
phase: 04-confirm-correct-diary-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/diary-drafts.ts
  - src/db/schema/diary.ts
  - drizzle/
autonomous: false
requirements: [CORRECT-07, CORRECT-02, DIARY-01]

must_haves:
  truths:
    - "The draft row can record what free-text input the bot is waiting for and for which component (D-04), so that state survives a bot restart."
    - "The draft row carries the calendar day the meal belongs to, frozen at message receipt (D-07)."
    - "A saved diary row points back at the draft it came from, and a confirmed draft points at its diary row (D-06)."
    - "The migration is generated by drizzle-kit, reviewed by the owner as SQL, and applied with db:migrate — never db:push."
  artifacts:
    - path: "src/db/schema/diary-drafts.ts"
      provides: "awaitingInput jsonb, localDate date, diaryId FK columns with D-04/D-06/D-07 rationale comments"
      contains: "awaiting_input"
    - path: "src/db/schema/diary.ts"
      provides: "draftId FK to diary_drafts, corrected doc comment (no diary_items table — D-06)"
      contains: "draft_id"
    - path: "drizzle/"
      provides: "One generated ALTER TABLE migration applied to the live database"
  key_links:
    - from: "src/db/schema/diary.ts"
      to: "src/db/schema/diary-drafts.ts"
      via: "draftId references diaryDrafts.id"
      pattern: "references\\(\\(\\) => diaryDrafts.id\\)"
    - from: "src/db/schema/diary-drafts.ts"
      to: "src/db/schema/diary.ts"
      via: "diaryId nullable back-reference set on confirm"
      pattern: "diary_id"
---

<objective>
Evolve `diary_drafts` and `diary` so Phase 4's correction, confirmation and diary-write
machinery has somewhere to put its state, then generate and apply the migration.

Purpose: every later plan in this phase reads or writes these columns. Nothing else can
be verified against a live database until this lands.
Output: two modified schema files plus one applied Drizzle migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confirm-correct-diary-persistence/04-CONTEXT.md
@.planning/phases/04-confirm-correct-diary-persistence/04-PATTERNS.md
@src/db/schema/diary-drafts.ts
@src/db/schema/diary.ts
@src/db/schema/users.ts

<interfaces>
Current shapes the executor must extend (not replace):

diary_drafts columns today: id, userId (FK users, cascade), updateId (bigint, unique),
chatId, messageId, source ('voice'|'text'), transcript, components (jsonb),
status ('draft'|'confirmed'|'abandoned', default 'draft'), createdAt, updatedAt.
Table constraints today: diary_drafts_status_check, diary_drafts_source_check,
diary_drafts_update_id_key, diary_drafts_user_idx on (user_id, created_at).

diary columns today: id, userId (FK users, cascade), eatenAt, localDate (date, notNull),
description (text, notNull), kcal/proteinG/fatG/carbsG/sugarG (real, nullable), createdAt,
index diary_user_date_idx on (user_id, local_date).

Exported types to keep exporting: DiaryDraftRow, NewDiaryDraft, DiaryRow, NewDiaryRow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase 4 columns to diary_drafts and diary</name>
  <read_first>
    - src/db/schema/diary-drafts.ts (current column list, check-constraint style, doc-comment discipline)
    - src/db/schema/diary.ts (current columns, the stale doc comment that must be rewritten)
    - src/db/schema/users.ts (the `check(...)` mirroring a `$type<>()` union convention on sex/activityLevel/goal)
    - .planning/phases/04-confirm-correct-diary-persistence/04-CONTEXT.md (D-04, D-06, D-07, D-11)
  </read_first>
  <action>
Add to `diaryDrafts` in src/db/schema/diary-drafts.ts:

- `awaitingInput: jsonb('awaiting_input').$type<DraftAwaitingInput | null>()` — nullable.
  Declare the payload type in this file as an exported interface
  `DraftAwaitingInput { kind: 'add_component' | 'typed_grams'; componentIndex?: number }`
  and export it, so plan 04 can re-export it from `src/application/types.ts` without a
  circular import. Doc comment must name D-04 and state why the flag is a column and not
  process memory (ARCHITECTURE.md Anti-Pattern 4).
- `localDate: date('local_date')` — NULLABLE, not notNull. Doc comment must name D-07,
  state it is frozen from the incoming message's timestamp converted into
  `users.timezone` and never recomputed, and state the deliberate tradeoff below.
  TRADEOFF TO WRITE DOWN IN THE COMMENT: the column is nullable only because rows created
  by Phase 3 before this migration have no day to backfill honestly. A NOT NULL column
  would force inventing a date for them. Phase 4's write path always sets it; a draft with
  `local_date IS NULL` is a pre-Phase-4 leftover and plan 08's `confirm()` refuses it with
  the same copy a stale draft gets. Do not invent a backfill value.
- `diaryId: integer('diary_id').references(() => diary.id)` — nullable, set only on
  confirm. Doc comment names D-06 and states that a confirmed draft is a durable record,
  not garbage — no cleanup may ever delete a `status = 'confirmed'` row.

Add to `diary` in src/db/schema/diary.ts:

- `draftId: integer('draft_id').notNull().references(() => diaryDrafts.id)`. Doc comment
  names D-06: the draft is the working copy of the meal composition, this row is the
  snapshot of its totals, and this FK is how `✎ Поправить` on a saved entry finds its way
  back into the same correction machinery.
- Rewrite the file's top doc comment: it currently claims "Phase 4 extends this table with
  per-component rows". That plan changed — D-06 rejected a normalised `diary_items` table.
  The new comment states Phase 4 adds `draft_id` only and keeps totals denormalised on this
  row, and that `sugarG` staying nullable is CALC-02/TECH_SPEC §5.8 (never 0, never guessed).

Import direction: `diary-drafts.ts` will import `diary` and `diary.ts` will import
`diaryDrafts` — Drizzle tolerates this cycle because both references are lazy arrow
functions, but if TypeScript complains about a circular type, break it by using
`references(() => diary.id)` only in `diary-drafts.ts` and declaring the reverse side as a
plain `integer('draft_id').notNull()` plus an explicit `foreignKey(...)` in the table's
extras array. Do not drop either link.

Do NOT touch the existing status check constraint values — 'draft' | 'confirmed' |
'abandoned' already covers every state this phase needs (D-06, D-08, D-11, D-12).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -v '^\s*[*/]' src/db/schema/diary-drafts.ts | grep -q "awaiting_input" && grep -v '^\s*[*/]' src/db/schema/diary-drafts.ts | grep -q "local_date" && grep -v '^\s*[*/]' src/db/schema/diary-drafts.ts | grep -q "diary_id" && grep -v '^\s*[*/]' src/db/schema/diary.ts | grep -q "draft_id"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `src/db/schema/diary-drafts.ts` exports `DraftAwaitingInput` with exactly the union `'add_component' | 'typed_grams'`.
    - `local_date` on `diary_drafts` is nullable (no `.notNull()` on that column).
    - `diary.draft_id` is `.notNull()` and references `diaryDrafts.id`.
    - `src/db/schema/diary.ts`'s top doc comment no longer contains the string "per-component rows".
    - `DiaryDraftRow`, `NewDiaryDraft`, `DiaryRow`, `NewDiaryRow` are still exported.
  </acceptance_criteria>
  <done>Both schema files carry the new columns with decision-referencing doc comments and the project typechecks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Generate, review and apply the migration</name>
  <read_first>
    - drizzle/0006_voice_pipeline_rls.sql (the RLS precedent — read it so you can confirm no new RLS statement is needed here)
    - drizzle/0005_unique_pyro.sql (what a generated column-change migration looks like in this repo)
    - package.json (the db:generate / db:migrate / verify-schema script names)
  </read_first>
  <what-built>
Claude runs `npm run db:generate`, which writes a new numbered `.sql` file into `drizzle/`
containing only `ALTER TABLE ... ADD COLUMN` statements for the four columns added in
Task 1. No new table is created, so no new `ENABLE ROW LEVEL SECURITY` statement is needed
— `diary` and `diary_drafts` already have RLS enabled with zero policies from
`drizzle/0006_voice_pipeline_rls.sql`. Claude prints the generated SQL in full.

`drizzle-kit generate` is offline: it only compares the TypeScript schema against the
previous migrations and writes a file. It does not touch your database.

Claude does NOT run `npm run db:migrate` — you do, after reading the SQL.
  </what-built>
  <how-to-verify>
1. Read the SQL Claude printed. You are looking for exactly four `ADD COLUMN` statements
   (`diary_drafts.awaiting_input`, `diary_drafts.local_date`, `diary_drafts.diary_id`,
   `diary.draft_id`) plus the two foreign-key constraints. If you see a `DROP`, a
   `TRUNCATE`, or any table being recreated, stop and say so — that is not what this
   migration should do.
2. If `diary.draft_id` is added as `NOT NULL` and the `diary` table already contains rows,
   Postgres will refuse the migration. `diary` should be empty (Phase 4 is its first ever
   write). If the migration fails with `column "draft_id" contains null values`, STOP and
   report it — do not delete rows to make it pass.
3. In your terminal, at the repo root (`/Users/smatov/GitLab/VoxBite`), run:
   `npm run db:migrate`
   Expected output: drizzle-kit lists the new migration file and prints that migrations
   completed. It takes a couple of seconds.
4. Then run:
   `npm run verify-schema`
   Expected: it exits 0 and prints no missing-column errors.
5. If step 3 fails with a connection error, your `DATABASE_URL` in `.env` is wrong or the
   Supabase project is paused — open the Supabase dashboard, confirm the project is active,
   and re-copy the connection string.
  </how-to-verify>
  <resume-signal>Type "approved" once `npm run db:migrate` and `npm run verify-schema` both succeeded, or paste the error output.</resume-signal>
  <verify>
    <automated>npm run verify-schema</automated>
  </verify>
  <acceptance_criteria>
    - A new `drizzle/00NN_*.sql` file exists containing `ADD COLUMN "awaiting_input"`, `ADD COLUMN "local_date"`, `ADD COLUMN "diary_id"`, `ADD COLUMN "draft_id"`.
    - The generated SQL contains no `DROP TABLE`, no `DROP COLUMN`, and no `TRUNCATE`.
    - No occurrence of `drizzle-kit push` anywhere in the repo (`grep -rn "db:push\|drizzle-kit push" package.json` returns nothing).
    - `npm run verify-schema` exits 0 after the owner ran `npm run db:migrate`.
  </acceptance_criteria>
  <done>The migration file is committed and applied to the live database; verify-schema passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase PostgREST → `public` tables | Every `public` table is published through the project's anon key; `diary`/`diary_drafts` hold health data |
| Developer machine → production database | `db:migrate` runs destructive-capable DDL against the owner's real data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | `diary_drafts`, `diary` via PostgREST | mitigate | No new table is introduced, so both tables keep the `ENABLE ROW LEVEL SECURITY` with zero policies applied in `drizzle/0006_voice_pipeline_rls.sql`; Task 2's review explicitly confirms the generated SQL is column-only |
| T-04-11 | Tampering | Migration workflow | mitigate | `drizzle-kit push` stays banned (Phase 1 lock); the generated SQL is reviewed by the owner before `db:migrate`, and Task 2 names the exact destructive statements to reject |
| T-04-12 | Denial of Service (data loss) | `diary.draft_id NOT NULL` on a non-empty table | accept | `diary` has never been written to; if the migration fails on existing rows the checkpoint instructs the owner to stop and report rather than delete data |
</threat_model>

<verification>
- `npx tsc --noEmit` passes.
- `npm test` still passes (no runtime code changed).
- `npm run verify-schema` passes against the live database.
</verification>

<success_criteria>
`diary_drafts` has `awaiting_input`, `local_date`, `diary_id`; `diary` has `draft_id`;
one reviewed migration is applied; no `push`, no RLS regression.
</success_criteria>

<output>
After completion, create `.planning/phases/04-confirm-correct-diary-persistence/04-01-SUMMARY.md`
</output>
