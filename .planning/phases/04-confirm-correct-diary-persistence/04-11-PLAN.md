---
phase: 04-confirm-correct-diary-persistence
plan: 11
type: execute
wave: 6
depends_on: [10]
files_modified:
  - docs/phase-04-manual-checklist.md
autonomous: false
requirements: [CORRECT-01, CORRECT-02, CORRECT-03, CORRECT-04, CORRECT-05, CORRECT-06, CORRECT-07, CORRECT-08, CALC-02, DIARY-01]

must_haves:
  truths:
    - "On a real phone, the two-level card is readable and every button does what its label says (CORRECT-01..06, D-01, D-02)."
    - "Killing and restarting the bot process mid-correction leaves the card fully functional with identical state (CORRECT-07, D-04)."
    - "A component whose FDC record has no sugar shows 'нет данных' on both the preview and the saved entry — never 0 (CALC-02, D-09)."
    - "A confirmed entry can be reopened, corrected, and deleted with a Да/Нет confirmation (CORRECT-08, D-05, D-08)."
    - "The saved entry names the day the message was dictated, not the day it was confirmed (DIARY-01, D-07)."
  artifacts:
    - path: "docs/phase-04-manual-checklist.md"
      provides: "the owner-executable walkthrough, with the exact commands and SQL to confirm each step"
  key_links:
    - from: "docs/phase-04-manual-checklist.md"
      to: "the live bot"
      via: "npm run bot plus a real Telegram client"
      pattern: "npm run bot"
---

<objective>
Verify on a real device what no unit test can: that the card is usable, that draft state truly
survives a restart, that a missing nutrient reads honestly end to end, and that the saved day is
the day the meal was dictated.

Purpose: 04-VALIDATION.md lists exactly three manual-only verifications, and this phase is the
first point where end-to-end nutrition accuracy is observable at all. Phase 2 set the precedent
of an owner sign-off checkpoint backed by a written checklist.
Output: `docs/phase-04-manual-checklist.md` plus the owner's walkthrough result.

This plan writes NO production code. If the walkthrough finds a defect, it is reported and fixed
by a follow-up gap-closure plan, not patched inside this checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-confirm-correct-diary-persistence/04-CONTEXT.md
@.planning/phases/04-confirm-correct-diary-persistence/04-VALIDATION.md
@package.json

<interfaces>
Owner-facing commands that already exist (package.json): `npm run bot` (tsx src/bot/index.ts,
long-polling), `npm test`, `npm run typecheck`, `npm run db:studio` (Drizzle Studio — a browser
UI for looking at table rows without writing SQL), `npm run verify-schema`.

The owner is a frontend/React developer with no backend experience (CLAUDE.md). Every step in
the checklist must name the exact command, the directory to run it in
(`/Users/smatov/GitLab/VoxBite`), what correct output looks like, and what to do when it does
not appear. Do not write "verify the draft persisted" — write which table to open in Drizzle
Studio, which column to look at, and what value it should hold.

04-VALIDATION.md's three manual-only rows: the two-level card's look-and-feel on a real device;
the restart-mid-correction survival check; and the `нет данных` end-to-end case.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the owner-executable manual checklist</name>
  <read_first>
    - .planning/phases/04-confirm-correct-diary-persistence/04-VALIDATION.md (the three manual-only verifications and their stated instructions)
    - .planning/phases/04-confirm-correct-diary-persistence/04-CONTEXT.md (D-01, D-02, D-05, D-07, D-08, D-09, D-10, D-11, D-12 — each gets a checklist line)
    - docs/ (the Phase 2 manual checklist document, if present — copy its structure, numbering and tone rather than inventing a second format)
    - package.json (the exact script names to cite)
    - src/bot/formatting/correction-copy.ts (the exact Russian strings the owner should expect to see, so the checklist quotes them verbatim)
  </read_first>
  <action>
Create `docs/phase-04-manual-checklist.md`. Structure it as numbered scenarios, each with:
what to do, what you should see (quoting the exact Russian string from `correctionCopy`), and
what it means if you see something else.

Preflight section (exact commands, run in `/Users/smatov/GitLab/VoxBite`):
- `npm test` — expect all suites green.
- `npm run typecheck` — expect no output and exit 0.
- `npm run verify-schema` — expect no missing-column errors.
- `npm run bot` — expect the startup line the bot already prints; leave this terminal open, the
  bot only runs while it does. Explain that `Ctrl-C` in that terminal stops the bot (this is
  used deliberately in scenario 6).

Scenarios (each one maps to a requirement/decision — cite the id on the line):
1. CORRECT-01 / D-01 / D-02: send a voice message describing a multi-ingredient dish. Confirm
   the card lists every component with grams and an FDC description, and that the FDC
   description is fully readable (not cut off with "…"). Tap a component: the SAME message
   changes into the single-component screen — no new message appears in the chat. Check the
   three candidate descriptions are readable as text with 1/2/3 buttons underneath.
2. CORRECT-03 / D-03: tap candidate 2. The mark moves, and the `≈` preview line changes. Note
   the preview numbers.
3. CORRECT-04: tap `+10 г` twice and `−10 г` once; grams and the preview move each time. Tap
   `⌨ Ввести граммы`, then type `200 г` as a normal chat message — the grams become 200 and no
   new "analysis" starts. Then repeat with the nonsense value `abc` and confirm you get
   `correctionCopy.gramsRejected` and can immediately type a valid number without re-tapping the
   button.
4. CORRECT-05 / D-12: remove components one at a time until none are left. Confirm the empty
   state message appears, `✅ Подтвердить` is GONE, and `➕ Добавить` remains.
5. CORRECT-06: from the empty state, tap `➕ Добавить` and type a simple ingredient with grams
   (e.g. `сметана 30`). It appears as a component with candidates. Confirm your OpenAI usage did
   not jump — this should cost one embedding, not a transcription plus a decomposition.
6. CORRECT-07 / D-04 (the restart check): start a correction, tap `⌨ Ввести граммы` but do NOT
   type yet. Go to the terminal running the bot and press `Ctrl-C`. Run `npm run bot` again.
   Now type the number. It must be applied to the same component in the same card. Then open
   `npm run db:studio`, find the `diary_drafts` table, locate your row, and confirm the
   `awaiting_input` column held a value while the bot was down.
7. CALC-02 / D-09 (the honest-number check): pick a component whose USDA record has no sugar
   (Drizzle Studio: `fdc_foods`, `sugar_g` column = null). Confirm both the preview line and,
   after saving, the saved card show `нет данных` for sugar — never `0`. If some components have
   sugar and others do not, the line must read as a lower bound naming how many are missing.
8. D-10: swap a component into a state with no match (or use a dish where one component found
   nothing) and tap `✅ Подтвердить`. Confirm it refuses, names the component, and offers to
   remove it or describe it differently.
9. CORRECT-02 / DIARY-01 / D-05 / D-07: confirm a good draft. The same message becomes the saved
   entry with final numbers, no `≈`, and the day it was saved to. Open `db:studio` → `diary` and
   confirm exactly ONE row exists for this meal, with `local_date` equal to the day you dictated
   it. Then tap `✅ Подтвердить` again quickly on an older card if one is available — no second
   row may appear.
10. CORRECT-08: tap `✎ Поправить` on the saved entry, change grams, and confirm the `diary`
    row's numbers changed in Drizzle Studio while `local_date` stayed the same.
11. D-08: tap `🗑 Удалить`. Confirm you are asked `Да / Нет` first. Tap `Нет` — nothing is
    deleted. Tap `🗑 Удалить` again and then `Да` — the `diary` row is gone from Drizzle Studio
    (a real delete, not a hidden flag) and the linked `diary_drafts` row's status reads
    `abandoned`.
12. D-11: if you have a card older than 24 hours in your chat history, tap any of its buttons.
    Confirm you get the "устарел" message and the buttons disappear. If you do not have one, note
    this line as untested rather than skipping it silently.

Close the document with a short "what to do if a step fails" section: stop, copy the exact
message you saw and the step number, and report it — do not attempt to fix data by hand in
Drizzle Studio, because a hand-edited row will make the next step lie.
  </action>
  <verify>
    <automated>test -f docs/phase-04-manual-checklist.md && grep -c "npm run" docs/phase-04-manual-checklist.md && grep -c "CORRECT-0" docs/phase-04-manual-checklist.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase-04-manual-checklist.md` exists and contains 12 numbered scenarios plus a preflight section.
    - `grep -c "npm run" docs/phase-04-manual-checklist.md` returns at least 5 (each command is named literally).
    - Each of CORRECT-01..08, CALC-02, DIARY-01 appears at least once in the document.
    - The restart scenario names `Ctrl-C`, `npm run bot`, `npm run db:studio`, the `diary_drafts` table and the `awaiting_input` column explicitly.
    - The document quotes at least four Russian strings verbatim from `correction-copy.ts`.
    - No step says "verify it works" without naming the observable value or message.
  </acceptance_criteria>
  <done>The owner has a checklist they can execute without knowing the codebase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner walkthrough and sign-off</name>
  <read_first>
    - docs/phase-04-manual-checklist.md (as written in Task 1)
    - .planning/phases/04-confirm-correct-diary-persistence/04-VALIDATION.md (the manual-only table this checkpoint discharges)
  </read_first>
  <what-built>
Claude has implemented the full confirm/correct/diary flow (plans 01-10) and written
`docs/phase-04-manual-checklist.md`. Everything automatable is already covered by the test suite:
`npm test` is green and `npx tsc --noEmit` is clean.

What remains cannot be automated: how the two-level card actually reads on a phone, whether
draft state genuinely survives killing the process, and whether a missing-sugar record reads
`нет данных` all the way through to the saved entry against real USDA data.

This walkthrough spends real money — each voice message costs a transcription plus a
decomposition plus embeddings, on the order of a cent or two per message. Adding a component
costs one embedding (a fraction of a cent). Twelve scenarios will not be noticeable against your
OpenAI spend cap, but you should know before you start rather than after.
  </what-built>
  <how-to-verify>
1. Open `docs/phase-04-manual-checklist.md`.
2. Run the preflight section first. If `npm test` or `npm run typecheck` fails, stop here and
   report it — do not continue to the live steps.
3. Work through scenarios 1-12 in order, on your phone, with `npm run bot` running in a terminal.
4. For each scenario, note: passed / failed / not testable. Scenario 12 (a card older than 24
   hours) may legitimately be "not testable" today.
5. When something looks wrong, copy the exact Russian message you saw and the scenario number.
   Do not edit rows by hand in Drizzle Studio to "fix" a step.
  </how-to-verify>
  <resume-signal>Type "approved" if every testable scenario passed, or list the scenario numbers that failed with what you saw.</resume-signal>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and `npx tsc --noEmit` exits 0 before the owner starts the live steps.
    - The owner records a pass/fail/not-testable result for each of the 12 scenarios in the plan summary.
    - Scenario 6 (restart mid-correction) and scenario 7 (`нет данных` end to end) are explicitly recorded, since 04-VALIDATION.md lists both as required manual verifications.
    - Any failure is captured with the exact observed message and scenario number, not a paraphrase.
  </acceptance_criteria>
  <done>The owner has exercised the correction flow on a real device and signed off, or the specific failures are recorded for a gap-closure plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live bot process → real OpenAI spend | The walkthrough triggers real paid calls |
| Live bot process → real health data in Postgres | The walkthrough writes and deletes real diary rows |
| Manual database inspection → data integrity | Hand-editing rows during verification invalidates later steps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-37 | Denial of Service (cost) | the walkthrough itself | mitigate | The checkpoint states the per-message cost up front (CLAUDE.md's "explain expensive actions before, not after"); scenario 5 explicitly asks the owner to confirm an added component did not trigger a transcription |
| T-04-38 | Tampering (data integrity) | hand-editing rows in Drizzle Studio | mitigate | The checklist's closing section forbids hand-fixing data mid-walkthrough and explains why a hand-edited row makes the following steps lie |
| T-04-05 | Repudiation / data loss | scenario 11's real delete | accept | The delete is performed on a test entry the owner created minutes earlier in the same session; D-08 hard delete is the behaviour under test |
| T-04-39 | Information Disclosure | screenshots/logs shared while reporting a failure | accept | The owner is the only user in the closed beta; the data is their own meals. Noted so it is a conscious acceptance rather than an oversight |
</threat_model>

<verification>
- `npm test` passes.
- `npx tsc --noEmit` passes.
- `docs/phase-04-manual-checklist.md` exists with all 12 scenarios.
- The owner's per-scenario results are recorded in the plan summary.
</verification>

<success_criteria>
The three manual-only verifications in 04-VALIDATION.md are executed on a real device with
recorded results, and either the phase is signed off or the specific failures are captured
precisely enough to plan gap closure without re-running the whole walkthrough.
</success_criteria>

<output>
After completion, create `.planning/phases/04-confirm-correct-diary-persistence/04-11-SUMMARY.md`
</output>
