---
phase: 03-voice-pipeline
plan: 08
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - src/config/env.ts
  - src/config/env.test.ts
  - .env.example
  - src/bot/pipeline-wiring.ts
  - src/bot/pipeline-wiring.test.ts
  - src/bot/bot.ts
  - src/bot/bot.wiring.test.ts
  - src/bot/startup-sweep.ts
  - src/bot/startup-sweep.test.ts
  - src/bot/index.ts
autonomous: false
requirements: [VOICE-01, VOICE-02, VOICE-04]

must_haves:
  truths:
    - "D-09: the bot still runs as long polling from the owner's own terminal via npm run bot — no webhook, no setWebhook call, no HTTP server, and src/bot/index.ts remains the only file that calls bot.start()"
    - "The voice, text and unsupported-content handlers are registered in bot.ts AFTER the allowlist middleware, and bot.wiring.test.ts asserts by source index that EVERY bot.on/bot.command/bot.callbackQuery registration appears after createAllowlistMiddleware — this closes T-03-44 and is the proof that a non-allowlisted user can never reach a paid call"
    - "D-11: on startup the bot finds every processed_updates row still in status processing, sends that chat the Russian pipelineCopy.interruptedByRestart notice, and moves the row to the terminal interrupted status — with NO auto-resume path (startup-sweep.ts never imports processMeal or downloadVoice, because the audio is gone by design, D-05)"
    - "D-11 (availability): a failing sweep — database unreachable, a send rejected by Telegram — logs one short Russian line and lets the bot start anyway; runStartupSweep never rejects and never aborts npm run bot"
    - "D-13: end to end in real Telegram, a voice message produces exactly ONE bot message ('Секунду, разбираю 🎧') which is then edited in place into the result card — no second message is sent"
    - "D-15: the daily message cap stays a code constant that no environment variable can raise, lower or disable — the only new env key in this phase is an STT model override restricted to the two model names already declared in src/adapters/stt/types.ts"
    - "D-17: sending one real meal message prints exactly one cost line in the terminal running npm run bot, and that line contains no transcript text"
    - "The owner's existing .env keeps working untouched: the new key is OPTIONAL, so npm run bot and npm run check-setup still succeed without editing .env"
  artifacts:
    - path: "src/bot/pipeline-wiring.ts"
      provides: "buildMealHandlerDeps() — constructs the real Transcriber/DishDecomposer/Embedder/FdcRepository/MessageEditor once, plus resolveSttModel()"
      contains: "buildMealHandlerDeps"
    - path: "src/bot/startup-sweep.ts"
      provides: "runStartupSweep() — the D-11 interrupted-run notice, with no resume path"
      contains: "findInterruptedUpdates"
    - path: "src/bot/bot.wiring.test.ts"
      provides: "the source-order assertion that no handler is registered ahead of the allowlist gate"
      contains: "createAllowlistMiddleware"
  key_links:
    - from: "src/bot/bot.ts"
      to: "src/bot/handlers/meal.ts"
      via: "bot.on('message:voice' / 'message:text' / unsupported types) registered after the allowlist"
      pattern: "createVoiceHandler"
    - from: "src/bot/bot.ts"
      to: "src/bot/pipeline-wiring.ts"
      via: "buildMealHandlerDeps(deps) called once in the composition root"
      pattern: "buildMealHandlerDeps"
    - from: "src/bot/index.ts"
      to: "src/bot/startup-sweep.ts"
      via: "awaited runStartupSweep before bot.start()"
      pattern: "runStartupSweep"
---

<objective>
Wire the phase together. Plans 01-07 built every part behind a port; none of it
is connected to a running bot yet. This plan is the composition root: it
constructs the real adapters once, registers the voice/text/unsupported handlers
into `bot.ts` behind the allowlist gate, adds the single environment override
the phase needs, performs the D-11 interrupted-run sweep at startup, and ends
with the owner sending a real voice message to a real bot.

Purpose: this is the first time the chain STT → decomposition → FDC matching
runs against real Telegram, and the first time the registration ordering that
protects the owner's OpenAI balance is enforced by an automated test rather
than by a comment.

Output: handlers registered and order-asserted, one optional env key, a startup
sweep that resolves crashed runs, and a manual end-to-end smoke check of roadmap
success criteria 1, 3, 4 and 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-voice-pipeline/03-CONTEXT.md
@.planning/phases/03-voice-pipeline/03-PATTERNS.md
@.planning/phases/03-voice-pipeline/03-VALIDATION.md
@CLAUDE.md

<interfaces>
Contracts this plan CONSUMES (all built by plans 01-07 — do not redefine any of
them, do not modify the files that own them):

src/adapters/stt/types.ts (03-01)
  STT_MODEL, STT_COMPARISON_MODEL, MAX_VOICE_SECONDS, Transcriber
src/adapters/stt/openai-transcribe.ts (03-01)
  createOpenAITranscriber(opts?: { apiKey?; client?; model?; maxRetries? }): Transcriber
src/adapters/llm/types.ts (03-04)
  DECOMPOSITION_MODEL, DishDecomposer
src/adapters/llm/openai-decompose.ts (03-04)
  createOpenAIDecomposer(opts?): DishDecomposer
src/adapters/embeddings/openai-embed.ts (Phase 1)
  createOpenAIEmbedder(opts?): Embedder
src/adapters/fdc-repository.ts (Phase 1)
  createDrizzleFdcRepository(db): FdcRepository
src/application/types.ts (03-03)
  MessageEditor, DAILY_MESSAGE_CAP
src/application/idempotency.ts (03-05)
  findInterruptedUpdates(db): Promise<{ updateId; chatId; telegramId }[]>
  markInterrupted(db, updateIds: number[]): Promise<void>
src/bot/formatting/pipeline-copy.ts (03-03)
  pipelineCopy.interruptedByRestart
src/bot/telegram/message-editor.ts (03-07)
  createMessageEditor(api): MessageEditor
src/bot/handlers/meal.ts (03-07)
  MealHandlerDeps { db; token; deps: PipelineDeps }
  createVoiceHandler(d), createTextHandler(d), createUnsupportedHandler()
src/application/voice-pipeline.ts (03-06)
  PipelineDeps { db; transcriber; decomposer; embedder; repo; editor }

Contracts this plan CREATES:

src/bot/pipeline-wiring.ts (new)
  export function resolveSttModel(rawOverride: string): string
  export interface MealWiringDeps { db: Db; token: string; api: EditorApi; sttModel: string }
  export function buildMealHandlerDeps(w: MealWiringDeps): MealHandlerDeps

src/bot/startup-sweep.ts (new)
  export interface StartupSweepDeps { db: Db; notify(chatId: number, text: string): Promise<unknown> }
  export interface SweepReport { found: number; notified: number; notifyFailed: number }
  export function runStartupSweep(deps: StartupSweepDeps): Promise<SweepReport>

src/config/env.ts (modified)
  OPTIONAL_ENV_KEYS gains 'STT_MODEL'; AppEnv gains STT_MODEL: string

src/bot/bot.ts (modified)
  BotDeps gains sttModel: string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: One optional STT model override in env.ts and .env.example</name>
  <files>src/config/env.ts, .env.example, src/config/env.test.ts</files>
  <read_first>
    - src/config/env.ts (the file being modified — `REQUIRED_ENV_KEYS`, `OPTIONAL_ENV_KEYS`, `AppEnv`, and the reason `BETA_ALLOWLIST` is optional-but-declared)
    - src/config/env.test.ts (the file being modified — in particular the case named "REQUIRED_ENV_KEYS and OPTIONAL_ENV_KEYS together match exactly the keys declared in .env.example", which reads `.env.example` with the regex `/^([A-Z_]+)=/gm` and hard-codes both arrays)
    - .env.example (the file being modified — the four existing blocks, each a "где взять"/"что будет если пусто" comment followed by one `KEY=` line)
    - .planning/phases/03-voice-pipeline/03-PATTERNS.md section "`src/config/env.ts` + `.env.example` (config, modified)" — the rule: new keys go in `OPTIONAL_ENV_KEYS`, never `REQUIRED_ENV_KEYS`, and must be mirrored into `.env.example`
    - src/adapters/stt/types.ts (plan 03-01 — `STT_MODEL` and `STT_COMPARISON_MODEL`, the only two legal values of the new key, and the D-03 note in its doc comment)
    - .planning/phases/03-voice-pipeline/03-01-PLAN.md Task 2 acceptance criteria (the single-constant grep gate: `grep -rn "gpt-4o-mini-transcribe" src/` must keep matching only `src/adapters/stt/types.ts`, so neither `env.ts` nor any file in this plan may contain that literal)
  </read_first>
  <action>
Add exactly ONE new environment key in this phase: `STT_MODEL`, declared in
`OPTIONAL_ENV_KEYS` (never `REQUIRED_ENV_KEYS`) and mirrored into `.env.example`
as a documented block ending in the line `STT_MODEL=` with an empty value.

Why optional and not required — the executor must not "tighten" this later: a
key in `REQUIRED_ENV_KEYS` makes `loadEnv()` throw and the bot refuse to start
until the owner edits `.env`. The owner already has a working `.env` from Phase
2, so an empty `STT_MODEL` must mean "use the code default" and `npm run bot`
and `npm run check-setup` must keep working with that file untouched. This is
exactly how `BETA_ALLOWLIST` already behaves: declared so the owner sees it
exists, legal when empty, excluded from the `missingKeys` check.

Why this key exists at all: D-03 records a disagreement the owner deliberately
overrode — `gpt-4o-mini-transcribe` is the default, and its known weakness is
rare words, which is precisely the Kazakh dish names D-01 identifies as the
phase's accuracy risk. D-03's resolution is empirical: `npm run verify-stt`
(plan 03-01) prints both models' transcripts for the same audio. This key is the
flip switch that follows that verdict — one line in `.env` plus a restart, no
TypeScript edit.

Why the other three constants named in 03-CONTEXT.md deliberately do NOT become
env keys. Record this reasoning inside the `.env.example` comment block so the
owner can find it without reading planning docs:
- `DAILY_MESSAGE_CAP` (D-15) and `MAX_VOICE_SECONDS` (D-14) are the phase's two
  spend guards. Their enforcement points (`src/application/limits.ts`,
  `src/bot/telegram/download-voice.ts`) import them directly as constants and
  accept no injection, so exposing them would mean editing files this plan does
  not own — and, more importantly, a spend guard whose value is read from a text
  file at startup can be silently weakened by a typo (`300` instead of `30`).
  They stay one-line constant edits.
- `DECOMPOSITION_MODEL` (D-16) has no second known-good value to switch to, so
  an override key would be dead configuration that lies to the owner about what
  is tunable. It stays a one-line constant edit as well.
D-14, D-15 and D-16 each say "constant or env var"; constant is a legal reading
of all three, and it is the safer reading for the two that bound spend.

Changes to `src/config/env.ts`:
- Append `'STT_MODEL'` to `OPTIONAL_ENV_KEYS`.
- Add `STT_MODEL: string;` to `AppEnv` with an inline comment stating that `''`
  is legal and means "use `STT_MODEL` from `src/adapters/stt/types.ts`".
- Populate it in the `cachedEnv` object as `process.env.STT_MODEL ?? ''`,
  exactly like `BETA_ALLOWLIST`.
- Do NOT import anything from `src/adapters/` into `env.ts`, and do NOT write
  any model-name literal in this file. `env.ts` carries the raw string only;
  validation and fallback live in `pipeline-wiring.ts` (Task 2). This keeps plan
  03-01's single-constant grep gate intact.

Changes to `.env.example`: add one block in the existing house style after the
`BETA_ALLOWLIST` block, written for someone who has never edited an env file.
In Russian it must say: that leaving the line empty is normal and is what you
want until `npm run verify-stt` says otherwise; that only two values are
accepted, written out literally (`.env.example` is not under `src/`, so the grep
gate does not apply to it); that any other value is ignored with a warning line
in the terminal and the cheap default is used instead; and that the more
expensive of the two costs roughly twice as much per second of audio. End the
block with the literal line `STT_MODEL=`.

Literal owner instructions to include in that same comment block and to repeat
in the SUMMARY, since the owner has never done this:
1. Open the file `.env` in the project root (not `.env.example` — `.env` is the
   real one; if you have never created it, run `cp .env.example .env` first).
2. To keep the current behaviour, change nothing. The bot will use
   `gpt-4o-mini-transcribe`.
3. To switch models, find the line `STT_MODEL=` and write the model name right
   after the `=`, with no spaces and no quotes.
4. Save the file, stop the bot with Ctrl+C in the terminal where it runs, and
   start it again with `npm run bot`. Env files are read once at startup, so a
   change does nothing until you restart.
5. To confirm nothing is broken, run `npm run check-setup` from the project
   root. When it is right the last line printed is `SETUP OK` and the command
   exits without an error. If it prints a `[FAIL]` line, that line names what to
   fix.

Changes to `src/config/env.test.ts`: update the hard-coded
`expect(OPTIONAL_ENV_KEYS).toEqual([...])` assertion to include `'STT_MODEL'`
(the `.env.example` sync assertion below it then passes on its own), and add one
new case asserting `loadEnv().STT_MODEL` is `''` when the variable is entirely
absent from `process.env`, copying the structure of the two existing
`BETA_ALLOWLIST` cases verbatim.
  </action>
  <verify>
    <automated>npx vitest run src/config/env.test.ts &amp;&amp; npm run typecheck &amp;&amp; test $(grep -c '^STT_MODEL=$' .env.example) -eq 1 &amp;&amp; test $(grep -rn "gpt-4o-mini-transcribe" src/ | grep -vc "src/adapters/stt/types.ts") -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^STT_MODEL=$' .env.example` prints 1
    - `src/config/env.ts` contains `'STT_MODEL'` on the `OPTIONAL_ENV_KEYS` line and `REQUIRED_ENV_KEYS` still lists exactly `DATABASE_URL`, `OPENAI_API_KEY`, `TELEGRAM_BOT_TOKEN`
    - `grep -rn "gpt-4o-mini-transcribe" src/` lists only `src/adapters/stt/types.ts`
    - `grep -c "^DAILY_MESSAGE_CAP=\|^MAX_VOICE_SECONDS=\|^DECOMPOSITION_MODEL=" .env.example` prints 0 (the two spend guards and the LLM model stay code constants)
    - `npx vitest run src/config/env.test.ts` exits 0, including the existing `.env.example` sync case and a new case asserting `STT_MODEL` defaults to `''`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>One optional, empty-by-default env key exists; the owner's current `.env` still starts the bot unchanged; and no environment variable can weaken a spend guard.</done>
</task>

<task type="auto">
  <name>Task 2: Construct the adapters once and register the handlers behind the allowlist</name>
  <files>src/bot/pipeline-wiring.ts, src/bot/pipeline-wiring.test.ts, src/bot/bot.ts, src/bot/bot.wiring.test.ts</files>
  <read_first>
    - src/bot/bot.ts (the file being modified — the six numbered registration sections, the `BotDeps` interface, and the module doc comment stating the order is load-bearing and that this file never calls `loadEnv()` or `createDb()`)
    - src/bot/bot.wiring.test.ts (the file being modified — `botSource()` strips comment lines, and the `indexOf` assertions in "bot.ts registration order (D-05 / T-02-23)" are the exact pattern to extend)
    - .planning/phases/03-voice-pipeline/03-PATTERNS.md sections "`src/bot/bot.ts` (composition root, modified)" and "bot.ts registration order is load-bearing"
    - .planning/phases/03-voice-pipeline/03-07-PLAN.md `<interfaces>` block (exact signatures of `createVoiceHandler`, `createTextHandler`, `createUnsupportedHandler`, `MealHandlerDeps`, `createMessageEditor`) and its threat T-03-44, which delegates the ordering assertion to this task
    - .planning/phases/03-voice-pipeline/03-06-PLAN.md `<interfaces>` block (`PipelineDeps` — the six collaborators that must be constructed here)
    - src/adapters/embeddings/openai-embed.ts and src/adapters/fdc-repository.ts (the two Phase 1 factories reused unchanged: `createOpenAIEmbedder`, `createDrizzleFdcRepository`)
    - src/adapters/stt/types.ts (plan 03-01 — `STT_MODEL`, `STT_COMPARISON_MODEL`, the only two values `resolveSttModel` may return)
    - src/bot/commands/start.ts (the `createStartHandler(db)` factory-closure style these registrations follow)
  </read_first>
  <behavior>
    - resolveSttModel('') returns STT_MODEL and logs nothing
    - resolveSttModel(STT_COMPARISON_MODEL) returns STT_COMPARISON_MODEL
    - resolveSttModel with surrounding whitespace trims and still recognises the value
    - resolveSttModel('whisper-9000') returns STT_MODEL and logs exactly one Russian warning line naming both accepted values
    - buildMealHandlerDeps returns an object whose `deps` carries all six PipelineDeps fields, each defined, plus top-level `db` and `token`
    - buildMealHandlerDeps passes resolveSttModel's result into the transcriber factory, asserted through the injected factory seam
    - buildMealHandlerDeps runs with no OPENAI_API_KEY and no DATABASE_URL set in the test process and performs no network call
    - bot.ts source: the index of 'createAllowlistMiddleware' is lower than the index of EVERY match of /bot\.(on|command|callbackQuery|hears)\(/ in the comment-stripped source
    - bot.ts source registers 'message:voice', 'message:text' and all six unsupported content types
  </behavior>
  <action>
Create `src/bot/pipeline-wiring.ts` — the small module that turns raw deps into
the object plan 03-07's handlers expect. It is a separate file rather than
inline code in `bot.ts` for one reason: `bot.ts`'s doc comment forbids that file
from reading env or building infrastructure, and keeping that true is what lets
a future webhook entrypoint reuse `createBot()` unchanged (D-09 leaves that seam
alone).

`resolveSttModel(rawOverride: string): string` — trim the input; if empty return
`STT_MODEL`; if it equals `STT_COMPARISON_MODEL` return that; otherwise return
`STT_MODEL` and `console.log` ONE Russian line stating the value was not
recognised, echoing what was typed, naming both accepted values, and saying the
cheap default is being used. Echoing the raw value is intentional — it is a
model name the owner typed, not a secret, and seeing their own typo is the
fastest possible diagnosis. Build the allowlist from the two imported constants
in `../adapters/stt/types.js`, never from string literals, so plan 03-01's
single-constant rule (D-03) survives. That allowlist is also the mitigation for
a mistyped or deliberately expensive model draining the balance: an unrecognised
value can only ever fall back to the cheaper model, never to an arbitrary one.

`buildMealHandlerDeps({ db, token, api, sttModel })` constructs, exactly once
per process:
- `createOpenAITranscriber({ model: resolveSttModel(sttModel) })`
- `createOpenAIDecomposer()`
- `createOpenAIEmbedder()` — the same factory the offline indexer used, at the
  same model and dimension count `fdc_foods` was indexed with. Do NOT pass a
  different model or dimension count here: a mismatch silently destroys
  retrieval quality (03-CONTEXT.md names this a hard constraint).
- `createDrizzleFdcRepository(db)`
- `createMessageEditor(api)`
and returns `{ db, token, deps: { db, transcriber, decomposer, embedder, repo,
editor } }` typed as `MealHandlerDeps`. Construct once at startup, never per
update: building a new OpenAI client for every voice message is pure waste.

For testability without an API key, accept an optional `factories?` override
object on `MealWiringDeps` defaulting to the real factories — the same seam
`createOpenAIEmbedder` uses for its `client?`. The unit test injects fakes;
production passes nothing.

Modify `src/bot/bot.ts`:
- Add `sttModel: string` to `BotDeps`. It is resolved from env by `index.ts` in
  Task 3, so `bot.ts` still never calls `loadEnv()`.
- At the top of section "5. Handlers", before any registration, call
  `const mealDeps = buildMealHandlerDeps({ db: deps.db, token: deps.token, api: bot.api, sttModel: deps.sttModel })`.
- Register, AFTER the existing `/whoami`, `/start`, `/cancel` and
  `callbackQuery` registrations so commands keep winning over the text handler:
  `bot.on('message:voice', createVoiceHandler(mealDeps))`,
  `bot.on('message:text', createTextHandler(mealDeps))`, and one registration
  covering the six unsupported content types (`audio`, `video_note`, `photo`,
  `document`, `sticker`, `video`) with `createUnsupportedHandler()`. Use
  grammY's array-of-filters form so the six types are one registration rather
  than six; verify the exact filter-string shape against the installed
  grammy 1.45.1 types before writing it rather than guessing.
- Update the module doc comment's registration-order sentence so it names the
  meal handlers in the same final tier as the commands, and add one sentence
  stating the security property: every registration in section 5 sits behind the
  section-1 allowlist gate, so a non-allowlisted user's update is dropped before
  it can reach a paid call. The comment is the explanation; the wiring test
  below is the enforcement.
- Do not touch sections 1-4 and do not reorder anything that already exists.

Extend `src/bot/bot.wiring.test.ts` with a new `describe` block for the Phase 3
registrations. The load-bearing assertion is the general one, not a
per-handler one: take the comment-stripped source, find
`createAllowlistMiddleware`'s index, then find EVERY match index of
`/bot\.(on|command|callbackQuery|hears)\(/g` and assert each one is greater.
Written that way, a future handler added anywhere above the gate fails this test
— which is what T-03-44 actually asked for and what a `createVoiceHandler`-only
assertion would not give. Add two supporting assertions: the source contains
`'message:voice'` and `'message:text'`, and it contains all six unsupported
content-type filters.

Write `src/bot/pipeline-wiring.test.ts` covering every behaviour bullet, using
`vi.spyOn(console, 'log')` for the warning case and restoring it afterwards. No
test may require `OPENAI_API_KEY`, `DATABASE_URL`, or a network call.
  </action>
  <verify>
    <automated>npx vitest run src/bot/pipeline-wiring.test.ts src/bot/bot.wiring.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/bot/pipeline-wiring.ts` exports `resolveSttModel` and `buildMealHandlerDeps`, and contains `createOpenAITranscriber`, `createOpenAIDecomposer`, `createOpenAIEmbedder`, `createDrizzleFdcRepository`, `createMessageEditor`
    - `grep -c "gpt-4o" src/bot/pipeline-wiring.ts` prints 0 (model names arrive only as imported constants)
    - `src/bot/bot.ts` contains `createVoiceHandler`, `createTextHandler`, `createUnsupportedHandler`, `buildMealHandlerDeps`, `'message:voice'` and `'message:text'`
    - `npx vitest run src/bot/bot.wiring.test.ts` exits 0 and contains a test that compares every `bot.on(`/`bot.command(`/`bot.callbackQuery(` occurrence index against the `createAllowlistMiddleware` index; temporarily moving the `bot.use(createAllowlistMiddleware(...))` line below the handlers must make that test fail (verify this once by hand, then restore the file)
    - `npx vitest run src/bot/pipeline-wiring.test.ts` exits 0 with at least 6 passing tests, including the unknown-model fallback-plus-warning case and the "no API key needed" construction case
    - `npm run typecheck` exits 0 and `npm test` exits 0
  </acceptance_criteria>
  <done>Voice, text and unsupported messages reach their handlers through real adapters built once at startup, and an automated test proves no handler can ever be registered ahead of the allowlist gate.</done>
</task>

<task type="auto">
  <name>Task 3: The D-11 startup sweep for runs interrupted by a restart</name>
  <files>src/bot/startup-sweep.ts, src/bot/startup-sweep.test.ts, src/bot/index.ts</files>
  <read_first>
    - src/bot/index.ts (the file being modified — `main()`'s order of `loadEnv` → `createDb` → `parseAllowlist` → `createBot` → `bot.start()`, the shutdown handlers, and the invariant that a caught grammY error object is never passed whole to `console.error` because it can carry the token)
    - src/application/idempotency.ts (plan 03-05 — `findInterruptedUpdates(db)` and `markInterrupted(db, updateIds)`; note `markInterrupted([])` deliberately issues no query, and note the module's statement that the caller notifies and then marks, with no resume)
    - src/bot/formatting/pipeline-copy.ts (plan 03-03 — `pipelineCopy.interruptedByRestart`, the exact string to send; do not compose a new one here)
    - src/bot/error-handler.ts (the logging invariant this module must hold: a short Russian line, never the update, never the token, never user content)
    - src/bot/storage/pg-storage-adapter.test.ts (the fake-`db` construction style for testing Drizzle calls without a database)
    - .planning/phases/03-voice-pipeline/03-CONTEXT.md D-11 and D-05 (the reasoning for notify-then-terminate and for the absence of any resume path)
  </read_first>
  <behavior>
    - no interrupted rows: findInterruptedUpdates is called once, notify is never called, markInterrupted is never called with a non-empty array, and the report is { found: 0, notified: 0, notifyFailed: 0 }
    - two interrupted rows: notify is called exactly twice, each with that row's chatId and with pipelineCopy.interruptedByRestart, and markInterrupted is called once with both updateIds
    - a notify that rejects for one row: the other row is still notified, markInterrupted is still called with BOTH ids, and the report shows notifyFailed 1
    - findInterruptedUpdates rejecting: runStartupSweep resolves (never rejects) with a zeroed report and logs one line
    - markInterrupted rejecting: runStartupSweep still resolves
    - the module never calls processMeal, downloadVoice or any transcription function — asserted by a source grep, not by a comment
    - no log line produced by the module contains a chat id, a telegram id or message text
  </behavior>
  <action>
Create `src/bot/startup-sweep.ts` exporting `runStartupSweep(deps)`.

What it does, and why each step is what D-11 asked for:
1. `findInterruptedUpdates(db)` — a `processed_updates` row still in status
   `processing` when the process starts can only mean the previous run died
   mid-pipeline (Ctrl+C, a closed laptop, a crash). That user is sitting in
   front of "Секунду, разбираю 🎧" that will never change.
2. For each row, `await deps.notify(row.chatId, pipelineCopy.interruptedByRestart)`
   inside its own try/catch. A Telegram send can fail (the user blocked the bot,
   the network is down); that must not stop the remaining rows.
3. `markInterrupted(db, allFoundIds)` — for ALL found rows, including any whose
   notification failed. Leaving an un-notified row in `processing` would make it
   reappear in every future sweep and re-apologise on every restart forever; one
   lost apology is better than an infinite loop of them.
4. Return `{ found, notified, notifyFailed }` and log ONE Russian summary line
   carrying those three counts and nothing else.

What it deliberately does NOT do: resume. The audio was never written to disk
(D-05), so there is nothing to re-transcribe, and a partial resume from the
persisted transcript is not worth the branch in a closed beta. This file must
not import `processMeal`, `downloadVoice`, the transcriber or the decomposer,
and the acceptance criteria grep-gate exactly that.

Wrap the whole body so `runStartupSweep` can never reject: a database that is
briefly unreachable at startup must produce one Russian line and a zeroed
report, not a crashed `npm run bot`. Log counts and the error kind only — never
a chat id, never a telegram id, never any message content (`error-handler.ts`'s
invariant).

Take `notify` as an injected function rather than a grammY `Api` or a `Bot`, in
the structural-typing style of `ack.ts` and `message-editor.ts`, so the whole
sweep is unit-testable with a two-line fake.

Modify `src/bot/index.ts`:
- After `createBot(...)` and before `bot.start(...)`, add
  `await runStartupSweep({ db, notify: (chatId, text) => bot.api.sendMessage(chatId, text) })`.
- State two ordering points in a comment: it runs before `bot.start()` so the
  apology reaches the user before any newly-polled update is handled, and it is
  awaited rather than detached so its summary line is not interleaved into live
  traffic. `bot.api` works before polling starts — an API call needs the token,
  not a running update loop.
- Pass `sttModel: env.STT_MODEL` into `createBot` (Task 2 added the field).
- Change nothing else: no webhook, no `setWebhook`, no HTTP server. D-09 keeps
  Phase 3 on long polling on the owner's machine, and `index.ts` remains the
  only file that calls `bot.start()`.

Write `src/bot/startup-sweep.test.ts` covering every behaviour bullet with a
fake `db` and a recording `notify`, in `pg-storage-adapter.test.ts`'s style. The
"notify rejects for one row" case is the important one — assert both that the
second row was still notified and that `markInterrupted` received both ids.
  </action>
  <verify>
    <automated>npx vitest run src/bot/startup-sweep.test.ts &amp;&amp; npm run typecheck &amp;&amp; test $(grep -v '^\s*[*/]' src/bot/startup-sweep.ts | grep -c "processMeal\|downloadVoice\|transcribe") -eq 0 &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/bot/startup-sweep.ts` exports `runStartupSweep` and contains `findInterruptedUpdates`, `markInterrupted` and `pipelineCopy.interruptedByRestart`
    - `grep -v '^\s*[*/]' src/bot/startup-sweep.ts | grep -c "processMeal\|downloadVoice\|transcribe"` prints 0 (no resume path, D-11)
    - `grep -c "console.log(row\|chatId}\|telegramId}" src/bot/startup-sweep.ts` prints 0 (counts only in logs)
    - `src/bot/index.ts` contains `runStartupSweep` and `sttModel: env.STT_MODEL`, and `grep -c "setWebhook\|webhookCallback\|express\|createServer" src/bot/index.ts` prints 0 (D-09)
    - `npx vitest run src/bot/startup-sweep.test.ts` exits 0 with at least 6 passing tests, including the partial-notify-failure case and the database-rejects case proving the function resolves
    - `npm run typecheck` exits 0 and `npm test` exits 0
  </acceptance_criteria>
  <done>A crash mid-pipeline leaves no user stranded on "Секунду, разбираю 🎧" and no row stuck in `processing`, and a failing sweep can never stop the bot from starting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Owner runs the whole chain end to end in Telegram</name>
  <files>none (manual verification against the real bot)</files>
  <read_first>
    - .planning/ROADMAP.md "Phase 3: Voice pipeline" success criteria 1, 3, 4 and 5 (the four this check grades; criterion 2 is graded separately by `npm run verify-stt`, plan 03-01)
    - .planning/phases/03-voice-pipeline/03-VALIDATION.md "Manual-Only Verifications" (why decomposition quality and the end-to-end Telegram path cannot be asserted with fakes)
    - src/bot/formatting/pipeline-copy.ts (the exact Russian strings that should appear, so the executor can tell the owner what to look for verbatim)
  </read_first>
  <what-built>
The full Phase 3 chain is now connected: a voice or text message describing a
meal is acknowledged instantly, transcribed, broken into ingredients with gram
estimates, each ingredient matched against the USDA FDC data indexed in Phase 1,
and the result replaces the acknowledgement message in place. Everything before
this task was verified with fakes — no real audio, no real OpenAI call, no real
Telegram. This task is the first time it runs for real, and it costs real money
(roughly $0.002 per message; the whole check below is a few cents).
  </what-built>
  <how-to-verify>
Do these in order. Every command is run from the project root
`/Users/smatov/GitLab/VoxBite` in a terminal.

**Step 0 — check the setup is intact**
Run: `npm run check-setup`
Expected: the last line printed is `SETUP OK`. If any line starts with `[FAIL]`,
stop and paste that line into the chat — it names exactly what is wrong.

**Step 1 — start the bot**
Run: `npm run bot`
Expected within a few seconds:
- a line `Список допущенных telegram_id (BETA_ALLOWLIST): N шт.` where N is at
  least 1 (if it says 0, your own id is not in `.env` — follow the instructions
  the bot prints right below that line);
- a one-line summary from the startup sweep about interrupted analyses (on a
  clean run it reports 0 found);
- `Бот запущен: @<имя_бота>. Останови его сочетанием Ctrl+C.`
Leave this terminal open and visible — the cost line in step 3 appears here.

**Step 2 — success criterion 1: voice message and instant acknowledgement**
In Telegram, open a chat with your bot and hold the microphone button to record
a short voice message describing a real meal, for example: "съел двести грамм
плова и один банан". Send it.
Expected: within about a second, before any analysis could possibly have
finished, the bot replies `Секунду, разбираю 🎧`.

**Step 3 — success criterion 3 and decision D-13: the card replaces that same message**
Wait for the analysis (a few seconds).
Expected:
- The `Секунду, разбираю 🎧` message you already see **changes into** the result
  card. A *new* message must NOT appear below it. If you end up with two bot
  messages, that is a bug — say so.
- The card lists each component with its grams and, under each, an English USDA
  description (e.g. `Rice, white, cooked`). Descriptions in English are correct
  and expected — that is the USDA record verbatim.
- Components with a poor match carry a visible `совпадение слабое, проверь`
  marker. That is working as designed, not an error.
- There are NO calorie/protein/fat/carb/sugar numbers on the card. КБЖУ
  arithmetic is Phase 4 — if you see numbers, that is a bug.
- In the terminal from step 1, exactly ONE cost line appeared for this message,
  showing seconds, tokens and an estimated dollar amount. It must NOT contain
  the text of what you said.

**Step 4 — success criterion 3: a single-ingredient dish stays single**
Send a voice message saying just: "съел один банан".
Expected: the card shows exactly ONE component. If it splits a banana into
parts, note it — that is a decomposition-prompt problem worth reporting.

**Step 5 — success criterion 5: typed text takes the same path**
Type and send, as ordinary text: `двести грамм гречки и яйцо`
Expected: the same `Секунду, разбираю 🎧` → card sequence, with no voice
message involved.

**Step 6 — success criterion 4: no food in the message**
Type and send: `привет, как дела`
Expected: the acknowledgement is replaced by a friendly Russian message saying
no food was heard and asking you to describe what you ate. It must read as a
normal answer, not as an error. (The other half of criterion 4 — the model
returning broken output, one retry, then "не смог разобрать" — cannot be
triggered on demand and is covered by the automated tests in plan 03-04.)

**Step 7 — decision D-06: unsupported message types cost nothing**
Send the bot a photo, and then a video note (кружочек).
Expected: a short polite Russian refusal for each, asking for a voice message or
text instead. Critically, NO new cost line appears in the terminal for either —
these must be free.

**Step 8 — decisions D-10 and D-11: interrupt a run and restart**
Send a voice message and, the moment the `Секунду, разбираю 🎧` appears, press
Ctrl+C in the terminal to stop the bot mid-analysis. Then run `npm run bot`
again.
Expected:
- On startup, the bot sends you a Russian message saying the analysis was
  interrupted and to send the message again. This is D-11 — it will NOT redo the
  analysis by itself, because the recording is deliberately not stored anywhere.
- The startup summary line in the terminal reports 1 interrupted analysis found.
- The interrupted message is NOT reprocessed: no new cost line for it appears.
- Starting the bot a third time reports 0 interrupted analyses — the row was
  resolved, not left to repeat forever.

**Step 9 — stop**
Press Ctrl+C. Expected: `Останавливаю бота...` and the process exits.

Report back: which steps behaved as described, and for any that did not, what
you saw instead (paste the terminal lines, and describe the Telegram messages —
do not paste anything containing your bot token).
  </how-to-verify>
  <acceptance_criteria>
    - `npm run check-setup` printed `SETUP OK`
    - `npm run bot` started, printed the startup-sweep summary line, and printed `Бот запущен:`
    - Step 2: the reply `Секунду, разбираю 🎧` arrived before the analysis finished (roadmap criterion 1)
    - Step 3: that same message was edited into the card — the owner confirms exactly one bot message exists for that meal (D-13), the card carries per-component grams and a USDA description, and it contains no КБЖУ numbers (D-20)
    - Step 3: exactly one cost line per processed message appeared in the terminal and contained no transcript text (D-17)
    - Step 4: a single-ingredient message produced exactly one component (roadmap criterion 3)
    - Step 5: typed text produced the same ack-then-card sequence (roadmap criterion 5)
    - Step 6: a message with no food produced the friendly "не услышал еды" reply, not an error (D-08, roadmap criterion 4 partial)
    - Step 7: a photo and a video note each produced a refusal and produced NO cost line (D-06)
    - Step 8: after an interrupted run and a restart, the owner received the interrupted-by-restart notice, no reprocessing occurred, and the next restart found 0 interrupted rows (D-10, D-11, roadmap criterion 5 idempotency)
  </acceptance_criteria>
  <resume-signal>Type "approved" when the steps behaved as described, or paste what you saw instead for any step that did not.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram update stream → registered handlers | the allowlist middleware is the only thing standing between an arbitrary Telegram user and a paid OpenAI call; its position in the registration order is the boundary |
| `.env` file → runtime behaviour | a text file the owner edits by hand now influences which paid model is used |
| crashed previous process → this process | `processed_updates` rows in `processing` are the only trace of a run that died; the sweep reads them and messages real users on the strength of them |
| startup sweep → Telegram | the sweep sends unsolicited messages at boot, to chat ids read from the database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-46 | Elevation of Privilege | a handler registered ahead of the allowlist gate in `bot.ts` | mitigate | `bot.wiring.test.ts` asserts by source index that EVERY `bot.on`/`bot.command`/`bot.callbackQuery` registration appears after `createAllowlistMiddleware`; the assertion is generic, so a future handler added above the gate fails the suite. Closes T-03-44 from plan 03-07 |
| T-03-47 | Denial of Service (financial) | a new env key silently weakening the D-14 duration cap or the D-15 daily cap | mitigate | no env key is created for either guard; both stay code constants, grep-gated by the acceptance criterion that `.env.example` declares neither. The `.env.example` comment records why, so a future edit is a deliberate act rather than an oversight |
| T-03-48 | Denial of Service (financial) | `STT_MODEL` override selecting an unknown or arbitrarily expensive model | mitigate | `resolveSttModel` accepts only the two constants exported by `src/adapters/stt/types.ts`; anything else falls back to the cheaper default and logs one warning line. The failure mode is "cheaper than intended", never "more expensive" |
| T-03-49 | Information Disclosure | startup sweep logging chat ids, telegram ids or transcripts | mitigate | the sweep logs three counts and an error kind only; acceptance criteria grep-gate id interpolation, extending `error-handler.ts`'s invariant |
| T-03-50 | Denial of Service (availability) | the sweep crashing `npm run bot` when the database is unreachable at boot | mitigate | `runStartupSweep` never rejects; a failure yields a zeroed report plus one Russian line, and `bot.start()` proceeds. Asserted by a unit test where `findInterruptedUpdates` rejects |
| T-03-51 | Spoofing / unsolicited messaging | the sweep messaging an arbitrary chat id | mitigate | rows in `processed_updates` are only ever written by `claimUpdate`, which runs downstream of the allowlist gate — so every chat id the sweep can reach belonged to an allowlisted user at the time the row was created |
| T-03-52 | Repudiation | an interrupted row whose notification failed being swept again on every restart | mitigate | `markInterrupted` is applied to all found ids regardless of notification outcome, so a row reaches a terminal status exactly once; the report's `notifyFailed` count is what surfaces the lost apology |
| T-03-53 | Information Disclosure | the bot token reachable through `bot.api` errors during the sweep | mitigate | the sweep's catch blocks never pass a caught grammY error object whole to `console.error` — only the error kind — matching the invariant already stated in `src/bot/index.ts` |
| T-03-54 | Elevation of Privilege | no inbound HTTP surface exists to attack | accept | D-09 keeps Phase 3 on long polling on the owner's machine; there is no webhook endpoint, no port bound, and no request authentication to get wrong. Revisit when deployment is planned |
</threat_model>

<verification>
- `npm test` full suite green (every plan 01-08 test file)
- `npm run typecheck` exits 0
- `npx vitest run src/bot/bot.wiring.test.ts` green, including the
  every-registration-after-the-allowlist assertion
- `grep -c "setWebhook\|webhookCallback" src/` prints 0 (D-09)
- `.env.example` and `src/config/env.ts` agree — `src/config/env.test.ts` green
- Task 4 completed by the owner with all nine steps reported
</verification>

<success_criteria>
- A real voice message in Telegram produces an immediate `Секунду, разбираю 🎧`
  that is later edited in place into the result card (roadmap criteria 1 and 3,
  D-13)
- Typed text produces the identical result through the identical pipeline
  (roadmap criterion 5, VOICE-04)
- An interrupted run is reported to the affected user at the next startup and
  its ledger row reaches a terminal status, with no auto-resume (D-11)
- No handler can be registered ahead of the allowlist gate without failing the
  test suite (T-03-44 closed)
- Exactly one cost line per processed message appears in the terminal, carrying
  no user content (D-17)
- The owner's existing `.env` still starts the bot unchanged, and no environment
  variable can weaken the D-14 or D-15 spend guards
</success_criteria>

<output>
After completion, create `.planning/phases/03-voice-pipeline/03-08-SUMMARY.md`
</output>
