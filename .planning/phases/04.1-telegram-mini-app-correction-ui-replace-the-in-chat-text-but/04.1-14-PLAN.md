---
phase: 04.1
plan: 14
type: execute
wave: 8
depends_on: ["04.1-13"]
files_modified:
  - docs/phase-04.1-manual-checklist.md
autonomous: false
requirements: [CORRECT-01, CORRECT-02, CORRECT-03, CORRECT-04, CORRECT-05, CORRECT-06, CORRECT-07, CORRECT-08, CALC-01, CALC-02, DIARY-01]

must_haves:
  truths:
    - "The owner has personally exercised every correction action inside a real Telegram WebView and confirmed it works"
    - "Draft state provably survives a bot restart mid-correction"
    - "A meal with a sugar-less ingredient shows 'нет данных', not 0, in the Mini App"
    - "A saved entry can be reopened, edited, and deleted, and the diary row follows"
    - "No component list, gram value or nutrient number appears in the chat message any more"
  artifacts:
    - path: "docs/phase-04.1-manual-checklist.md"
      provides: "owner-executable end-to-end verification script for the whole phase"
      contains: "нет данных"
  key_links:
    - from: "docs/phase-04.1-manual-checklist.md"
      to: "every phase requirement ID"
      via: "one scenario per requirement, labelled with its ID"
      pattern: "CORRECT-0"
---

<objective>
Write the owner-executable manual checklist for this phase and have the owner walk it end to end
inside real Telegram, then record the outcome.

Purpose: there is no way to script a real Telegram WebView session (04.1-VALIDATION.md's
Manual-Only Verifications table), and this phase exists precisely because a fully green automated
suite once coexisted with a feature that did not work in production (04-UAT.md). Automated tests
prove the pieces behave; only this walkthrough proves the product does.
Output: `docs/phase-04.1-manual-checklist.md` plus a recorded pass/fail per requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-CONTEXT.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-UI-SPEC.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-VALIDATION.md
@.planning/phases/04-confirm-correct-diary-persistence/04-UAT.md
@docs/phase-04-manual-checklist.md
@docs/phase-04.1-deploy.md
</context>

<interfaces>
Follow `docs/phase-04-manual-checklist.md` exactly as the format template: Russian, a preflight
section first, one numbered scenario per requirement with its ID in the heading, and for every step
three things — what to do, what you should see (verbatim where it is bot/UI text), and what it means
if you see something else. Its opening glossary paragraph exists because the owner has no backend
experience; write a new one covering THIS phase's new terms (`Mini App`, `WebView`, `initData`,
`деплой`, `serverless-функция`) in the same plain style.

Requirements to cover, from `.planning/REQUIREMENTS.md` and this phase's ROADMAP entry:
CORRECT-01 (see the analysis), CORRECT-02 (confirm as-is), CORRECT-03 (swap candidate),
CORRECT-04 (±10 г and typed grams), CORRECT-05 (remove a component), CORRECT-06 (add one by text,
FDC-matched), CORRECT-07 (draft state survives a bot restart), CORRECT-08 (edit or delete a saved
entry), CALC-01 (totals are arithmetic, not LLM), CALC-02 ("нет данных", never 0), DIARY-01 (saved
to the right day in the user's timezone).

Decisions that must be visually confirmed, not just assumed:
- D-02: the analysis message carries its own Open button, addressed to that specific draft.
- D-03: the chat message shows NO component list, NO gram values, NO candidate names, NO numbers.
- D-04: typing a message while a draft is open starts a NEW meal analysis — it is no longer
  intercepted as a correction. This is the removed defect; the checklist must confirm the removal
  behaves as intended rather than assuming it.

Known accepted gap to state plainly in the document rather than let the owner discover it as a bug
(CONTEXT.md's Deferred section): the only way back to an already-saved entry is scrolling chat
history to its original message and tapping its still-working Open button. Phase 5's diary views
close this.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the owner-executable manual checklist</name>
  <read_first>
    - docs/phase-04-manual-checklist.md (the format, tone and level of hand-holding to match)
    - docs/phase-04.1-deploy.md (written in plan 13 — the checklist's preflight references it)
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-UI-SPEC.md (every string the owner should expect to see on screen)
    - .planning/REQUIREMENTS.md (the exact Russian wording of each requirement)
    - .planning/phases/04-confirm-correct-diary-persistence/04-UAT.md (rounds 3-4 — the scenario that must now behave differently)
  </read_first>
  <files>docs/phase-04.1-manual-checklist.md</files>
  <action>
    Write the document in Russian, mirroring `docs/phase-04-manual-checklist.md`'s structure.

    Section 0 — Preflight, mandatory first:
    - `npm test` green; `npm run typecheck` clean; `npm run check-setup` prints `SETUP OK`.
    - The Vercel deployment shows "Ready" and opening its URL in a plain browser shows the Russian
      "разбор устарел" message (explain again that this is correct outside Telegram).
    - `npm run bot` starts without the missing-env error (which would mean `MINI_APP_BASE_URL` is
      not set locally — point at `docs/phase-04.1-deploy.md`).

    Then one scenario per requirement, each headed with its ID. At minimum:
    - **CORRECT-01 / D-02 / D-03.** Send a voice message describing a multi-ingredient meal. Confirm
      the chat shows the ack, then that same message gains a `🔧 Открыть для коррекции` button.
      Explicitly confirm the chat message contains NO ingredient names, NO gram numbers and NO
      calories — write out that this is intentional (D-03) and what to report if numbers appear.
      Tap the button; the Mini App opens inside Telegram (not in an external browser) and lists
      every component with its grams and matched food.
    - **CORRECT-03.** Tap `Сменить вариант` on one component; up to three alternatives appear
      inline with a `✓` on the current one; pick a different one; the row and the header totals
      both change.
    - **CORRECT-04.** Tap `+10 г` and `−10 г`; then tap the number itself, type `250`, submit.
      Then type `abc` and confirm `Не понял количество. Напиши число в граммах, например 200.`
      appears under the field and the value did not change.
    - **CORRECT-05.** Remove one component; the list and totals update. Then remove ALL of them and
      confirm the empty state (`Компонентов не осталось.` + `Добавь один заново или отмени разбор.`)
      appears with both `➕ Добавить` and `✕ Отменить разбор`, and that the confirm button is
      disabled.
    - **CORRECT-06.** Add a component by typing a name; confirm a real food is matched and appears
      as a new row with candidates of its own.
    - **CALC-02.** Include an ingredient FDC has no sugar value for. Confirm the sugar line reads
      `нет данных` — in BOTH the running total and, where shown, per component. State bluntly that
      a `0` here is a failure, not a rounding artefact, and must be reported.
    - **CALC-01.** Note the totals shown before confirming; after confirming, check the saved diary
      row's numbers match. Give the exact `npm run db:studio` steps (as the Phase 4 checklist does)
      for looking at the `diary` table.
    - **CORRECT-02 / DIARY-01.** Confirm the analysis. Expect `Сохранено в дневник` briefly, then
      the Mini App closing by itself. Then verify in Drizzle Studio that exactly ONE new `diary`
      row exists and its `local_date` is today's date in the owner's own timezone — including the
      late-evening edge case if the owner can test it.
    - **CORRECT-07.** Start a correction, change one gram value, then stop the bot with `Ctrl+C`
      and start it again with `npm run bot`. Reopen the Mini App from the same chat message and
      confirm the changed value is still there. Explain why this matters: the draft lives in the
      database, not in the bot's memory. Note that the Mini App itself keeps working while the bot
      is stopped, because it talks to the database directly — that is expected, not a bug.
    - **CORRECT-08.** Reopen the message of the entry saved above. Confirm the footer now shows
      `Сохранить изменения` and `🗑 Удалить` instead of `✅ Подтвердить`. Change a gram value, tap
      `Сохранить изменения`, then check in Drizzle Studio that the SAME `diary` row's numbers
      changed and that NO second row appeared. Then tap `🗑 Удалить`, confirm the two-step prompt
      `Удалить запись? Это навсегда.` with `Да, удалить` / `Нет` appears in place, tap `Нет` first
      and confirm nothing was deleted, then repeat and tap `Да, удалить` and confirm the row is
      gone.
    - **D-04 removal check (the reason this phase exists).** With a draft open and un-confirmed,
      send a plain text message in the chat, e.g. `200`. Confirm it is treated as a NEW meal
      description (it gets its own ack and its own Open button) and is NOT applied as a correction
      to the open draft. Reference 04-UAT.md rounds 3-4 in one sentence: the old behaviour silently
      routed such a message into the open draft — and sometimes into the paid analysis pipeline,
      creating a duplicate diary row. Getting a new, separate analysis here is the fixed behaviour.
    - **Theme check.** Switch Telegram between light and dark themes and reopen the Mini App;
      colours follow. Confirm every button is comfortably tappable with a thumb.

    Close with a "Что делать, если что-то не так" section: how to find the Vercel build log, how to
    read the bot's terminal output, and the instruction to report the exact on-screen text rather
    than paraphrasing it.

    Also include the accepted-gap note from `<interfaces>` about reaching old saved entries only by
    scrolling chat history until Phase 5 ships, so the owner does not report it as a defect.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('docs/phase-04.1-manual-checklist.md','utf8'); const ids=['CORRECT-01','CORRECT-02','CORRECT-03','CORRECT-04','CORRECT-05','CORRECT-06','CORRECT-07','CORRECT-08','CALC-01','CALC-02','DIARY-01']; const missing=ids.filter(i=>!c.includes(i)); if(missing.length) throw new Error('checklist missing scenarios for: '+missing.join(', ')); for (const s of ['нет данных','Удалить запись? Это навсегда.','Компонентов не осталось.','Открыть для коррекции','Сохранено в дневник']) if(!c.includes(s)) throw new Error('checklist missing expected UI string: '+s); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The automated check above exits 0 (every requirement ID and five key UI strings present).
    - The document is in Russian and opens with a glossary covering `Mini App`, `WebView`,
      `initData` and `деплой` in plain language.
    - Section 0 preflight includes `npm test`, `npm run typecheck`, `npm run check-setup` and the
      Vercel "Ready" check.
    - Every scenario states what to do, what should be seen, and what it means otherwise — spot
      check three scenarios and confirm all three parts are present in each.
    - The D-04 removal scenario exists and explicitly references 04-UAT.md rounds 3-4.
    - The accepted-gap note about reaching old saved entries is present.
  </acceptance_criteria>
  <done>The owner has a single document that walks them from a green test suite to a fully exercised product, in language that assumes no backend knowledge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner walks the checklist end to end in real Telegram</name>
  <read_first>
    - docs/phase-04.1-manual-checklist.md
  </read_first>
  <action>
    Pause. Point the owner at `docs/phase-04.1-manual-checklist.md` and wait for a per-scenario
    result. There is no way to script a real Telegram WebView session, so this cannot be automated.
    Do not mark the plan complete on a partial walkthrough, and do not paraphrase the owner's
    reported failures — record their literal wording for the phase verifier.
  </action>
  <what-built>
    The whole phase: the bot now attaches a `🔧 Открыть для коррекции` button to each analysed
    meal instead of drawing a card in chat, and every correction happens on a real touch screen
    inside Telegram. The old in-chat correction flow, its buttons, and the text-input gate that
    misrouted typed messages have been deleted, and the database column that backed the gate has
    been dropped.

    Everything below this point has been verified by automated tests at the level of individual
    functions and endpoints. What has NOT been verified is the product: whether a real voice
    message, in the real Telegram app, on your real phone, ends up as a correct diary entry.
    This phase exists because that exact gap once hid a broken feature behind a fully green test
    suite. This is the check that closes it.

    Expect it to take roughly 20-30 minutes and to spend a few cents of OpenAI credit (each voice
    message runs transcription and decomposition; each added component runs one embedding).
  </what-built>
  <how-to-verify>
    1. Open `docs/phase-04.1-manual-checklist.md` in the project.
    2. Do Section 0 (preflight) first and do not continue past it if any step fails.
    3. Work through every scenario in order, on a real phone, in the real Telegram app — not in
       Telegram Web or Telegram Desktop for the Mini App parts, since the WebView behaves
       differently there.
    4. Tick each box as it passes. For anything that does not match, write down the EXACT text you
       saw on screen (or take a screenshot) rather than describing it in your own words — the
       precise wording is usually what identifies the cause.
    5. Reply with either "all passed", or a numbered list of every scenario that failed with what
       you saw instead.
  </how-to-verify>
  <acceptance_criteria>
    - The owner reports a result for every scenario in the checklist, not a general impression.
    - Any failure is reported with the literal on-screen text or a screenshot.
    - The executor does NOT mark this plan complete on a partial walkthrough; an unfinished
      checklist is a fail, not a pass.
    - Failures are recorded verbatim in the SUMMARY for the phase verifier to triage — the
      executor does not silently fix them inside this plan unless the fix is a one-line copy
      correction, which must still be listed in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Type "all passed", or list the scenarios that failed and exactly what you saw.</resume-signal>
  <done>Every checklist scenario has a recorded pass or a recorded failure with its literal on-screen text.</done>
</task>

<task type="auto">
  <name>Task 3: Record the outcome and run the final automated gate</name>
  <read_first>
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-VALIDATION.md
    - docs/phase-04.1-manual-checklist.md
  </read_first>
  <files>docs/phase-04.1-manual-checklist.md</files>
  <action>
    1. Run the phase gate from 04.1-VALIDATION.md: `npm test` (full suite) and `npx tsc --noEmit`.
       Record the test-file and test counts.

    2. Run the phase-wide invariant sweep and record each result:
       - `grep -rn "awaitingInput\|AwaitingInput\|awaiting_input" src/ api/ scripts/` — no output.
       - `grep -rn "crc:\|correctionCopy\|correction-card\|correction-keyboards" src/ api/` — no output.
       - `grep -rnE "grams\s*[*/]|/\s*100" api/ mini-app/src/` — no output (CALC-01: no second
         summation anywhere outside `calculate-total.ts` and its two documented input builders).
       - `grep -rnE "window\.confirm|alert\(" mini-app/src/` — no output.
       - `grep -rn "initDataUnsafe" mini-app/src/lib/api-client.ts` — no output.

    3. Fill in the per-task verification map in
       `.planning/phases/.../04.1-VALIDATION.md` is NOT this plan's job — leave that file alone.
       Instead, record in this plan's SUMMARY a requirement-by-requirement table with three
       columns: requirement ID, the automated command that covers it, and the manual checklist
       scenario that covers it, with the observed result of each. Every one of CORRECT-01..08,
       CALC-01, CALC-02 and DIARY-01 must have a row.

    4. Append a short "Результаты последней проверки" section at the end of
       `docs/phase-04.1-manual-checklist.md` recording the date the owner walked it and the outcome,
       so the document carries its own history the next time it is used.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 and `npx tsc --noEmit` exits 0; both counts recorded in the SUMMARY.
    - All five greps in step 2 produce no output; each is recorded in the SUMMARY as run, not as
      assumed.
    - The SUMMARY contains an 11-row requirement coverage table (automated + manual per ID) with an
      observed result in each row.
    - `docs/phase-04.1-manual-checklist.md` ends with a dated results section.
  </acceptance_criteria>
  <done>The phase has a written, evidence-backed answer for every requirement, from both an automated command and a human walkthrough.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated test suite -> "the product works" | The gap this phase's predecessor fell through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-64 | Repudiation | a green suite hiding a broken product | mitigate | Blocking human walkthrough in a real Telegram WebView, requiring a per-scenario result rather than an overall impression; a partial walkthrough is an explicit fail |
| T-04.1-65 | Tampering | the removed text-gate defect quietly returning | mitigate | A dedicated D-04 scenario sends a plain text message while a draft is open and requires it to start a NEW analysis, directly re-testing 04-UAT.md rounds 3-4 |
| T-04.1-66 | Tampering | a zeroed nutrient passing as a real value | mitigate | A CALC-02 scenario deliberately uses a sugar-less ingredient and states that `0` is a failure, checked in both the total and the per-component display |
| T-04.1-67 | Information Disclosure | meal data reappearing in chat | mitigate | The CORRECT-01 scenario explicitly requires confirming the chat message contains no ingredient names, gram values or calories (D-03) |
| T-04.1-68 | Denial of Wallet | the walkthrough itself costing money | accept | A few cents of OpenAI credit for a handful of voice messages and embeddings; stated up front so the owner is not surprised |
</threat_model>

<verification>
- `npm test` passes.
- `npm run typecheck` passes.
- The owner reported a result for every checklist scenario.
- All five invariant greps produce no output.
</verification>

<success_criteria>
Every requirement in this phase has both an automated result and a human-observed result recorded,
and the defect class that motivated the phase was re-tested by hand and behaves correctly.
</success_criteria>

<output>
Create `.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-14-SUMMARY.md` when done.
Include the 11-row requirement coverage table and the owner's verbatim walkthrough result.
</output>
