---
phase: 04.1
plan: 04
type: execute
wave: 3
depends_on: ["04.1-03"]
files_modified:
  - api/drafts/[id].ts
  - api/drafts/[id]/confirm.ts
  - api/drafts/__tests__/get-draft.test.ts
  - api/drafts/__tests__/confirm.test.ts
autonomous: true
requirements: [CORRECT-01, CORRECT-02, CORRECT-07, CALC-01, CALC-02, DIARY-01]

must_haves:
  truths:
    - "The Mini App can load one specific draft and see every component, its grams, its chosen FDC record and its alternative candidates"
    - "The Mini App can confirm a draft as-is and the meal is written to the diary exactly once"
    - "A draft belonging to another user is indistinguishable from a draft that does not exist"
    - "A stale draft is reported as expired rather than silently corrected"
  artifacts:
    - path: "api/drafts/[id].ts"
      provides: "GET one draft, scoped to the validated caller"
      exports: ["default", "createGetDraftHandler"]
    - path: "api/drafts/[id]/confirm.ts"
      provides: "POST confirm -> confirmMeal -> one diary row"
      exports: ["default", "createConfirmHandler"]
  key_links:
    - from: "api/drafts/[id].ts"
      to: "src/application/draft-store.ts readDraft"
      via: "readDraft(db, draftId, user.id)"
      pattern: "readDraft\\("
    - from: "api/drafts/[id]/confirm.ts"
      to: "src/application/confirm-meal.ts confirmMeal"
      via: "confirmMeal(db, draftId, user.id)"
      pattern: "confirmMeal\\("
---

<objective>
Add the two endpoints that bracket the correction flow: reading a draft into the Mini App, and
confirming it into the diary.

Purpose: CORRECT-01 (see the analysis) and CORRECT-02 (confirm it as-is) plus DIARY-01 (it lands
in the right day). These are the two calls without which the Mini App has nothing to show and no
way to finish.
Output: `api/drafts/[id].ts` (GET) and `api/drafts/[id]/confirm.ts` (POST), both tested against
fakes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-PATTERNS.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-03-SUMMARY.md
@src/application/draft-store.ts
@src/application/confirm-meal.ts
@src/bot/handlers/correction.ts
</context>

<interfaces>
From `api/_lib/http.ts` (plan 03):
- `requireUser(req, res): Promise<{ id: number; timezone: string } | null>` — writes 401/403 itself.
- `reasonToStatus(reason: string): number` — throws on `'already_confirmed'`/`'already_deleted'`.
- `sendError(res, status, error)`; `parseDraftId(raw): number | null`;
  `logApiError(operation, draftId, reason)`.

From `api/_lib/draft-response.ts` (plan 03):
- `buildDraftResponse(draft: PersistedDraft): DraftResponse`
- `buildComponentsResponse(draftId, status, saved, localDate, components): DraftResponse`

From `src/application/draft-store.ts`:
- `readDraft(db, draftId, userId): Promise<PersistedDraft | null>` — returns `null` both for
  "does not exist" and "belongs to another user"; this indistinguishability IS the IDOR control.
- `markDraftStatus(db, draftId, userId, status): Promise<void>`

From `src/application/types.ts`:
- `isDraftExpired(status: string, createdAt: Date, now: Date): boolean` — always `false` for any
  status other than `'draft'`, so a confirmed draft never expires (D-06/D-11).

From `src/application/confirm-meal.ts`:
- `confirmMeal(db, draftId, userId, now?): Promise<ConfirmMealResult>` where
  `ConfirmMealResult = { ok: true; diaryId: number; localDate: string; components: DraftComponent[] }
   | { ok: false; reason: 'not_found'|'expired'|'no_local_date'|'empty'|'blocked'|'already_confirmed'|'write_failed'; blockedComponent?: string }`

From `src/bot/handlers/correction.ts` (the dispatch shape being ported to HTTP, read it before
writing either handler — steps 3-5 for the read gate, `case 'confirm'` at lines ~340-377 for the
result branches).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/drafts/[id]</name>
  <read_first>
    - api/_lib/http.ts
    - api/_lib/draft-response.ts
    - api/_lib/__tests__/fakes.ts
    - src/application/draft-store.ts (module header IDOR rule + `readDraft` + `markDraftStatus`)
    - src/bot/handlers/correction.ts (lines ~175-205: the identity -> scoped read -> status/expiry gate sequence)
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-PATTERNS.md (section `api/drafts/[id].ts`)
  </read_first>
  <files>api/drafts/[id].ts, api/drafts/__tests__/get-draft.test.ts</files>
  <behavior>
    - A non-GET method -> 405 `{ error: 'method_not_allowed' }`, and `requireUser` is not called.
    - Missing/invalid initData -> 401 (delegated to `requireUser`), and `readDraft` is never called.
    - A non-numeric `id` -> 400 `{ error: 'invalid_draft_id' }`.
    - `readDraft` returns null -> 404 `{ error: 'not_found' }`.
    - A draft belonging to another user produces the identical 404 body as a draft that does not
      exist — asserted by comparing the two recorded responses for byte equality.
    - `status === 'abandoned'` -> 410 `{ error: 'expired' }`.
    - `status === 'draft'` and older than the 24h TTL -> `markDraftStatus(..., 'abandoned')` is
      called exactly once, then 410 `{ error: 'expired' }`.
    - `status === 'confirmed'` and created 10 days ago -> 200, NOT expired (confirmed drafts are
      exempt), with `saved: true`.
    - A healthy draft -> 200 whose body equals `buildDraftResponse(draft)`, including every
      component's full `candidates` array (the Mini App's candidate picker needs all three) and a
      `total` object.
    - `readDraft` is always called with `(db, draftId, user.id)` — asserted on the recorded
      arguments; never with a user id taken from the request.
  </behavior>
  <action>
    Create `api/drafts/[id].ts`.

    Structure it exactly like the shared template in 04.1-PATTERNS.md's "Auth + validation +
    dispatch pattern": method check, `requireUser`, `parseDraftId`, scoped read, expiry gate,
    respond. Follow the `api/` extensionless-import convention locked in plan 03.

    Export a named factory `createGetDraftHandler(deps: Partial<GetDraftDeps> = {})` whose
    `GetDraftDeps` carries injectable `getDb`, `readDraft`, `markDraftStatus`, `requireUser` and
    `now: () => Date` references defaulting to the real implementations, and
    `export default createGetDraftHandler();` as the Vercel entry point. This mirrors the
    injectable-deps shape `src/bot/handlers/correction.ts` uses (`CorrectionHandlerDeps` with
    `swapCandidate?: typeof swapCandidateReal` overrides) and is what makes the endpoint testable
    without a Vercel dev server. Resolving `getDb()` must happen inside the handler body, not at
    module scope, so importing the module in a test opens no socket.

    The expiry gate must reproduce the bot handler's exact order and semantics: `abandoned`
    status first, then `isDraftExpired(draft.status, draft.createdAt, now())` with a
    `markDraftStatus(..., 'abandoned')` write before responding. Both cases return the SAME
    status and body (410 `expired`) — the Mini App renders the byte-identical
    `Этот разбор устарел, отправь сообщение заново.` string for the 404 and the 410 cases alike
    (04.1-UI-SPEC.md copy table), so the API must not help a caller tell them apart beyond the
    status code the frontend already collapses.

    Log failures only through `logApiError('get-draft', draftId, reason)`.
  </action>
  <verify>
    <automated>npx vitest run api/drafts/__tests__/get-draft.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run api/drafts/__tests__/get-draft.test.ts` passes with one test per bullet in
      `<behavior>`.
    - The test file imports `makeReq`/`makeRes` from `api/_lib/__tests__/fakes` and defines no
      second request/response fake of its own.
    - `grep -c "readDraft(" "api/drafts/[id].ts"` returns at least 1 and every call passes three
      arguments ending in the value returned by `requireUser`.
    - `grep -cE "req\.(body|query)\.(userId|telegramId)" "api/drafts/[id].ts"` returns 0.
    - `grep -c "createDb()" "api/drafts/[id].ts"` returns 0 at module scope (only inside the
      handler function body, via the injected `getDb`).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The Mini App can load exactly its own draft, and cannot learn anything about anyone else's.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/drafts/[id]/confirm</name>
  <read_first>
    - api/drafts/[id].ts (the template established in task 1)
    - api/_lib/http.ts
    - api/_lib/draft-response.ts
    - src/application/confirm-meal.ts (the `ConfirmMealResult` union and `confirmMeal`'s documented order of operations)
    - src/bot/handlers/correction.ts (`case 'confirm'`, lines ~340-377, including the already-confirmed re-read branch)
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-PATTERNS.md (section `api/drafts/[id]/confirm.ts`)
  </read_first>
  <files>api/drafts/[id]/confirm.ts, api/drafts/__tests__/confirm.test.ts</files>
  <behavior>
    - A non-POST method -> 405, `confirmMeal` never called.
    - Missing/invalid initData -> 401, `confirmMeal` never called.
    - `confirmMeal` returns `ok: true` -> 200 with a `DraftResponse` whose `status` is
      `'confirmed'`, `saved` is `true`, `localDate` equals the value `confirmMeal` returned, and
      `components` equals the array `confirmMeal` returned.
    - `reason: 'blocked'` -> 422 with body containing `error: 'blocked'` AND the
      `blockedComponent` name `confirmMeal` supplied (the one place a component name legitimately
      crosses into a response — the UI needs it to name what to fix).
    - `reason: 'empty'` -> 422 `{ error: 'empty' }`.
    - `reason: 'already_confirmed'` -> the handler re-reads the draft and returns 200 with its
      current state, NOT an error (idempotent double-tap).
    - `reason: 'already_confirmed'` when the subsequent re-read returns null -> 410
      `{ error: 'expired' }` rather than a crash.
    - `reason: 'not_found'` -> 404; `'expired'` and `'no_local_date'` -> 410; `'write_failed'` -> 500.
    - `confirmMeal` is called exactly once per request with `(db, draftId, user.id)`.
    - Confirming twice in sequence calls `confirmMeal` twice but produces exactly one `ok: true`
      and one idempotent 200 — asserted on the fake's recorded calls (DIARY-01: never two diary
      rows).
  </behavior>
  <action>
    Create `api/drafts/[id]/confirm.ts` following task 1's factory shape
    (`createConfirmHandler(deps)` + `export default createConfirmHandler()`), with injectable
    `getDb`, `confirmMeal`, `readDraft`, `requireUser`, `now`.

    Map results using the mapping locked in plan 03's `reasonToStatus`, EXCEPT the two branches
    that mapping deliberately refuses: `'already_confirmed'` is an idempotent success (re-read the
    draft with `readDraft(db, draftId, user.id)` and return `buildDraftResponse` at 200), and
    `'blocked'` needs the extra `blockedComponent` field alongside its 422.

    There is no request body — confirm takes no parameters beyond the draft id in the path. Do not
    invent one, and do not accept a components array from the client: the server confirms whatever
    is currently persisted, which is the whole reason `corrections.ts` writes each mutation to the
    database immediately.

    Nothing in this handler may compute or adjust nutrient values: `confirmMeal` already ran
    `calculateTotal` internally and wrote the diary row; the response's `total` comes from
    `buildComponentsResponse`, which calls `summarizeDraft` on the same components.
  </action>
  <verify>
    <automated>npx vitest run api/drafts/__tests__/confirm.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run api/drafts/__tests__/confirm.test.ts` passes with one test per bullet in
      `<behavior>`.
    - `grep -c "confirmMeal(" "api/drafts/[id]/confirm.ts"` returns exactly 1 call site.
    - `grep -cE "grams\s*/\s*100|calculateTotal" "api/drafts/[id]/confirm.ts"` returns 0.
    - The `already_confirmed` test asserts the response status is 200, not 409 or 422.
    - The `blocked` test asserts the response body contains the exact component name string
      returned by the fake `confirmMeal`.
    - `npm test` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Confirming from the Mini App writes exactly one diary row for the correct day, and tapping confirm twice is harmless.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> `/api/drafts/[id]*` | Untrusted; a caller may guess any draft id |
| API -> `diary` table | A successful confirm creates durable health data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-13 | Information Disclosure | `GET /api/drafts/[id]` | mitigate | `readDraft(db, draftId, user.id)` is scoped by both ids; a foreign draft returns the byte-identical 404 body as a nonexistent one, asserted by a dedicated equality test |
| T-04.1-14 | Elevation of Privilege | draft id from the path | mitigate | `parseDraftId` accepts only positive safe integers; the user id is never taken from the request, only from `requireUser` |
| T-04.1-15 | Repudiation / Tampering | double confirm | mitigate | `confirmMeal`'s existing `claimConfirm` compare-and-swap is reused unchanged; the endpoint maps `already_confirmed` to an idempotent 200 rather than retrying |
| T-04.1-16 | Information Disclosure | `blocked` response carrying a component name | accept | The component name is the caller's own submitted meal data, returned only to the validated owner of that draft, and is required for the UI to name what to fix |
| T-04.1-17 | Denial of Service | unbounded reads | accept | Closed beta, low traffic (PROJECT.md); Vercel's platform limits apply. Revisit if beta feedback shows abuse |
</threat_model>

<verification>
- `npm test` passes.
- `npm run typecheck` passes.
- `grep -rcE "grams\s*/\s*100" api/` returns 0.
</verification>

<success_criteria>
The Mini App can fetch one draft with all its candidates and totals, and can turn it into exactly
one diary entry, with foreign and nonexistent drafts indistinguishable.
</success_criteria>

<output>
Create `.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-04-SUMMARY.md` when done.
</output>
