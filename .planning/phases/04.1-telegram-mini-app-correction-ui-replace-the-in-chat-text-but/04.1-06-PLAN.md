---
phase: 04.1
plan: 06
type: execute
wave: 3
depends_on: ["04.1-03"]
files_modified:
  - api/drafts/[id]/remove-component.ts
  - api/drafts/[id]/add-component.ts
  - api/_lib/matching.ts
  - api/drafts/__tests__/remove-component.test.ts
  - api/drafts/__tests__/add-component.test.ts
autonomous: true
requirements: [CORRECT-05, CORRECT-06]

must_haves:
  truths:
    - "The user can remove a component from the analysis, including the last one, without an error"
    - "The user can add a missing component by typing its name, and it is matched against FDC by the same search the original decomposition used"
    - "Adding or removing a component on an already-saved entry keeps the saved diary numbers correct"
    - "Exactly one OpenAI embedding client exists per serverless container, never one per request"
  artifacts:
    - path: "api/drafts/[id]/remove-component.ts"
      provides: "POST remove-component -> corrections.removeComponent"
      exports: ["default", "createRemoveComponentHandler"]
    - path: "api/drafts/[id]/add-component.ts"
      provides: "POST add-component -> corrections.addComponent (FDC search reused)"
      exports: ["default", "createAddComponentHandler"]
    - path: "api/_lib/matching.ts"
      provides: "process-level singleton Embedder + FdcRepository for the API"
      exports: ["getMatchingDeps"]
  key_links:
    - from: "api/drafts/[id]/add-component.ts"
      to: "src/application/corrections.ts addComponent"
      via: "addComponent(db, draftId, userId, rawText, { embedder, repo })"
      pattern: "addComponent\\("
    - from: "api/_lib/matching.ts"
      to: "src/adapters/embeddings/openai-embed.ts"
      via: "createOpenAIEmbedder() memoised at module scope"
      pattern: "createOpenAIEmbedder"
---

<objective>
Add the two endpoints that change which components exist: removing one, and adding a missing one
by typed name (which triggers the same FDC embedding search the original decomposition used).

Purpose: CORRECT-05 and CORRECT-06. `addComponent` is the only endpoint in this phase that spends
money (one OpenAI embedding call per invocation), so it also needs the "one client per container,
never one per request" discipline `correction-wiring.ts` already established for the bot.
Output: two endpoints, one shared matching-deps module, two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-PATTERNS.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-03-SUMMARY.md
@src/application/corrections.ts
@src/bot/correction-wiring.ts
@src/bot/handlers/correction.ts
</context>

<interfaces>
From `src/application/corrections.ts`:
- `export const MAX_COMPONENT_TEXT_LENGTH = 100;`
- `removeComponent(db, draftId, userId, componentIndex, now?): Promise<CorrectionResult>` — removing
  the LAST component is a legitimate first-class state (D-12), not an error and not an abandon.
- `addComponent(db, draftId, userId, rawText, deps: { embedder: Embedder; repo: FdcRepository }, now?): Promise<CorrectionResult>`
  — returns `empty_text` for a blank string, `text_too_long` past `MAX_COMPONENT_TEXT_LENGTH`,
  `match_failed` when the embedding or FDC search throws.

From `src/bot/correction-wiring.ts` (the discipline to mirror, NOT to import — it lives in the bot layer):
- ONE `Embedder` and ONE `FdcRepository` per process. Its module header states the invariant: a
  second embedder built with a different model/dimension count than the offline FDC indexer used
  would silently destroy retrieval quality with no error.

From `src/adapters/embeddings/openai-embed.ts`: `createOpenAIEmbedder()` (zero-argument).
From `src/adapters/fdc-repository.ts`: `createDrizzleFdcRepository(db)`.

From `api/_lib/http.ts` / `api/_lib/draft-response.ts` (plan 03): `requireUser`, `reasonToStatus`,
`sendError`, `parseDraftId`, `logApiError`, `buildComponentsResponse`, `buildDraftResponse`.

From `src/application/confirm-meal.ts`: `recomputeSavedEntry(db, draftId, userId, now?)`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST /api/drafts/[id]/remove-component</name>
  <read_first>
    - api/drafts/[id]/swap-candidate.ts (the endpoint template — copy its structure exactly)
    - api/_lib/http.ts
    - src/application/corrections.ts (`removeComponent` and the D-12 empty-state note above it)
    - src/bot/handlers/correction.ts (`case 'rm'` and its recompute guard)
  </read_first>
  <files>api/drafts/[id]/remove-component.ts, api/drafts/__tests__/remove-component.test.ts</files>
  <behavior>
    - Non-POST -> 405; `removeComponent` never called.
    - Missing/invalid initData -> 401; `removeComponent` never called.
    - Body `{ componentIndex: number }`; non-integer/negative/missing -> 400 `invalid_body`.
    - Valid body -> `removeComponent(db, draftId, user.id, componentIndex)` called exactly once.
    - Removing the last remaining component -> 200 with `components: []`, NOT an error and NOT a
      410; the response's `blockedComponent` is null and the frontend renders the empty state.
    - `ok: true` on a confirmed draft -> `recomputeSavedEntry` called exactly once.
    - `ok: true` on a plain draft -> `recomputeSavedEntry` not called.
    - `'out_of_range'` -> 422; `'not_found'` -> 404; `'expired'` -> 410; `'write_failed'` -> 500.
  </behavior>
  <action>
    Create `api/drafts/[id]/remove-component.ts` as a structural copy of
    `api/drafts/[id]/swap-candidate.ts` (plan 05), changing only the zod schema
    (`{ componentIndex: z.number().int().nonnegative() }`) and the application function called.

    Keep the same factory shape (`createRemoveComponentHandler(deps)` + default export), the same
    `zod` `safeParse` 400 path, the same `status === 'confirmed' -> recomputeSavedEntry` guard, and
    the same `logApiError('remove-component', draftId, reason)` logging discipline.

    Do not special-case the empty-list outcome into an abandon, a 4xx, or an automatic
    `markDraftStatus` call. D-12 makes the empty component list a real state the user recovers
    from with ➕ Добавить; `removeComponent` already implements that and the endpoint just reports it.
  </action>
  <verify>
    <automated>npx vitest run api/drafts/__tests__/remove-component.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file passes with one test per bullet in `<behavior>`, including an explicit
      "removing the last component returns 200 with an empty array" case.
    - `grep -v '^\s*\*' "api/drafts/[id]/remove-component.ts" | grep -c "recomputeSavedEntry"`
      returns at least 1.
    - `grep -cE "markDraftStatus|claimAbandon" "api/drafts/[id]/remove-component.ts"` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Components can be removed, including the last one, and a saved entry stays in sync.</done>
</task>

<task type="auto">
  <name>Task 2: Build the per-container matching dependencies for the API</name>
  <read_first>
    - src/bot/correction-wiring.ts (the whole module header — it states the one-embedder invariant this task must reproduce)
    - src/adapters/embeddings/openai-embed.ts
    - src/adapters/fdc-repository.ts
    - api/_lib/db.ts
  </read_first>
  <files>api/_lib/matching.ts</files>
  <action>
    Create `api/_lib/matching.ts` exporting
    `getMatchingDeps(db: Db): { embedder: Embedder; repo: FdcRepository }`.

    Memoise both at module scope so a warm Vercel container reuses them across invocations —
    building a new OpenAI client per request is pure waste and, worse, risks drifting from the
    single embedding-model/dimension configuration the offline FDC indexer used. Reproduce
    `correction-wiring.ts`'s invariant in this module's own doc comment, in plain terms, including
    the consequence: a different embedding model or dimension count than the indexer used produces
    no error at all, just quietly wrong ingredient matches.

    Construction must be lazy (inside `getMatchingDeps`, guarded by a module-level cache variable),
    never at import time — `createOpenAIEmbedder()` reads `OPENAI_API_KEY` through `loadEnv()`, and
    importing this module in a test must not require a configured environment.

    Add a module comment recording the deployment consequence for plan 13: `OPENAI_API_KEY` must be
    set as a Vercel environment variable, because this one endpoint spends money on the server side.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `node -e "const m=require('node:module');" ` not required; instead:
      `grep -cE "^const (embedder|repo|cached)" api/_lib/matching.ts` shows the cache variables are
      declared but NOT initialised with a `createOpenAIEmbedder()` call at module scope
      (`grep -c "createOpenAIEmbedder()" api/_lib/matching.ts` returns 1, and that single
      occurrence is inside the exported function body).
    - The module doc comment contains the words `dimension` and `indexer` (the invariant is
      restated, not just referenced).
  </acceptance_criteria>
  <done>One embedder and one FDC repository per container, created lazily, with the model-drift invariant written down where the next reader will see it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POST /api/drafts/[id]/add-component</name>
  <read_first>
    - api/drafts/[id]/remove-component.ts (the template just written)
    - api/_lib/matching.ts (task 2)
    - src/application/corrections.ts (`addComponent`, `MAX_COMPONENT_TEXT_LENGTH`, and the doc comment above `addComponent` about Russian text being sent verbatim to an English index)
    - src/bot/handlers/correction.ts (the `add_component` branch and its recompute guard)
  </read_first>
  <files>api/drafts/[id]/add-component.ts, api/drafts/__tests__/add-component.test.ts</files>
  <behavior>
    - Non-POST -> 405; `addComponent` never called; no embedding call made.
    - Missing/invalid initData -> 401; no embedding call made (the paid path is behind auth).
    - Body `{ raw: string }` where `raw` is a string of length 1..`MAX_COMPONENT_TEXT_LENGTH`;
      a non-string, an empty string, or a string longer than that -> 400 `invalid_body` and NO
      embedding call is made.
    - A body that passes schema validation reaches `addComponent(db, draftId, user.id, raw, { embedder, repo })`
      with `raw` passed verbatim (untrimmed) — asserted on the fake's recorded argument.
    - `'empty_text'` -> 422; `'text_too_long'` -> 422; `'match_failed'` -> 422
      `{ error: 'match_failed' }`.
    - `ok: true` -> 200 with a `DraftResponse` containing the new component and its candidates.
    - `ok: true` on a confirmed draft -> `recomputeSavedEntry` called exactly once.
    - `'not_found'` -> 404; `'expired'` -> 410; `'write_failed'` -> 500.
    - The fake embedder records exactly one `embed` call for one successful request, and zero for
      every rejected one.
  </behavior>
  <action>
    Create `api/drafts/[id]/add-component.ts` from task 1's structure, with `getMatchingDeps`
    injected through the deps object so the test supplies fakes and never touches OpenAI.

    Zod schema: `{ raw: z.string().min(1).max(MAX_COMPONENT_TEXT_LENGTH) }` with
    `MAX_COMPONENT_TEXT_LENGTH` imported from `src/application/corrections` — do not hardcode 100.
    Rejecting an over-long string at the schema is defence in depth AND a cost control: it means an
    oversized payload never reaches the paid embedding call. `addComponent` still enforces the same
    bound internally, and that duplication is deliberate; the schema check is about not spending
    money, the internal check is about the correction rule.

    The endpoint must NOT translate, normalise, or pre-process the typed text, and must NOT run its
    own FDC search. `addComponent` already calls the sanctioned `matchIngredient()` path; a second
    matching implementation here is explicitly forbidden by 04.1-RESEARCH.md's Don't Hand-Roll table.

    Same recompute-on-confirmed guard. `logApiError('add-component', draftId, reason)` only —
    never log the typed text.
  </action>
  <verify>
    <automated>npx vitest run api/drafts/__tests__/add-component.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test file passes with one test per bullet in `<behavior>`.
    - The test asserting "no embedding call on a rejected body" inspects a fake embedder's recorded
      call count and asserts 0.
    - `grep -cE "matchIngredient|cosine|embed\(" "api/drafts/[id]/add-component.ts"` returns 0 —
      the endpoint runs no matching of its own.
    - `grep -c "MAX_COMPONENT_TEXT_LENGTH" "api/drafts/[id]/add-component.ts"` returns at least 1
      and `grep -cE "max\(100\)" "api/drafts/[id]/add-component.ts"` returns 0.
    - `grep -v '^\s*\*' "api/drafts/[id]/add-component.ts" | grep -c "recomputeSavedEntry"` returns
      at least 1.
    - `npm test` exits 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Adding a component runs the same FDC search the pipeline uses, spends money only for authenticated, schema-valid requests, and keeps a saved entry in sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> `add-component` | The only API path in this phase that spends money per request |
| API container -> OpenAI | An API key lives in the serverless environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-23 | Denial of Wallet | `add-component` embedding call | mitigate | The paid call sits behind `requireUser` (validated, fresh `initData` for an onboarded user) AND behind a zod `max(MAX_COMPONENT_TEXT_LENGTH)` schema, so no unauthenticated or oversized request reaches OpenAI. Tested by asserting a fake embedder records zero calls for every rejected request |
| T-04.1-24 | Denial of Wallet | per-request client construction | mitigate | `api/_lib/matching.ts` memoises the embedder/repository per container, mirroring `correction-wiring.ts`'s construct-once rule |
| T-04.1-25 | Tampering | duplicate FDC matching logic | mitigate | Endpoint greps forbid `matchIngredient`/`embed(` inside the handler; matching happens only inside the already-tested `addComponent` |
| T-04.1-26 | Tampering / Repudiation | diary desync after add/remove on a saved entry | mitigate | Both endpoints carry the `status === 'confirmed' -> recomputeSavedEntry` guard with dedicated tests |
| T-04.1-27 | Information Disclosure | typed component text in logs | mitigate | `logApiError` takes only operation + draft id + reason; greps forbid interpolating the raw text |
| T-04.1-28 | Denial of Wallet | an authenticated user spamming add-component | accept | No per-user rate limit is added; parity with the Phase 4 bot flow, which had none either, and the beta is a handful of known users. `DAILY_MESSAGE_CAP` covers the far more expensive voice pipeline. Revisit if beta cost logs show it |
</threat_model>

<verification>
- `npm test` passes.
- `npm run typecheck` passes.
- `grep -rc "createOpenAIEmbedder()" api/` returns exactly 1 (only in `api/_lib/matching.ts`).
</verification>

<success_criteria>
Components can be added and removed from the Mini App, added components go through the one
sanctioned FDC search, money is spent only on authenticated valid requests, and saved entries stay
consistent.
</success_criteria>

<output>
Create `.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-06-SUMMARY.md` when done.
</output>
