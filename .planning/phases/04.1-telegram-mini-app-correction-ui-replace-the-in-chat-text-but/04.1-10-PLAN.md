---
phase: 04.1
plan: 10
type: execute
wave: 5
depends_on: ["04.1-04", "04.1-07", "04.1-09"]
files_modified:
  - mini-app/src/components/SummaryTotals.tsx
  - mini-app/src/components/AddComponent.tsx
  - mini-app/src/components/FooterActions.tsx
  - mini-app/src/App.tsx
autonomous: true
requirements: [CORRECT-01, CORRECT-02, CORRECT-06, CORRECT-08, CALC-01, CALC-02, DIARY-01]

must_haves:
  truths:
    - "The user always sees the running calories/protein/fat/carbs/sugar for what is currently in the list"
    - "A nutrient with no FDC data reads 'нет данных', never 0"
    - "The user can add a missing component by typing its name"
    - "The user can confirm a new analysis, and it is saved to the diary"
    - "Reopening a saved entry offers 'Сохранить изменения' and '🗑 Удалить', and delete asks a second time before it happens"
    - "Confirm is disabled, with a reason shown, whenever a component has no FDC match or the list is empty"
  artifacts:
    - path: "mini-app/src/components/SummaryTotals.tsx"
      provides: "the running total block rendered from the API's total object"
      contains: "нет данных"
    - path: "mini-app/src/components/AddComponent.tsx"
      provides: "inline add-a-component field calling the FDC-backed add endpoint"
      contains: "Добавить"
    - path: "mini-app/src/components/FooterActions.tsx"
      provides: "confirm / save+delete / cancel actions incl. the two-step delete prompt"
      contains: "Это навсегда"
  key_links:
    - from: "mini-app/src/components/SummaryTotals.tsx"
      to: "DraftResponse.total"
      via: "props only — no arithmetic"
      pattern: "total\\."
    - from: "mini-app/src/components/FooterActions.tsx"
      to: "POST /api/drafts/:id/confirm and /recompute and /delete and /cancel"
      via: "apiPost per action"
      pattern: "confirm|recompute|delete|cancel"
---

<objective>
Complete the Mini App screen: the running totals block, the add-a-component control, the empty
state, and the footer that confirms a new analysis or saves/deletes a reopened one — including the
two-step delete prompt and the close-the-app-after-save behaviour.

Purpose: this closes CORRECT-02, CORRECT-06 and the user-facing half of CORRECT-08, and it is
where CALC-02 becomes visible — the difference between showing "нет данных" and quietly showing
"0" is the difference between a trustworthy nutrition number and a lie.
Output: three components plus the final `App.tsx` composition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-UI-SPEC.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-08-SUMMARY.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-09-SUMMARY.md
@src/bot/formatting/correction-card.ts
</context>

<interfaces>
From `mini-app/src/types.ts`: `DraftResponse` (`status`, `saved`, `components`, `total`,
`contributingCount`, `blockedComponent`), `NutrientTotal`
(`kcal|proteinG|fatG|carbsG|sugarG: number|null` plus `missingCount` keyed by the same five names).

From `mini-app/src/copy.ts` (plan 08): `previewPrefix` (`≈`), `noData` (`нет данных`),
`partialTotal(known, missing, total)`, `notSavedMarker`, `btnAdd` (`➕ Добавить`),
`btnConfirm` (`✅ Подтвердить`), `btnSaveChanges` (`Сохранить изменения`),
`btnDelete` (`🗑 Удалить`), `btnDeleteYes` (`Да, удалить`), `btnDeleteNo` (`Нет`),
`deletePrompt` (`Удалить запись? Это навсегда.`), `btnCancelDraft` (`✕ Отменить разбор`),
`emptyStateHeading`, `emptyStateBody`, `blockedConfirm(name)`, `componentTooLong`,
`addNotFound(name)`, `savedToDiary` (`Сохранено в дневник`), `saveFailed`, `askComponent`.

From `mini-app/src/lib/api-client.ts`: `apiPost`.
From `mini-app/src/lib/telegram.ts`: `closeApp()`.
From `mini-app/src/App.tsx` (plans 08/09): `updateFromResponse(next: DraftResponse)`.

API contracts:
- `POST /api/drafts/:id/add-component` body `{ raw: string }` -> `DraftResponse`;
  422 codes `text_too_long`, `empty_text`, `match_failed`.
- `POST /api/drafts/:id/confirm` (no body) -> `DraftResponse`; 422 `blocked` (with
  `blockedComponent`), 422 `empty`.
- `POST /api/drafts/:id/recompute` (no body) -> `DraftResponse`; 422 `blocked`; 409 `not_saved`.
- `POST /api/drafts/:id/delete` body `{ confirmed: true }` -> `{ deleted: true }`.
- `POST /api/drafts/:id/cancel` (no body) -> `{ cancelled: true }`.

From `src/bot/formatting/correction-card.ts`'s `formatTotalsBlock` — the exact rendering rules to
reproduce: five labelled lines (`Калории`/ккал, plus protein/fat/carbs/sugar with their units),
`Math.round` applied at RENDER time only, `нет данных` when the value is `null`, and
`partialTotal(rounded+unit, missingCount[key], contributingCount)` when the value is present but
some contributing items lacked that nutrient. Read that function and copy its rules exactly; the
Mini App is now the only place these numbers are ever shown.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SummaryTotals — the running total block</name>
  <read_first>
    - src/bot/formatting/correction-card.ts (`formatTotalsBlock` and the `NUTRIENT_META` label/unit table above it)
    - mini-app/src/types.ts, mini-app/src/copy.ts
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-UI-SPEC.md (Screen Layout item 1, Typography usage rules, and the "нет данных" copy row)
  </read_first>
  <files>mini-app/src/components/SummaryTotals.tsx</files>
  <action>
    Props: `{ total: NutrientTotal, contributingCount: number, saved: boolean }`.

    Render five labelled lines using the SAME labels and units `correction-card.ts`'s
    `NUTRIENT_META` table declares (`Калории`/`ккал`, and the protein/fat/carbs/sugar labels with
    their `г` unit) — copy them from that file so the Mini App and the retired card never disagreed
    on a label. Per line:
    - `total[key] === null` -> render `copy.noData`.
    - value present and `total.missingCount[key] === 0` -> render `Math.round(value)` plus the unit.
    - value present and `missingCount[key] > 0` -> render
      `copy.partialTotal(`${Math.round(value)} ${unit}`, total.missingCount[key], contributingCount)`.

    `Math.round` at render time is the ONLY numeric operation permitted in this file, and it is the
    same rule `calculate-total.ts`'s header states (raw floats from the math layer, rounding at the
    edge, so future weekly sums do not compound rounding error). No addition, no multiplication, no
    division, no reduce over components — this component receives a finished total and displays it.

    When `saved` is false, prefix the block with `copy.previewPrefix` (`≈`) and show
    `copy.notSavedMarker` beneath it, exactly as the chat card did. When `saved` is true, show
    neither — the numbers on screen are the numbers in the diary.

    Typography: the kcal number uses the Heading role (the only place that role is used per the
    UI-SPEC); the other four lines use Caption.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p mini-app/tsconfig.json && node -e "const c=require('fs').readFileSync('mini-app/src/components/SummaryTotals.tsx','utf8'); if(/[^a-zA-Z_]\+=|reduce\(|\*\s|\/\s*100/.test(c.replace(/\/\/.*|\/\*[\s\S]*?\*\//g,''))) throw new Error('arithmetic other than Math.round found'); if(!c.includes('Math.round')) throw new Error('missing render-time rounding'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p mini-app/tsconfig.json` exits 0.
    - The automated arithmetic check above exits 0.
    - `grep -c "noData" mini-app/src/components/SummaryTotals.tsx` returns at least 1, and there is
      no `?? 0`, `|| 0`, or `Number(x) || 0` coalescing anywhere in the file
      (`grep -cE "\?\? 0|\|\| 0" ...` returns 0) — CALC-02.
    - `grep -c "partialTotal" mini-app/src/components/SummaryTotals.tsx` returns 1.
    - The five labels and units are byte-identical to `correction-card.ts`'s `NUTRIENT_META`
      entries — record the comparison in the SUMMARY.
  </acceptance_criteria>
  <done>The user sees the current calories and macros, honestly marked as approximate until saved, with missing data named rather than zeroed.</done>
</task>

<task type="auto">
  <name>Task 2: AddComponent control and the empty state</name>
  <read_first>
    - mini-app/src/copy.ts, mini-app/src/lib/api-client.ts
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-UI-SPEC.md (Screen Layout items 3 and 4)
    - src/application/corrections.ts (`addComponent`'s doc comment, `MAX_COMPONENT_TEXT_LENGTH`)
  </read_first>
  <files>mini-app/src/components/AddComponent.tsx</files>
  <action>
    Props: `{ draftId, onUpdated(next: DraftResponse), onError(message: string) }`.

    Collapsed: a single `copy.btnAdd` (`➕ Добавить`) button carrying `.tap-target`.
    Expanded (in place — no modal, no navigation, per UI-SPEC item 3): a text `<input>` with
    `copy.askComponent` as its placeholder, plus a submit button. `maxLength` on the input is
    `MAX_COMPONENT_TEXT_LENGTH` (100) — mirror the value with a comment naming
    `src/application/corrections.ts` as its source, since this file cannot import from `src/`.

    Submit POSTs `{ raw }` to `/api/drafts/${draftId}/add-component` with the text exactly as
    typed. While in flight, replace the submit button with a spinner in place and disable the input.
    - success -> `onUpdated(data)`, clear the field, collapse back to the button;
    - 422 `text_too_long` -> render `copy.componentTooLong` inline below the field, keep the text;
    - 422 `match_failed` or `empty_text` -> render `copy.addNotFound(typedText)` /
      an appropriate inline message below the field, keep the text;
    - anything else -> `onError(copy.saveFailed)`.
    Never a toast, never an `alert()`.

    Do not translate, normalise, or append a gram value to the typed text. `addComponent` on the
    server already splits a trailing gram number and runs the sanctioned FDC search; adding a
    client-side pre-step would be a second implementation of the same rule.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p mini-app/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit -p mini-app/tsconfig.json` exits 0.
    - `grep -cE "alert\(|window.confirm|<dialog" mini-app/src/components/AddComponent.tsx` returns 0.
    - `grep -c "maxLength" mini-app/src/components/AddComponent.tsx` returns 1 with the value 100,
      and the line above it names `src/application/corrections.ts`.
    - `grep -cE "\.trim\(\)|replace\(|split\(" mini-app/src/components/AddComponent.tsx` returns 0.
    - All three inline error branches (`text_too_long`, `match_failed`, `empty_text`) are present.
  </acceptance_criteria>
  <done>A missing ingredient can be added by typing its name, and every rejection explains itself right where the user typed.</done>
</task>

<task type="auto">
  <name>Task 3: FooterActions and the final App.tsx composition</name>
  <read_first>
    - mini-app/src/App.tsx (current state after plan 09)
    - mini-app/src/components/SummaryTotals.tsx, mini-app/src/components/AddComponent.tsx (tasks 1-2)
    - mini-app/src/copy.ts, mini-app/src/lib/telegram.ts
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-UI-SPEC.md (Screen Layout items 4 and 5, and the "Platform integration requirements" block)
  </read_first>
  <files>mini-app/src/components/FooterActions.tsx, mini-app/src/App.tsx</files>
  <action>
    1. `FooterActions.tsx` — props `{ draftId, saved: boolean, componentCount: number, blockedComponent: string | null, onUpdated, onError, onSaved() }`.

       Sticky footer surface using the `.sticky-footer` class from plan 08 (which already carries
       `env(safe-area-inset-bottom)`).

       Disabled/blocked rule, applying to BOTH primary actions: when `blockedComponent !== null` OR
       `componentCount === 0`, the primary button is disabled and `copy.blockedConfirm(blockedComponent)`
       is rendered directly above it. This mirrors `findBlockingComponent` server-side — the server
       will refuse anyway, so the UI states why in advance instead of letting the user tap into a
       422. When `componentCount === 0` and `blockedComponent` is null, show the empty-state reason
       instead of `blockedConfirm(null)`.

       `saved === false` (a new analysis): a single full-width accent-filled `copy.btnConfirm`
       POSTing to `/api/drafts/${draftId}/confirm`. When `componentCount === 0`, additionally show
       `copy.btnCancelDraft` as a secondary action POSTing to `/api/drafts/${draftId}/cancel`;
       on success call `closeApp()` (there is nothing left to look at).

       `saved === true` (a reopened saved entry): `copy.btnSaveChanges` (accent-filled) stacked
       above `copy.btnDelete` (destructive text, outline style). `Сохранить изменения` POSTs to
       `/api/drafts/${draftId}/recompute`.

       Delete is two-step and entirely client-side: tapping `copy.btnDelete` REPLACES the footer's
       action row in place with `copy.deletePrompt` plus `copy.btnDeleteYes` (destructive) and
       `copy.btnDeleteNo` (neutral). Never `window.confirm()` — it is unreliable inside Telegram's
       WebView. Only `Да, удалить` POSTs `{ confirmed: true }` to `/api/drafts/${draftId}/delete`;
       `Нет` restores the normal action row with no request. On a successful delete, call
       `closeApp()`.

       On a successful confirm or recompute: call `onUpdated(data)`, then `onSaved()`.

       Every button carries `.tap-target`. While any action is in flight, disable all footer
       buttons so a double-tap cannot fire two confirms.

    2. `App.tsx` — final composition:
       - sticky header (`.sticky-header`): `copy.headerLevel1` in Label style plus
         `<SummaryTotals total={draft.total} contributingCount={draft.contributingCount} saved={draft.saved} />`;
       - scrollable list: the `ComponentRow` map from plan 09, OR, when `draft.components.length === 0`,
         the empty state — `copy.emptyStateHeading` and `copy.emptyStateBody` (Body style) shown
         together with the `AddComponent` control and (via `FooterActions`) `✕ Отменить разбор`,
         never as text alone;
       - `<AddComponent …/>` persistently below the list;
       - `<FooterActions …/>`.
       - `onSaved` handler: render `copy.savedToDiary` as an inline confirmation, wait ~800ms, then
         call `closeApp()`. This is the Mini App's only "it worked" signal, because D-03 removed
         every chat-side confirmation message. Implement the delay with a `setTimeout` that is
         cleared on unmount.
       - The screen-level inline error banner from plan 09 stays; a failed footer action shows
         there, never as a full-screen state — a failed save must not hide the user's data.
  </action>
  <verify>
    <automated>npm run miniapp:build && npx tsc --noEmit -p mini-app/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run miniapp:build` exits 0 and emits `mini-app/dist/index.html` plus a JS bundle.
    - `npx tsc --noEmit -p mini-app/tsconfig.json` exits 0.
    - `grep -rcE "window.confirm|alert\(" mini-app/src/` shows 0 for every file.
    - `grep -c "confirmed: true" mini-app/src/components/FooterActions.tsx` returns exactly 1, and
      it is inside the `Да, удалить` handler.
    - `grep -c "closeApp" mini-app/src/components/FooterActions.tsx` returns at least 2 (delete and
      cancel paths) and `grep -c "closeApp" mini-app/src/App.tsx` returns at least 1 (the
      save-then-close path).
    - `grep -rcE "grams\s*[*/]|/\s*100|reduce\(" mini-app/src/components/*.tsx mini-app/src/App.tsx | grep -v ':0$'` produces no output.
    - The disabled-primary-button logic references BOTH `blockedComponent` and a zero component
      count.
    - `grep -c "sticky-footer" mini-app/src/components/FooterActions.tsx` returns 1 and
      `grep -c "sticky-header" mini-app/src/App.tsx` returns 1.
  </acceptance_criteria>
  <done>The Mini App is functionally complete: totals, add, remove, swap, grams, confirm, save, delete — with delete asking twice and the app closing itself after a successful save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| footer actions -> irreversible server operations | Confirm writes a diary row; delete destroys one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-44 | Tampering | accidental delete | mitigate | Two-step in-footer prompt (`Удалить запись? Это навсегда.` + `Да, удалить`/`Нет`); only the second tap sends `{ confirmed: true }`; `window.confirm` forbidden by grep |
| T-04.1-45 | Repudiation | double confirm creating two diary rows | mitigate | All footer buttons disabled while any action is in flight; the server's `claimConfirm` compare-and-swap remains the authoritative guard |
| T-04.1-46 | Tampering | showing 0 instead of missing data | mitigate | `SummaryTotals` greps clean of `?? 0` / `|| 0`; `null` renders `copy.noData` (CALC-02) |
| T-04.1-47 | Tampering | client-side totals | mitigate | `SummaryTotals` receives a finished `NutrientTotal`; an automated check rejects any arithmetic other than `Math.round` in that file (CALC-01) |
| T-04.1-48 | Information Disclosure | a failed save blanking the screen and losing context | mitigate | Footer failures render in the inline banner, never as a full-screen state |
</threat_model>

<verification>
- `npm run miniapp:build` exits 0.
- `npx tsc --noEmit -p mini-app/tsconfig.json` exits 0.
- `npm test` still passes.
- No `window.confirm`, `alert(`, or nutrient arithmetic anywhere under `mini-app/src`.
</verification>

<success_criteria>
Every requirement's user-facing half is reachable from one screen: see the analysis and its
totals, correct it in four ways, add a missing part, confirm it, and later reopen it to edit or
delete — with missing nutrient data named honestly throughout.
</success_criteria>

<output>
Create `.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-10-SUMMARY.md` when done.
</output>
