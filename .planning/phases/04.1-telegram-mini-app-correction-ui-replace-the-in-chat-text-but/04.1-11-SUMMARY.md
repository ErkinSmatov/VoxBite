---
phase: 04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but
plan: 11
subsystem: bot
tags: [grammy, telegram, cleanup, dead-code-removal, drizzle]

# Dependency graph
requires:
  - phase: 04.1 (plans 02, 04, 05, 06, 07)
    provides: Mini App API endpoints (api/drafts/**) covering the same correction surface (swap-candidate, adjust-grams, typed-grams, add-component, remove-component, confirm, cancel, recompute) that the chat-native UI used to provide
provides:
  - Deletion of Phase 4's entire chat-native correction UI (9 files) and the D-04 awaiting-input text-routing gate that produced two live production defects (04-UAT.md rounds 3-4)
  - Application layer (draft-store.ts, corrections.ts, types.ts) with no awaiting_input concept left in code — only the schema column/type remain, owned by plan 12
  - A single surviving correction surface: the Telegram Mini App's HTTP API
affects: ["04.1-12 (drops the awaiting_input column/schema type — now safe since no code references it)"]

# Tech tracking
tech-stack:
  added: []
  patterns: ["deletion over patching for defect-class routing ambiguity", "grep-enforced dead-reference sweeps as a plan's own acceptance criteria"]

key-files:
  created: []
  modified:
    - src/bot/handlers/meal.ts
    - src/bot/bot.ts
    - src/application/corrections.ts
    - src/application/draft-store.ts
    - src/application/types.ts
    - src/application/draft-totals.ts
    - src/application/confirm-meal.ts

key-decisions:
  - "Comment-only references to the deleted files outside the plan's declared files_modified list (in api/**, entry-point-reachability.test.ts, message-editor.test.ts, confirm-meal.ts) were reworded rather than left dangling — the plan's own acceptance criteria requires a repo-wide zero-hit grep, and CLAUDE.md's GSD enforcement treats the plan's stated acceptance criteria as load-bearing."
  - "MIN_GRAMS export from corrections.ts has zero callers outside its own module and test file (flagged per task 3's instruction, not silently deleted — the phase's verifier decides)."

requirements-completed: [CORRECT-03, CORRECT-04, CORRECT-05, CORRECT-06, CORRECT-07]

duration: ~55min
completed: 2026-09-02
---

# Phase 04.1 Plan 11: Delete chat-native correction UI Summary

**Deleted Phase 4's entire chat-native correction UI (9 files, ~2650 lines) and the D-04 awaiting-input text-routing gate that produced two live production defects — the Mini App's HTTP API is now the only way to correct a meal analysis.**

## Performance

- **Duration:** ~55 min
- **Tasks:** 3
- **Files modified/deleted:** 27 (9 deleted, 18 edited)

## Accomplishments

- Removed the exact routing-ambiguity defect class (04-UAT.md rounds 3-4: a typed correction on a reopened saved entry leaking into the paid decomposition pipeline) by deleting the mechanism, not patching another edge case in it.
- `src/application/draft-store.ts` went from 10 to 7 exported functions (removed `findAwaitingDraft`, `setAwaitingInput`, `clearAwaitingInput`); `corrections.ts`'s two mutation functions (`applyTypedGrams`, `addComponent`) lost their `clearAwaitingInput()` side-effect call with zero change to their `CorrectionResult` return shapes.
- Confirmed (via full-repo grep and an export-to-caller sweep) that no dangling reference or dead code survived the removal — `draft-totals.ts` is now the sole copy of the preview-total rule instead of a temporary duplicate.

## Task Commits

1. **Task 1: Delete the chat correction UI and unwire it from the bot** - `4b31787` (feat)
2. **Task 2: Remove the awaiting-input concept from the application layer** - `9feceaa` (feat)
3. **Task 3: Confirm no duplicate or dead logic survived the removal** - `9088995` (docs)

## Files Created/Modified

**Deleted (9):**
- `src/bot/handlers/correction.ts`, `correction.test.ts`
- `src/bot/formatting/correction-card.ts`, `correction-card.test.ts`
- `src/bot/formatting/correction-copy.ts`, `correction-copy.test.ts`
- `src/bot/keyboards/correction-keyboards.ts`, `correction-keyboards.test.ts`
- `src/bot/correction-wiring.ts`

**Edited — core:**
- `src/bot/handlers/meal.ts` — removed `interceptCorrectionText` dep and gate 0.5; kept the hoisted `findOnboardedUser` call; module header now records why the gate was removed (with a 04-UAT.md reference) instead of explaining it
- `src/bot/bot.ts` — removed correction wiring imports/calls and the `crc:`-pattern callback registration
- `src/application/corrections.ts` — removed both `clearAwaitingInput()` call sites and the import; `applyTypedGrams`'s doc comment rewritten
- `src/application/draft-store.ts` — removed the three awaiting-input exports and the `awaitingInput` column entry
- `src/application/types.ts` — removed `PersistedDraft.awaitingInput` and the re-exported `DraftAwaitingInput` type
- `src/application/draft-totals.ts` — module comment updated: no longer describes itself as a temporary duplicate

**Edited — tests:**
- `src/bot/handlers/meal.test.ts`, `src/bot/bot.wiring.test.ts`, `src/bot/bot.wiring.runtime.test.ts` — dropped correction-specific mocks/assertions, kept unrelated registration-order assertions
- `src/application/corrections.test.ts` (27→27 tests, assertions rewritten), `src/application/draft-store.test.ts` (23→16 tests, 7 removed matching the two deleted functions' dedicated describe blocks)
- `src/application/confirm-meal.test.ts` and 7 files under `api/drafts/__tests__/` + `api/_lib/__tests__/draft-response.test.ts` — removed the now-excess `awaitingInput: null` field from `PersistedDraft`-typed fixtures

**Edited — comment-only, outside the plan's `files_modified` list (required by the plan's own grep acceptance criteria):**
- `api/drafts/[id].ts`, `api/drafts/[id]/confirm.ts`, `api/drafts/[id]/swap-candidate.ts`, `api/drafts/[id]/remove-component.ts`, `api/drafts/[id]/cancel.ts`, `api/_lib/matching.ts`, `api/_lib/__tests__/fakes.ts` — reworded provenance comments naming the deleted `src/bot/handlers/correction.ts`/`correction-wiring.ts`
- `src/bot/entry-point-reachability.test.ts`, `src/bot/telegram/message-editor.test.ts`, `src/application/confirm-meal.ts` — reworded remaining `crc:`/`correctionCopy` mentions

## Dependency Sweep (Task 1, before deletion)

```
grep -rn "correction-card\|correction-copy\|correction-keyboards\|handlers/correction\|correction-wiring" src/ scripts/ api/
```

Every hit was inside one of the 9 files on the deletion list, one of the 5 edit targets (`meal.ts`, `bot.ts`, `bot.wiring.test.ts`, `bot.wiring.runtime.test.ts`), or a comment-only provenance reference in `api/**` (not a code dependency) — no hit indicated an incomplete removal list. All comment-only references were subsequently reworded so the post-removal grep (below) is clean.

## Post-removal grep sweep (all pass with zero output)

```
grep -rn "correction-card\|correction-copy\|correction-keyboards\|handlers/correction\|correction-wiring\|CRC_PATTERN\|interceptCorrectionText" src/ scripts/ api/
grep -rn "awaitingInput\|AwaitingInput\|awaiting_input" src/application/ src/bot/ api/
grep -rn "crc:" src/
grep -rn "correctionCopy" src/ api/
grep -rniE "formatTotalsBlock|NUTRIENT_META" src/
```

`grep -rn "awaiting_input" src/db/schema/` still produces output — intentional, plan 12 owns dropping the column.

## Before/After Test Counts (Task 2)

| File | Before | After | Removed | Why |
|---|---|---|---|---|
| `src/application/corrections.test.ts` | 27 | 27 | 0 | Two assertions rewritten (no longer check `clearAwaitingInput`'s side effect); no `it()` blocks deleted |
| `src/application/draft-store.test.ts` | 23 | 16 | 7 | `describe('setAwaitingInput / clearAwaitingInput')` (3 tests) and `describe('findAwaitingDraft')` (4 tests) deleted in full — exactly matching the two removed functions, no unrelated test lost |
| `src/application/draft-store.ts` exported functions | 10 | 7 | 3 | `findAwaitingDraft`, `setAwaitingInput`, `clearAwaitingInput` |

Full suite: 788 → 781 tests, all passing. `npx tsc --noEmit` clean throughout.

## Export-to-Caller Table (Task 3)

### `src/application/draft-store.ts`

| Export | Callers |
|---|---|
| `saveDraft` | `src/application/voice-pipeline.ts` |
| `readDraft` | `src/application/corrections.ts`, `src/application/confirm-meal.ts`, `api/drafts/[id].ts`, `api/drafts/[id]/{confirm,remove-component,swap-candidate,add-component,recompute,typed-grams,adjust-grams}.ts`, `api/_lib/draft-response.ts` |
| `updateDraftComponents` | `src/application/corrections.ts` |
| `markDraftStatus` | `src/application/confirm-meal.ts`, `api/drafts/[id].ts` |
| `claimConfirm` | `src/application/confirm-meal.ts` |
| `claimAbandon` | `src/application/confirm-meal.ts`, `api/drafts/[id]/cancel.ts` |
| `linkDiaryRow` | `src/application/confirm-meal.ts` |

Zero-caller exports: none.

### `src/application/corrections.ts`

| Export | Callers |
|---|---|
| `GRAM_STEP` | `api/drafts/[id]/adjust-grams.ts` |
| `MIN_GRAMS` | **none outside this module and its own test** (flagged below) |
| `MAX_GRAMS` | `api/drafts/[id]/adjust-grams.ts` |
| `MAX_COMPONENT_TEXT_LENGTH` | `api/drafts/[id]/add-component.ts` |
| `parseGrams` | none outside this module and its own test (internal helper also used by `splitTrailingGrams`, not separately re-exported by a caller) |
| `swapCandidate` | `api/drafts/[id]/swap-candidate.ts` |
| `adjustGrams` | `api/drafts/[id]/adjust-grams.ts` |
| `applyTypedGrams` | `api/drafts/[id]/typed-grams.ts` |
| `removeComponent` | `api/drafts/[id]/remove-component.ts` |
| `addComponent` | `api/drafts/[id]/add-component.ts` |

**Flagged zero-caller export: `MIN_GRAMS`.** It is used internally by `parseGrams`/`adjustGrams` inside `corrections.ts` and imported by `corrections.test.ts` for assertions, but no other module (in particular no `api/drafts/**` endpoint) imports it directly — every endpoint delegates bounds enforcement entirely to `corrections.ts`'s functions rather than re-checking the constant itself. This is exported-but-unused-externally, not dead code (the constant is load-bearing inside its own module); not deleted per this task's instruction to report rather than silently remove — the phase's verifier decides whether to keep the export public or narrow it to module-private.

**`parseGrams`** is also exported but has no caller outside `corrections.ts`/its own test — it is invoked internally via `applyTypedGrams` and `splitTrailingGrams`, and unit-tested directly, but no `api/**` handler imports it separately (they all go through `applyTypedGrams`/`addComponent`). Also flagged as exported-but-internally-only for the same reason as `MIN_GRAMS`.

## Decisions Made

- Reworded (rather than left dangling) every comment-only reference to the deleted `src/bot/handlers/correction.ts`/`correction-card.ts`/`correction-copy.ts`/`correction-keyboards.ts`/`correction-wiring.ts` files across `api/**` and a handful of unrelated test files, because the plan's own acceptance criteria mandate a zero-hit grep across the whole of `src/`, `scripts/`, and `api/` — not just the plan's declared `files_modified` list. This is Rule 3 (auto-fix blocking issue) applied to satisfy the plan's stated, machine-checkable acceptance criteria.
- Left `MIN_GRAMS` and `parseGrams` exported (not narrowed to module-private) despite having no external caller today — narrowing visibility is an architectural judgment call the plan reserves for "the phase's verifier," not this executor.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, satisfying the plan's own acceptance criteria] Reworded comment-only references outside the declared `files_modified` list**
- **Found during:** Task 1 (dependency sweep) and Task 3 (final sweep)
- **Issue:** The plan's acceptance criteria for both Task 1 and Task 2 require a repo-wide zero-hit grep across `src/`, `scripts/`, `api/` for the deleted files' names and for `awaitingInput`/`crc:`/`correctionCopy`. Several files not listed in the plan's `files_modified` frontmatter (7 `api/**` files, `src/bot/entry-point-reachability.test.ts`, `src/bot/telegram/message-editor.test.ts`, `src/application/confirm-meal.ts`, `src/application/confirm-meal.test.ts`, and 8 `api/**`/`src/application/` test fixtures with a vestigial `awaitingInput: null` field) contained comment-only or fixture-only references that would have failed those greps.
- **Fix:** Reworded provenance comments to describe the deleted mechanism without naming its file path literally; removed the vestigial `awaitingInput: null` field from `PersistedDraft`-typed test fixtures (required anyway — TypeScript's excess-property check on an object literal typed as `PersistedDraft` would otherwise fail `tsc --noEmit` once the field left the interface).
- **Files modified:** listed under "Files Created/Modified" above, "comment-only" and fixture groups
- **Verification:** all five acceptance-criteria greps for Tasks 1–3 produce zero output; `npm test` (781/781) and `npx tsc --noEmit` both clean
- **Committed in:** `4b31787` (api/** comments, part of Task 1), `9feceaa` (fixtures, part of Task 2), `9088995` (remaining test/comment references, Task 3)

---

**Total deviations:** 1 auto-fixed (Rule 3, satisfying the plan's own stated acceptance criteria)
**Impact on plan:** No scope creep beyond what the plan's machine-checkable acceptance criteria already required. No behavior changed by any of these edits — all are comments or test-fixture shape corrections.

## Issues Encountered

None. The worktree had to be fast-forwarded to `main` before starting (per this plan's `<worktree_branch_check>` warning) since it was forked before plans 02/04/05/06/07 merged — done via `git merge main --ff-only` plus `npm install`, no conflicts.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 12 (drop the `awaiting_input` column/schema type via migration) is now unblocked: `grep -rn "awaiting_input" src/db/schema/` is the only remaining reference in the codebase, exactly as this plan's `<interfaces>` note anticipated.
- The Mini App's API is confirmed as the sole correction surface — `entry-point-reachability.test.ts` still asserts the `web_app` button is the product's only reachable entry point.

## Self-Check: PASSED

- `git log --oneline -3` confirms all three task commits (`4b31787`, `9feceaa`, `9088995`) exist on the current branch.
- `ls src/bot/handlers/correction.ts` etc. confirm all 9 deleted files are absent.
- `npm test` (781/781 passing) and `npx tsc --noEmit` (clean) both re-verified immediately before writing this summary.

---
*Phase: 04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but*
*Completed: 2026-09-02*
