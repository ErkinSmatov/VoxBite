---
phase: 04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but
plan: 01
subsystem: infra
tags: [vite, react, vercel, vitest, typescript, tma.js, mini-app-toolchain]

# Dependency graph
requires: []
provides:
  - "Mini App npm toolchain: @tma.js/init-data-node@2.0.8, react@19.2.8, react-dom@19.2.8 installed"
  - "vite@8.2.2, @vitejs/plugin-react@6.1.1, @vercel/node@10.0.0, @types/react, @types/react-dom dev toolchain"
  - "npm run miniapp:dev / npm run miniapp:build scripts"
  - "vitest.config.ts test.include covers api/**/*.test.ts"
  - "tsconfig.json include covers api/**/* and excludes mini-app/ (separate browser tsconfig)"
  - "mini-app/ Vite React SPA scaffold (tsconfig.json, vite.config.ts, index.html)"
  - "vercel.json static build + SPA rewrite config (excludes /api paths)"
affects: [04.1-02, 04.1-03, 04.1-08, 04.1-13]

# Tech tracking
tech-stack:
  added: [vite@8.2.2, react@19.2.8, react-dom@19.2.8, "@tma.js/init-data-node@2.0.8", "@vitejs/plugin-react@6.1.1", "@vercel/node@10.0.0"]
  patterns:
    - "mini-app/tsconfig.json is a standalone DOM/react-jsx config, does NOT extend root tsconfig.json (root has no DOM lib, must never typecheck browser code)"
    - "mini-app/vite.config.ts resolves root/outDir from import.meta.dirname (not process.cwd()) because it is invoked via --config mini-app/vite.config.ts from the repo root"

key-files:
  created: [mini-app/tsconfig.json, mini-app/vite.config.ts, mini-app/index.html, vercel.json]
  modified: [package.json, package-lock.json, vitest.config.ts, tsconfig.json, .gitignore]

key-decisions:
  - "@twa-dev/sdk deliberately NOT installed - Mini App will read window.Telegram.WebApp directly (Telegram's own injected global), one fewer [ASSUMED]-provenance dependency"
  - "@tma.js/init-data-node@2.0.8 owner-approved on npmjs.com before install (Task 1 blocking-human checkpoint) - confirmed repo under Telegram-Mini-Apps org, no deprecation banner, healthy download count"
  - "vercel.json omits a functions block / runtime pin - Node version comes from package.json engines + Vercel project setting, avoiding a known first-deploy failure mode from a stale hardcoded runtime string"

requirements-completed: [CORRECT-07]

# Metrics
duration: 12min
completed: 2026-08-31
---

# Phase 04.1 Plan 01: Mini App Toolchain Summary

**Installed the Vite/React/Vercel Mini App build toolchain and wired `api/` into the existing `npm test` / `npm run typecheck` commands, so later plans' code under `api/` and `mini-app/` is actually built, tested, and type-checked.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-08-31T11:26:00Z
- **Completed:** 2026-08-31T11:38:32Z
- **Tasks:** 3 (1 checkpoint, 2 auto)
- **Files modified:** 9 (2 created dirs' files: mini-app/tsconfig.json, mini-app/vite.config.ts, mini-app/index.html, vercel.json; 5 modified: package.json, package-lock.json, vitest.config.ts, tsconfig.json, .gitignore)

## Accomplishments
- Installed `@tma.js/init-data-node@2.0.8`, `react@19.2.8`, `react-dom@19.2.8` as runtime deps (owner-approved via Task 1 checkpoint before install), plus `vite@8.2.2`, `@vitejs/plugin-react@6.1.1`, `@vercel/node@10.0.0`, `@types/react@19.2.18`, `@types/react-dom@19.2.5` as dev deps
- Added `miniapp:dev` and `miniapp:build` npm scripts
- `vitest.config.ts` now discovers `api/**/*.test.ts`; `tsconfig.json` now type-checks `api/**/*` while explicitly excluding `mini-app/` (which has its own standalone browser tsconfig)
- Scaffolded `mini-app/` (Vite React SPA config + HTML entry with the Telegram WebApp bridge script) and root `vercel.json` (static build + SPA rewrite, `/api/*` left untouched for serverless functions)
- `.gitignore` updated so `mini-app/dist` and `.vercel` never enter git

## Task Commits

Each task was committed atomically:

1. **Task 1: Owner verifies `@tma.js/init-data-node` is real** - resolved by orchestrator pre-checkpoint (owner replied "Approved" via `npm view` verification of package existence, repo org, and absence of a deprecation banner); no code change, no commit for this task itself
2. **Task 2: Install Mini App dependencies and add build scripts** - `9d70b3b` (feat)
3. **Task 3: Wire api/ into test + typecheck commands and scaffold the Vite/Vercel build** - `e340ad5` (feat)

**Plan metadata:** commit pending (this SUMMARY.md)

## Files Created/Modified
- `package.json` - added `@tma.js/init-data-node`, `react`, `react-dom` runtime deps; `vite`, `@vitejs/plugin-react`, `@vercel/node`, `@types/react`, `@types/react-dom` dev deps; `miniapp:dev`/`miniapp:build` scripts
- `package-lock.json` - regenerated lockfile for the new dependency tree
- `vitest.config.ts` - added `'api/**/*.test.ts'` to `test.include`
- `tsconfig.json` - added `"api/**/*"` to `include`, added `"exclude": ["node_modules", "mini-app"]`
- `mini-app/tsconfig.json` - new standalone browser tsconfig (DOM libs, `react-jsx`, does not extend root)
- `mini-app/vite.config.ts` - new Vite config, React plugin, root/outDir from `import.meta.dirname`
- `mini-app/index.html` - new SPA entry point with `telegram-web-app.js` bridge script and `viewport-fit=cover`
- `vercel.json` - new Vercel build config (buildCommand, outputDirectory, SPA rewrite excluding `/api`)
- `.gitignore` - added `mini-app/dist` and `.vercel`

## Decisions Made
- `@twa-dev/sdk` intentionally not installed — reading `window.Telegram.WebApp` directly avoids installing a second `[ASSUMED]`-provenance package for functionality the raw Telegram-injected global already provides.
- Exact-pinned all new runtime/dev dependency versions (no `^` prefix) to match the existing repo convention, except `@types/react` and `@types/react-dom` which keep the `^` style already used for `@types/node`.
- `vercel.json` deliberately has no `functions` runtime-pin block, per plan guidance, to avoid a known first-deploy failure from a stale/incorrect runtime literal — Node version is inherited from `package.json` `engines` plus the Vercel project setting.

## Deviations from Plan

None - plan executed exactly as written. Task 1's checkpoint was pre-resolved by the orchestrator per the `<checkpoint_resolution>` instructions in this execution's prompt (owner already verified and approved `@tma.js/init-data-node` via `npm view` before this agent was spawned), so Task 1 required no further action beyond confirming Task 2 was gated on that approval, which it was.

## Issues Encountered

The worktree branch (`worktree-agent-a077add790c32e51b`) was 265 commits behind `main` when this agent started — it had branched before the Phase 04.1 planning commits (including this very `04.1-01-PLAN.md`) landed on `main`. The branch had zero commits unique to it (a pure `main`-ancestor), so a `git merge --ff-only main` was performed to bring the plan files and prior phase state into the worktree before execution could begin. This was a safe fast-forward (no divergent history, clean working tree beforehand) and is not a plan deviation — it was necessary just to read the plan file.

## User Setup Required

None - no external service configuration required. (The one owner-facing item, verifying `@tma.js/init-data-node` on npmjs.com, was resolved as a pre-execution checkpoint per this run's instructions, not a post-hoc setup step.)

## Next Phase Readiness

- `api/**/*.test.ts` and `api/**/*` are now discovered by `npm test` and `npm run typecheck` respectively, so plan 03 (which writes the `initData` validation middleware and its tests) will have its tests actually execute rather than being silently skipped.
- `mini-app/` scaffold exists but `mini-app/src/main.tsx` does not yet exist (created in plan 08); `npm run miniapp:build` is expected to fail until then — this matches the plan's explicit statement, not a defect.
- Resolved dependency versions for downstream plans: `@tma.js/init-data-node@2.0.8` (plan 03 must code against this exact package's real exported API), `react@19.2.8`/`react-dom@19.2.8` (plan 08+), `@vercel/node@10.0.0` (plan 02+ for typed `VercelRequest`/`VercelResponse`).
- No blockers for wave 2 plans that depend on this plan's `files_modified`.

---
*Phase: 04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but*
*Completed: 2026-08-31*

## Self-Check: PASSED

All created/modified files verified present on disk; both task commits (`9d70b3b`, `e340ad5`) verified present in `git log`.
