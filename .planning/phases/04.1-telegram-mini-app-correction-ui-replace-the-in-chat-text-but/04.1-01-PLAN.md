---
phase: 04.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - tsconfig.json
  - vercel.json
  - mini-app/tsconfig.json
  - mini-app/vite.config.ts
  - mini-app/index.html
  - .gitignore
autonomous: false
requirements: [CORRECT-07]
user_setup: []

must_haves:
  truths:
    - "npm test discovers and runs test files under api/ as well as src/"
    - "npm run typecheck type-checks api/ without type-checking mini-app/"
    - "npm run miniapp:build produces a static bundle in mini-app/dist"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel build + SPA rewrite configuration"
      contains: "mini-app/dist"
    - path: "mini-app/vite.config.ts"
      provides: "Vite React SPA build config"
      contains: "@vitejs/plugin-react"
    - path: "mini-app/index.html"
      provides: "SPA HTML entry with Telegram WebApp bridge script"
      contains: "telegram-web-app.js"
    - path: "mini-app/tsconfig.json"
      provides: "browser/DOM TypeScript config isolated from the Node root config"
      contains: "react-jsx"
  key_links:
    - from: "vitest.config.ts"
      to: "api/**/*.test.ts"
      via: "test.include glob"
      pattern: "api/\\*\\*/\\*\\.test\\.ts"
    - from: "tsconfig.json"
      to: "api/**/*"
      via: "include array"
      pattern: "api/\\*\\*"
---

<objective>
Add the Mini App toolchain to this repository: the npm packages the new `/api` serverless
functions and the React frontend need, plus the config changes that make the existing
`npm test` / `npm run typecheck` commands actually cover the new `api/` directory.

Purpose: every later plan in this phase writes files under `api/` or `mini-app/`. Without this
plan, tests placed under `api/` are silently never run (vitest's `include` today is
`src/**/*.test.ts` + `scripts/**/*.test.ts` only) and `tsc --noEmit` never sees them — a green
suite would prove nothing.
Output: updated `package.json` deps + scripts, updated `vitest.config.ts` / `tsconfig.json`,
new `vercel.json`, new `mini-app/` build scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Owner verifies the npm package `@tma.js/init-data-node` is real before it is installed</name>
  <read_first>
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-RESEARCH.md (section "Package Legitimacy Audit" and "Standard Stack")
  </read_first>
  <action>
    Pause and present the verification steps below to the owner. Run NO `npm install` command
    until the owner replies. If the owner reports a 404, a deprecation banner, or a repository
    outside the `Telegram-Mini-Apps` GitHub organisation, stop the plan and report instead of
    installing anything.
  </action>
  <what-built>
    Nothing yet. This is a safety gate that runs BEFORE any `npm install`.

    Research flagged `@tma.js/init-data-node` as `[ASSUMED]`: its name was found via a web search,
    not via official Telegram documentation. That package will hold the entire authentication
    boundary of the new API (it decides whether an incoming request really came from Telegram),
    so installing a wrong/typosquatted package here would be the single worst outcome possible in
    this phase. The owner confirms it by eye before it is installed.

    Note: `@twa-dev/sdk` (the second `[ASSUMED]` package in RESEARCH.md) will NOT be installed at
    all. Decision made during planning: the Mini App reads `window.Telegram.WebApp` directly (the
    global that Telegram itself injects, documented at core.telegram.org/bots/webapps), so the
    optional wrapper package is unnecessary and one fewer unverified dependency.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@tma.js/init-data-node in a browser.
    2. Confirm the page loads (a 404 means the package name is wrong — STOP and report it).
    3. Confirm the "Repository" link on the right side points at
       `github.com/Telegram-Mini-Apps/tma.js` (or another repo under the `Telegram-Mini-Apps`
       GitHub organisation).
    4. Confirm there is NO orange "This package has been deprecated" banner at the top.
       (The older package `@telegram-apps/init-data-node` DOES have that banner — that is the
       deprecated predecessor and must not be used.)
    5. Confirm the weekly download count is not near-zero (a few thousand or more is expected).
  </how-to-verify>
  <acceptance_criteria>
    - Owner replies with either "approved" or a description of what did not match.
    - If the npm page 404s, is deprecated, or the repo is not under `Telegram-Mini-Apps`, the
      executor MUST NOT run `npm install` and MUST stop and report instead.
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow the install, or describe what looked wrong.</resume-signal>
  <done>The owner has confirmed on npmjs.com that `@tma.js/init-data-node` exists, is not deprecated, and is published from the Telegram-Mini-Apps organisation — or the plan has stopped.</done>
</task>

<task type="auto">
  <name>Task 2: Install Mini App dependencies and add build scripts</name>
  <read_first>
    - package.json
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-RESEARCH.md (section "Standard Stack" -> Installation)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Only after Task 1 returned "approved".

    Run exactly two install commands from the repository root:
    `npm install @tma.js/init-data-node react react-dom`
    `npm install -D vite @vitejs/plugin-react @vercel/node @types/react @types/react-dom`

    Do NOT install `@twa-dev/sdk` — see Task 1's note.

    Then add these npm scripts to `package.json` `"scripts"`, keeping every existing script
    unchanged:
    - `"miniapp:dev": "vite --config mini-app/vite.config.ts"`
    - `"miniapp:build": "vite build --config mini-app/vite.config.ts"`

    Pin the installed versions exactly as npm resolved them (this repo already pins exact
    versions with no `^` prefix for runtime dependencies — follow that, matching the existing
    style in `dependencies`; `@types/*` entries may keep the `^` style already used for
    `@types/node`).

    Record the resolved version of `@tma.js/init-data-node` in the plan SUMMARY, because plan 03
    must write code against that package's actual exported API, not against a remembered one.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};for(const k of ['@tma.js/init-data-node','react','react-dom','vite','@vitejs/plugin-react','@vercel/node']){if(!d[k])throw new Error('missing dep '+k)};if(d['@twa-dev/sdk'])throw new Error('@twa-dev/sdk must not be installed');if(!p.scripts['miniapp:build'])throw new Error('missing miniapp:build script');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('@tma.js/init-data-node')"` — the module resolves without throwing
      MODULE_NOT_FOUND.
    - `package.json` `dependencies` contains `@tma.js/init-data-node`, `react`, `react-dom`.
    - `package.json` `devDependencies` contains `vite`, `@vitejs/plugin-react`, `@vercel/node`,
      `@types/react`, `@types/react-dom`.
    - `package.json` contains no `@twa-dev/sdk` entry anywhere.
    - `npm run miniapp:build` and `npm run miniapp:dev` exist as scripts.
    - Every pre-existing script (`test`, `typecheck`, `bot`, `db:generate`, `db:migrate`,
      `check-setup`, `index-fdc`, ...) is still present and unchanged.
    - `npm test` still passes (no test change expected yet, only that installs broke nothing).
  </acceptance_criteria>
  <done>The three runtime packages and four dev packages are installed at pinned versions, the two miniapp scripts exist, and the existing suite is still green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire api/ into the test + typecheck commands and scaffold the Vite/Vercel build</name>
  <read_first>
    - vitest.config.ts
    - tsconfig.json
    - .gitignore
    - .planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-RESEARCH.md (section "Recommended Project Structure")
  </read_first>
  <files>vitest.config.ts, tsconfig.json, .gitignore, vercel.json, mini-app/tsconfig.json, mini-app/vite.config.ts, mini-app/index.html</files>
  <action>
    1. `vitest.config.ts`: add `'api/**/*.test.ts'` to the existing `test.include` array, keeping
       `'src/**/*.test.ts'` and `'scripts/**/*.test.ts'`. Do not change `environment: 'node'` or
       `globals: false`. The Mini App's React files are NOT unit-tested in this phase (verified
       by hand inside a real Telegram WebView per 04.1-VALIDATION.md) so no jsdom environment and
       no `mini-app/**` glob is added.

    2. `tsconfig.json`: add `"api/**/*"` to `include`. Add a top-level
       `"exclude": ["node_modules", "mini-app"]` so the root Node config (which has
       `"lib": ["ES2023"]` and `"types": ["node"]`, i.e. no DOM) never tries to type-check the
       browser code. Leave every existing compilerOption untouched — in particular
       `strict`, `noUncheckedIndexedAccess`, and `"moduleResolution": "Bundler"`.

    3. New `mini-app/tsconfig.json` — a standalone browser config, NOT extending the root one:
       `target`/`lib` including `"DOM"` and `"DOM.Iterable"` plus `ES2023`,
       `"jsx": "react-jsx"`, `"module": "ESNext"`, `"moduleResolution": "Bundler"`,
       `"strict": true`, `"noUncheckedIndexedAccess": true`, `"noEmit": true`,
       `"types": []`, `"include": ["src/**/*"]`.

    4. New `mini-app/vite.config.ts`: `defineConfig` from `vite`, `plugins: [react()]` using
       `@vitejs/plugin-react`, `root` set to the `mini-app` directory itself, `base: '/'`,
       `build.outDir` resolving to `mini-app/dist`, `build.emptyOutDir: true`. Because the
       config is invoked from the repo root via `--config mini-app/vite.config.ts`, resolve
       `root`/`outDir` from `import.meta.dirname` rather than from `process.cwd()`.

    5. New `mini-app/index.html`: minimal HTML5 document, `<html lang="ru">`,
       `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">`
       (the `viewport-fit=cover` part is required for the `env(safe-area-inset-*)` padding that
       04.1-UI-SPEC.md mandates on the sticky header/footer), a
       `<script src="https://telegram.org/js/telegram-web-app.js"></script>` tag in `<head>`
       loaded BEFORE the app bundle (this is the script that creates `window.Telegram.WebApp`),
       `<div id="root"></div>`, and `<script type="module" src="/src/main.tsx"></script>`.
       `main.tsx` does not exist yet — it is created in plan 08; the build is not expected to
       succeed until then.

    6. New `vercel.json` at the repo root:
       - `"buildCommand": "npm run miniapp:build"`
       - `"outputDirectory": "mini-app/dist"`
       - a `rewrites` entry sending every non-`/api` path to `/index.html` so the SPA is served
         at `/` while Vercel's own filesystem routing still picks up `api/*.ts` as serverless
         functions.
       Do NOT add a `functions` block pinning a runtime string — the Node major version comes
       from `package.json`'s existing `"engines": { "node": ">=22" }` plus the Vercel project
       setting, and a wrong runtime literal is a known first-deploy failure. Plan 13 verifies the
       deployed runtime.

    7. `.gitignore`: add `mini-app/dist` and `.vercel` (the local directory the Vercel CLI
       creates, which contains project ids and must never be committed).
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && node -e "const c=require('fs').readFileSync('vitest.config.ts','utf8');if(!/api\/\*\*\/\*\.test\.ts/.test(c))throw new Error('vitest include missing api glob');const t=require('fs').readFileSync('tsconfig.json','utf8');if(!/api\/\*\*/.test(t))throw new Error('tsconfig include missing api');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' vitest.config.ts | grep -c "api/\*\*/\*.test.ts"` returns 1.
    - `npx tsc --noEmit` exits 0 and does not report any error whose path starts with `mini-app/`.
    - `npm test` exits 0 with the same number of passing tests as before this plan (no test
      files exist under `api/` yet, so the count must not change).
    - `vercel.json` parses as JSON (`node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"`)
      and its `outputDirectory` value is exactly `mini-app/dist`.
    - `mini-app/index.html` contains the literal string
      `https://telegram.org/js/telegram-web-app.js` and the literal string `viewport-fit=cover`.
    - `git check-ignore mini-app/dist` exits 0.
  </acceptance_criteria>
  <done>`npm test` and `npm run typecheck` both cover `api/` and both still pass; the Vite/Vercel scaffold files exist and parse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> this repository | Third-party code enters the build and, for `@tma.js/init-data-node`, becomes the authentication boundary |
| git repository -> Vercel | Anything committed is deployed publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-SC | Tampering | `npm install @tma.js/init-data-node` | mitigate | Task 1 blocking-human checkpoint verifying the npmjs.com page, repo org, and absence of a deprecation banner before any install runs; `@twa-dev/sdk` dropped entirely rather than installed on an `[ASSUMED]` name |
| T-04.1-01 | Information Disclosure | `.vercel` directory, `mini-app/dist` | mitigate | Both added to `.gitignore` in task 3 so project ids and build output never enter git |
| T-04.1-02 | Tampering | test discovery config | mitigate | `api/**/*.test.ts` added to vitest `include` so the auth-boundary tests written in plan 03 actually execute — silently-unrun tests are indistinguishable from passing ones |
</threat_model>

<verification>
- `npm test` passes.
- `npm run typecheck` passes.
- `node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"` exits 0.
- No `@twa-dev/sdk` anywhere in `package.json`.
</verification>

<success_criteria>
Test and typecheck commands cover `api/`, the Vite + Vercel scaffold exists, and the one
security-critical dependency was human-verified before installation.
</success_criteria>

<output>
Create `.planning/phases/04.1-telegram-mini-app-correction-ui-replace-the-in-chat-text-but/04.1-01-SUMMARY.md` when done.
Record the exact resolved version of `@tma.js/init-data-node` in it — plan 03 needs it.
</output>
